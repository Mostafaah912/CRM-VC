# HeyMode Customer OS — Architecture Log

این فایل حافظه‌ی مشترک پروژه است. با هر تصمیم معماری و هر یافته‌ی راستی‌آزمایی به‌روز می‌شود.

## محیط توسعه (Local Mac) — ثبت‌شده در شروع پروژه

- سیستم: MacBook Pro (MacBookPro12,1) / Intel x86_64 / macOS 12.7.6
- PHP: 8.4 (از طریق Laravel Herd)
- Laravel: نصب‌شده با starter kit — React + Inertia + Laravel built-in auth (با 2FA و Passkeys)
- Node.js: 22.17.0 / npm: 10.9.2
- Composer: نصب‌شده
- PostgreSQL: 15.19 ← توجه: نسخه ۱۵ است، نه ۱۶
- Redis: 7.4.6
- تست: Pest
- Homebrew: نصب‌شده ولی به‌عنوان وابستگی پروژه استفاده نمی‌شود (محدودیت Intel + macOS 12)

## تصمیم مهم درباره نسخه PostgreSQL

- محیط توسعه روی PostgreSQL 15 است. **روی VPS هم باید PostgreSQL 15 نصب شود**، نه 16،
  تا اختلاف نسخه بین توسعه و تولید نباشد.
- تمام SQLهای سند (NTILE، percentile_cont، JSONB، generate_series، window functions)
  روی نسخه 15 پشتیبانی می‌شوند.

## استقرار

- فعلاً VPS استفاده نمی‌شود. کل توسعه، تست و دیباگ روی مک محلی انجام می‌شود.
- استقرار روی VPS لینوکس در Sprint 8 انجام می‌شود.
- Supervisor فقط بخشی از استقرار VPS است، نه محیط توسعه. روی مک، Queue Worker
  به‌صورت دستی با `php artisan horizon` اجرا می‌شود.

## یافته‌های راستی‌آزمایی (P0-00)

### V8 — حجم کل تاریخچه سفارش

• تعداد کل سفارش‌ها (هر وضعیت): 24421
✓ در محدوده قابل مدیریت برای Full Sync کامل.
• نمونه بررسی‌شده در این اجرا: 600 سفارش اخیر

### V2 — وضعیت‌های سفارش (Slug واقعی)

• وضعیت 'completed': 296 سفارش در نمونه
• وضعیت 'cancelled': 239 سفارش در نمونه
• وضعیت 'processing': 59 سفارش در نمونه
• وضعیت 'pending': 3 سفارش در نمونه
• وضعیت 'refunded': 3 سفارش در نمونه

• در config/woo.php، realized_statuses باید شامل معادل‌های پرداخت‌شده/ارسال‌شده/تکمیل‌شده باشد.
• پیش‌فرض سند: ['processing','shipped','completed'] — با لیست بالا تطبیق دهید.
⚠ وضعیت 'shipped' در نمونه دیده نشد. اگر وضعیت ارسال سفارشی دارید، Slug واقعی‌اش را پیدا و ثبت کنید.

### V1 — واحد پول و اعشار

• کد واحد پول ووکامرس: IRT
• تعداد رقم اعشار قیمت: 0
• نمونه مبلغ یک سفارش واقعی: 403880
⚠ این عدد را با مبلغی که در پنل همان سفارش می‌بینید مقایسه کنید:
⚠ اگر یکی بود → واحد ذخیره تومان است. اگر عدد API ده برابر بود → ریال است (ضریب در config/hm.php).

### V3 و V4 — خرید مهمان و وجود موبایل

• سفارش مهمان (customer_id=0): 0 (0٪ نمونه)
• سفارش کاربر ثبت‌نامی: 600
• در نمونه، خرید مهمان دیده نشد.

• سفارش بدون موبایل: 0 (0٪ نمونه)
✓ کمتر از ۱٪ سفارش بدون موبایل — کلید هویت موبایل امن است.

### V5 و V10 — موبایل مشترک و اتکاپذیری ایمیل

• موبایل‌هایی که با بیش از یک نام خانوادگی سفارش داده‌اند: 1
⚠ این‌ها در identity_conflicts برای بازبینی ثبت می‌شوند (مشتری تقسیم نمی‌شود).

• سفارش‌های با ایمیل معتبر: 2 (0.3٪ نمونه)
• ایمیل ناقص است — طبق سند فقط شناسه ثانویه و بدون تطبیق در فاز ۱.

### V6 و V7 — ساختار عودت

• سفارش نمونه با عودت: #53960
• مبلغ عودت: 11340000
✓ عودت دارای line_items است → عودت در سطح قلم قابل محاسبه است (طبق فرض سند).
• V7: وب‌هوک عودت به‌صورت پیش‌فرض فرض نمی‌شود؛ Polling ساعتی استفاده می‌شود.

### V9 — پوشش SKU روی محصولات و واریانت‌ها

• نمونه محصول بررسی‌شده: 100
• محصول بدون SKU: 0
✓ همه محصولات نمونه SKU دارند.
• واریانت بررسی‌شده (از 3 محصول متغیر): 15، بدون SKU: 0
✓ همه واریانت‌های بررسی‌شده SKU دارند.

## تصمیم‌های قطعی بعد از P0-00

- **واحد پول: تومان (IRT).** مبالغ به تومان و بدون اعشار ذخیره می‌شوند (bigint).
  هیچ تبدیلی لازم نیست — API هم تومان می‌دهد. (تأییدشده با مبلغ نمونه ۴۰۳٬۸۸۰.)
- **realized_statuses = ['processing','completed']** (وضعیت shipped وجود ندارد).
- **خرید مهمان: غیرفعال.** همه سفارش‌ها customer_id دارند. مسیر woo_guest_order بلااستفاده.
- **موبایل: ۱۰۰٪ موجود.** کلید هویت موبایل بدون مسیر Fallback.
- **عودت: دارای line_items.** عودت سطح قلم پیاده می‌شود. (نمونه: عودت ۱٬۱۳۴٬۰۰۰ تومان)
- **ایمیل: تقریباً وجود ندارد (۰٫۳٪).** حتی به‌عنوان شناسه ثانویه هم استفاده نمی‌شود.
- **کل سفارش‌ها: ~۲۴٬۴۲۱.** Full Sync کامل بدون محدودیت زمانی.

## P0-01 — Scaffold RTL + shadcn/ui (نسخه‌های واقعی نصب‌شده)

اسکلت Laravel با `laravel new` از قبل با استارتر React + Inertia + Fortify (2FA و Passkeys) و shadcn/ui (استایل `new-york`، Tailwind v4 CSS-first) ساخته شده بود. در این گام دوباره Scaffold نشد؛ فقط RTL اضافه و نسخه‌های واقعی ثبت شد.

- Laravel Framework: **13.32.0**
- PHP: **8.4.15** (از طریق Herd)
- Inertia (inertiajs/inertia-laravel): **3.3.4**
- Laravel Fortify: **1.39.0**
- Pest: **5.2.1**
- React / React DOM: **19.3.0**
- @inertiajs/react: **3.7.1**
- TypeScript: **5.9.3**
- Tailwind CSS: **4.3.3** (بدون `tailwind.config.js` — پیکربندی CSS-first در `resources/css/app.css`)
- Vite: **8.3.0**
- Node.js: **22.17.0** / npm: **10.9.2**
- shadcn/ui: از قبل نصب‌شده (`components.json`، استایل `new-york`، `baseColor: neutral`، آیکون‌ها از `lucide-react`) — تأیید شد که `npm run build` سبز است.

### تغییرات RTL

- طبق تصمیم D10 (فقط فارسی، بدون i18n)، `resources/views/app.blade.php` اکنون همیشه `lang="fa" dir="rtl"` دارد — دیگر وابسته به `app()->getLocale()` نیست، چون این اپ زبان دوم ندارد.
- Tailwind v4 از `rtl:`/`ltr:` بر پایه ویژگی `dir` به‌صورت داخلی پشتیبانی می‌کند؛ نیازی به پلاگین یا `tailwind.config.js` جداگانه نبود.
- `APP_LOCALE` در `.env` عمداً روی `en` باقی ماند — طبق D10 متن رابط کاربری در خود کامپوننت React فارسی نوشته می‌شود، نه از طریق فایل‌های ترجمه Laravel؛ تغییر `APP_LOCALE` بدون فایل‌های `resources/lang/fa` پیام‌های اعتبارسنجی Fortify را می‌شکند و در Scope این گام نیست.
- ⚠ **ریسک ثبت‌شده برای Sprint 3:** کامپوننت‌های shadcn/ui استارتر (sidebar، dropdown، breadcrumb و…) از `ml-`/`mr-`/`left-`/`right-` فیزیکی استفاده می‌کنند که با `dir="rtl"` خودکار flip نمی‌شوند. اصلاح آن‌ها به کلاس‌های منطقی (`ms-`/`me-`/`start-`/`end-`) هنگام ساخت UI واقعی در Sprint 3 انجام می‌شود، نه اینجا.

## P0-02 — Horizon + تأیید Queue روی Redis

- `laravel/horizon` نصب شد (نسخه منتشرشده با `composer.lock`: **^5.49**، از طریق `composer require laravel/horizon` + `php artisan horizon:install`).
- `app/Providers/HorizonServiceProvider.php` ثبت شد در `bootstrap/providers.php`. Gate پیش‌فرض `viewHorizon` دست‌نخورده ماند (فقط env محلی مجاز است)؛ محدودسازی واقعی به نقش Owner زمانی انجام می‌شود که ماژول Core/Permissions (P0-07) و کاربران واقعی (Sprint 8) وجود داشته باشند — طبق چک‌لیست سخت‌سازی بخش ۲۱، نه اینجا.
- `config/horizon.php`: صف `supervisor-1` روی ترتیب اولویت بخش ۲۲ تنظیم شد: `['critical','sync','metrics','ai','default']`. تعریف Job واقعی برای هرکدام در Sprintهای بعدی (Sync/Metrics/AI) اضافه می‌شود.
- **اصلاح باگ در `.env`:** `DB_CONNECTION` دو بار تعریف شده بود (`sqlite` سپس `pgsql`) — خط تکراری حذف شد (مقدار مؤثر همیشه `pgsql` بود، چون phpdotenv خط دوم را می‌گیرد، ولی تکرار گمراه‌کننده بود).
- **`QUEUE_CONNECTION` و `CACHE_STORE` از `database` به `redis` تغییر کردند** — طبق معماری بخش ۰۶ («Redis: queue + cache + lock»). `.env.example` هم با همین مقادیر و با `DB_CONNECTION=pgsql` به‌روز شد تا نمونه واقعی پروژه را نشان دهد، نه پیش‌فرض SQLite اسکلت Laravel.
- **یافته محیطی مهم:** روی این مک، Redis 7.4.6 نصب است (`/usr/local/bin/redis-server`) ولی به‌صورت سرویس خودکار (launchd/brew services) اجرا نمی‌شود — باید دستی با `redis-server --daemonize yes` بالا بیاید. این باید در مستندات Setup پروژه (بعداً) یا اسکریپت dev ثبت شود؛ فعلاً فقط اینجا یادداشت شد.
- **تأیید سرتاسری صف:** یک Job موقت (`HorizonProbeJob`, بعد از تست حذف شد) روی صف پیش‌فرض dispatch شد، `php artisan horizon` آن را از Redis برداشت، اجرا کرد و نتیجه در Cache (که آن هم الان روی Redis است) قابل مشاهده بود. `horizon:status` → `running`، `horizon:terminate` تمیز خاتمه داد.
- PostgreSQL 15.19 هم تأیید شد در دسترس است (`php artisan db:show`).

## اصلاح زیرساخت تست — قبل از P0-03

`phpunit.xml` استارتر روی `DB_CONNECTION=sqlite` و `DB_DATABASE=:memory:` تنظیم شده بود که مستقیماً قانون بخش ۳/۸ CLAUDE.md («هرگز SQLite») را نقض می‌کرد. برای اینکه اولین تست‌های TEST-FIRST (P0-03) درست شروع شوند:

- دیتابیس جدا `heymode_testing` روی همان PostgreSQL محلی ساخته شد (با اجازه کاربر، چون نقش `heymode` نیاز به `CREATEDB` داشت که ابتدا نداشت).
- اتصال/رمز عبور تست در `.env.testing` (در `.gitignore`، هرگز Commit نمی‌شود) قرار گرفت؛ `phpunit.xml` دیگر `DB_CONNECTION`/`DB_DATABASE`/`DB_URL` را Override نمی‌کند تا مقادیر واقعی از `.env.testing` خوانده شوند.
- نتیجه: `RefreshDatabase` هر بار `heymode_testing` را Migrate می‌کند، نه دیتابیس توسعه `heymode` را — داده محلی دست‌نخورده می‌ماند.

## P0-03 — PhoneNormalizer، JalaliDate، Money (TEST FIRST)

- `App\Support\PhoneNormalizer::normalize()` طبق بخش ۰۸ PRD: علامت‌های RTL/LTR (U+200E/U+200F) حذف، ارقام فارسی و عربی-هندی به ASCII، سپس تشخیص فرمت (`0912...`، `912...`، `+98912...`، `0098912...`، با فاصله/خط‌تیره) و تبدیل به خروجی قطعی `989XXXXXXXXX`. برای خالی/کوتاه/غیرموبایل (شهری مثل `021`/`031`)/غیرایرانی، `InvalidPhoneException` می‌اندازد — هرگز مقدار نامعتبر برنمی‌گرداند.
- `App\Support\JalaliDate` تبدیل شمسی↔میلادی با الگوریتم استاندارد چرخه ۳۳ ساله (همان الگوریتمی که در P0-04 معادل PL/pgSQL آن ساخته می‌شود تا دو طرف Stack هم‌رأی باشند). ورودی همیشه ابتدا به `Asia/Tehran` تبدیل می‌شود، طبق قانون «ذخیره UTC، نمایش Asia/Tehran + شمسی».
- `App\Support\Money` فرمت‌کننده/Parser مبلغ `int` تومان: نمایش با جداکننده هزارگان فارسی و پسوند «تومان»، و `parseToman()` که هر ورودی غیر عدد صحیح خالص (مثلاً با اعشار) را رد می‌کند تا «هرگز float برای پول» در همان مرز ورودی اعمال شود.
- **تست قبل از کد:** هر سه فایل تست (`tests/Unit/Support/{PhoneNormalizerTest,JalaliDateTest,MoneyTest}.php`) قبل از پیاده‌سازی نوشته و اجرا شدند (قرمز با خطای «Class not found»)، سپس پیاده‌سازی تا سبز شدن ادامه یافت.
- تست‌های JalaliDate شامل تاریخ‌های Nowruz شناخته‌شده (۱۳۹۹ تا ۱۴۰۳)، روز کبیسه ۳۰ اسفند ۱۳۹۹، و Round-trip روی بازه وسیع تاریخ میلادی — همگی سبز، که هم الگوریتم و هم لنگرهای تاریخی حافظه‌محور را تأیید متقابل می‌کنند.

## P0-04 — توابع PL/pgSQL جلالی

Migration جدید: `database/migrations/2026_09_18_103627_create_extensions_and_jalali_functions.php` (معادل «001 extensions + jalali functions» بخش ۰۹ PRD؛ اسم فایل از قرارداد Timestamp خود Laravel پیروی می‌کند، نه شماره‌گذاری متنی PRD).

- `CREATE EXTENSION IF NOT EXISTS pg_trgm` — برای GIN Index نام مشتری/محصول در Sprint 1.
- `to_jalali(timestamptz) RETURNS text` — همان الگوریتم چرخه ۳۳ ساله `App\Support\JalaliDate` (P0-03) ولی در PL/pgSQL، تا SQL طرف Metrics/Cohort (Sprint 4-6) با PHP هم‌رأی باشد. ورودی ابتدا به `Asia/Tehran` تبدیل می‌شود (طبق قانون ذخیره UTC/نمایش Tehran). `STABLE` علامت‌گذاری شد نه `IMMUTABLE`، چون تبدیل با نام Zone به‌صورت رسمی در PostgreSQL «Immutable» تضمین‌شده نیست.
- `to_jalali_month(timestamptz) RETURNS text` — همان مقدار را به ۷ کاراکتر `YYYY-MM` کوتاه می‌کند؛ دقیقاً فرمت ستون `cohort_month varchar(7)`.
- `jalali_month_diff(text, text) RETURNS int` — اختلاف ماه بین دو رشته `YYYY-MM` (`IMMUTABLE`، چون فقط محاسبه رشته‌ای است، وابسته به Timezone نیست).
- **تست اول:** `tests/Integration/JalaliFunctionsTest.php` (۱۷ تست) قبل از اجرای Migration نوشته شد و روی PostgreSQL واقعی (`heymode_testing`، از طریق `RefreshDatabase`) اجرا می‌شود؛ به همین دلیل به `tests/Integration` نیاز بود که به `phpunit.xml` و `tests/Pest.php` (خط `->in('Feature', 'Integration')`) اضافه شد.
- **یافته حین تست:** یک فرض غلط در تست اولیه («۳۰ اسفند ۱۴۰۲ وجود دارد») رد شد — با شمارش مستقیم روز بین دو لنگر Nowruz تأییدشده (۱۴۰۲-۰۱-۰۱ = ۲۰۲۳-۰۳-۲۱ و ۱۴۰۳-۰۱-۰۱ = ۲۰۲۴-۰۳-۲۰، دقیقاً ۳۶۵ روز فاصله) ثابت شد سال ۱۴۰۲ عادی (۳۶۵ روزه) است و اسفندش ۲۹ روز دارد؛ تست اصلاح شد، نه تابع.
- یک تست دیگر مقادیر تولیدشده توسط SQL را مستقیماً با خروجی زمان‌اجرای `App\Support\JalaliDate::format()` روی چند Timestamp نمونه مقایسه می‌کند تا توافق دو طرف Stack تضمین شود.
- **Idempotent/Rollback:** `up()` از `CREATE EXTENSION IF NOT EXISTS` و `CREATE OR REPLACE FUNCTION` استفاده می‌کند (اجرای دوباره خطا نمی‌دهد). `down()` هر سه تابع و پسوند را با `IF EXISTS` حذف می‌کند. رفتار با `migrate` → `migrate:rollback --step=1` → `migrate` دوباره روی دیتابیس توسعه دستی تأیید شد.
- **اصلاح جانبی:** یک خطای از پیش موجود PHPStan در `config/horizon.php` (از P0-02، `Str::slug()` با نوع `bool|string` از `env()`) هنگام اجرای Larastan روی کل پروژه کشف و با `(string)` Cast درست‌شده — چون Larastan اکنون برای اولین‌بار روی کل مسیر `app/` اجرا شد.

## P0-05 — Core Migrations

جدول‌های Core بخش ۰۹ PRD ساخته شدند: `roles`، `permissions`، `role_user`، `permission_role`، `permission_overrides`، `settings`، `audit_logs`. `users` هم با یک Migration جدید تکمیل شد (`is_active`، `last_login_at`، و کوتاه‌سازی طول `name`/`email` به ۱۲۰/۱۶۰ طبق Schema). `email_verified_at` و `two_factor_recovery_codes` عمداً حذف نشدند — فهرست جدول PRD جامع همه ستون‌های موردنیاز Framework نیست و این دو برای تأیید ایمیل و بازیابی 2FA در P0-06 لازم‌اند.

تمام Foreign Key‌ها صریح‌اند با رفتار `onDelete` مشخص: پیوندهای Pivot (`role_user`, `permission_role`) و خود `permission_overrides.user_id/permission_id` با حذف والد Cascade می‌شوند؛ ارجاع‌های «چه کسی این را ساخت/آخرین‌بار تغییر داد» (`permission_overrides.created_by`, `settings.updated_by`, `audit_logs.user_id`) با حذف کاربر `SET NULL` می‌شوند تا رکورد تاریخی از بین نرود.

CHECK Constraintها (چون Laravel فعلاً متد Fluent برای CHECK ندارد، با `DB::statement` بعد از `Schema::create` اضافه شدند):

- `permission_overrides.effect IN ('allow','deny')`
- `audit_logs.actor_type IN ('user','system','ai')` (پیش‌فرض `'user'`)

### 🔴 یافته بحرانی: Timezone پیش‌فرض اتصال PostgreSQL

هنگام تست، `password can be reset with valid token` (یک تست از پیش موجود در استارتر) شکست خورد. علت: `SHOW timezone` روی این اتصال `Asia/Tehran` برمی‌گرداند (تنظیم پیش‌فرض سرور محلی)، نه UTC. Laravel رشته‌های Datetime بدون Offset («Naive») می‌نویسد و فرض می‌کند UTC هستند؛ PostgreSQL چنین رشته‌ای را وقتی وارد ستون `timestamptz` می‌شود با Timezone **نشست** (نه UTC) تفسیر می‌کند. نتیجه: هر مقدار `timestamptz` نوشته‌شده توسط اپ ۳ ساعت و ۳۰ دقیقه جابه‌جا می‌شد — یک باگ بی‌صدا که مستقیماً قانون «Timestamps ذخیره UTC» بخش ۲ CLAUDE.md را نقض می‌کرد و روی _همه_ جدول‌های آینده (Orders، Metrics، …) اثر می‌گذاشت، نه فقط جدول‌های این Migration.

**اصلاح:** `'timezone' => 'UTC'` به اتصال `pgsql` در `config/database.php` اضافه شد (Laravel's `PostgresConnector` این کلید را می‌خواند و پس از اتصال `SET timezone` اجرا می‌کند). این مقدار Hardcode است، نه از `.env` — چون یک قاعده معماری ثابت است، نه یک تنظیم محیطی. بعد از این اصلاح، `SHOW timezone` مقدار `UTC` می‌دهد و کل Suite (۱۰۷ تست) سبز شد. توابع `to_jalali*` بخش P0-04 تحت تأثیر این باگ نبودند چون صریحاً `AT TIME ZONE 'Asia/Tehran'` را با نام منطقه می‌نویسند، نه با تکیه بر Timezone نشست.

### تبدیل ستون‌های Timestamp موجود به `timestamptz`

با کشف این مسئله، کل Schema برای ستون‌های `timestamp without time zone` باقی‌مانده از Migrationهای منتشرشده استارتر بررسی شد:

- `users` (`created_at`, `updated_at`, `email_verified_at`, `two_factor_confirmed_at`) و `password_reset_tokens.created_at` و `failed_jobs.failed_at` — با `ALTER COLUMN ... TYPE timestamptz USING ... AT TIME ZONE 'UTC'` تبدیل شدند (Migration جدید، نه ویرایش Migration منتشرشده، طبق قانون بخش ۳).
- `jobs`/`job_batches` عمداً دست‌نخورده ماندند — ستون‌های شبه‌Timestamp آن‌ها عدد صحیح Unix Timestamp داخلی Laravel Queue هستند، نه واقعاً `timestamp`؛ تبدیل آن‌ها نوع‌شان را می‌شکند و ربطی به این قانون ندارد.
- `passkeys` (`created_at`, `updated_at`, `last_used_at`) عمداً برای **P0-06** نگه داشته شد — چون Passkey بخشی از Authentication است، نه Core.

### تست Schema/Constraint

`tests/Integration/CoreSchemaTest.php` (۱۱ تست): مقادیر پیش‌فرض `users`، عدم‌وجود هیچ ستون `timestamp without time zone` باقیمانده (به‌جز سه استثنای آگاهانه بالا)، رد شدن مقدار نامعتبر `effect`/`actor_type`، یکتایی `roles.name`/`permissions(module,action)`/`permission_overrides(user_id,permission_id)`، Cascade حذف Role روی Pivotها، و رفتار PK بودن `settings.key`.

### تأیید Idempotent/Rollback

`migrate:fresh` → `migrate:rollback --step=9` → `migrate` روی دیتابیس توسعه بدون خطا اجرا شد؛ همه Migrationهای جدید `down()` کامل دارند.

## P0-06 — Authentication + 2FA

استارتر Laravel از قبل با Fortify کامل بود (Login/Logout/Register/Reset Password/Email Verification/2FA/Passkeys، همراه با Throttle روی Login و روی 2FA). طبق دستور صریح («Stack موجود را حفظ کن، Framework جدید اضافه نکن») چیزی جایگزین نشد؛ کار این گام تکمیل شکاف‌ها بود، نه بازسازی.

### باگ واقعی پیدا و رفع‌شده در Factory تست

هنگام نوشتن تست «کد 2FA نامعتبر»، `UserFactory::withTwoFactor()` معلوم شد `two_factor_secret` را با `encrypt('secret')` می‌ساخت — یعنی مقدار رمزگشایی‌شده لفظاً کلمه «secret» (۶ کاراکتر) است، در حالی که Google2FA حداقل ۱۶ کاراکتر Base32 لازم دارد و به‌جای برگرداندن «کد اشتباه»، Exception پرتاب می‌کند (`SecretKeyTooShortException`). چون تا امروز هیچ تستی واقعاً کد را Verify نمی‌کرد، این باگ بی‌صدا مانده بود. اصلاح شد به تولید یک Secret واقعی با `Google2FA::generateSecretKey()` (`database/factories/UserFactory.php`).

### تست‌های اضافه‌شده

`tests/Feature/Auth/TwoFactorChallengeTest.php`: کد ۲FA نامعتبر رد می‌شود و کاربر را وارد نمی‌کند؛ کد بازیابی نامعتبر هم همینطور. سناریوهای «ورود موفق»، «رمز اشتباه»، «خروج»، «مسیر محافظت‌شده» (`DashboardTest.php`)، «کاربر با 2FA فعال به چالش هدایت می‌شود»، و «Rate Limit» همگی از قبل در `tests/Feature/Auth/AuthenticationTest.php` و `DashboardTest.php` موجود و سبز بودند.

### ترجمه صفحات Auth به فارسی RTL

طبق D10، هفت صفحه مسیر ورود/ثبت‌نام/بازیابی/۲FA (`resources/js/pages/auth/{login,register,forgot-password,reset-password,verify-email,confirm-password,two-factor-challenge}.tsx`) که هنوز انگلیسی بودند، به فارسی ترجمه شدند (متن، Placeholder، عنوان `Head`، `layout.title/description`). Layout‌های مشترک (`auth-layout` → `auth-simple-layout`) هیچ متن Hardcode‌شده‌ای نداشتند. در همین مسیر، یک نمونه از ریسک ثبت‌شده در P0-01 (کلاس فیزیکی `ml-auto` در دکمه «فراموشی رمز عبور») به کلاس منطقی `ms-auto` تغییر کرد چون دقیقاً همان خط ترجمه می‌شد؛ بقیه اصلاح فیزیکی→منطقی طبق تصمیم قبلی برای Sprint 3 می‌ماند.

⚠️ **یادداشت برای بعد، نه تصمیم:** ثبت‌نام عمومی (`register`) هنوز باز است. چون هی‌مد یک ابزار داخلی ۵ نفره است (نه SaaS)، احتمالاً در Sprint 8 (P8-05 «کاربران واقعی») باید بسته یا محدود به Invite شود — این یک تصمیم معماری است که نباید خودسرانه در این گام گرفته می‌شد، فقط اینجا برای تصمیم‌گیری بعدی یادداشت شد.

### تکمیل Timestamptz برای `passkeys` (شکاف باقیمانده از P0-05)

سه ستون `passkeys.{created_at,updated_at,last_used_at}` که در P0-05 عمداً نگه داشته شده بودند («چون Passkey بخشی از Authentication است»)، حالا با Migration جدید (`convert_passkeys_timestamps_to_timestamptz`) به `timestamptz` تبدیل شدند. بعد از این، در کل Schema هیچ ستون `timestamp without time zone` باقی نمانده به‌جز `jobs`/`job_batches` (که عمداً Unix Integer داخلی Queue هستند، نه Timestamp واقعی).

### «2FA اجباری برای Owner/Manager» — عمداً اینجا پیاده نشد

بخش ۲۱ PRD این را زیر «چک‌لیست سخت‌سازی» فهرست کرده که دقیقاً معادل Backlog Item **P8-01** (Sprint 8، نه Sprint 0) است؛ enforcement آن هم نیازمند رابطه واقعی کاربر↔نقش است که در P0-07 ساخته می‌شود و در Sprint 8 با کاربران واقعی (P8-05) پر می‌شود. اجرای زودهنگام آن اینجا یا duplicate‌کاری با P0-07 بود یا Enforcement روی داده جعلی. تصمیم: طبق ترتیب Backlog خود PRD، به P8-01 موکول شد؛ این یک انحراف از Scope نیست.

### تأیید

Full test suite: ۱۰۹ سبز. `npm run build` سبز. `tsc --noEmit` سبز. Pint/PHPStan سبز (تنها هشدار باقیمانده `npm run check` مربوط به فرمت جدول‌های Markdown در خود `PRD.md` است — فایل منبع حقیقت پروژه، از قبل همینطور بوده و در این گام دست‌نخورده ماند، چون تغییر آن خارج از اختیار این گام است).

## P0-07 — Permissions (CRITICAL)

پیاده‌سازی اختصاصی طبق تصمیم C8/بخش ۲۰ PRD؛ Spatie اضافه نشد.

### محل کد

کد جدید زیر `app/Modules/Core/{Enums,Models,Services}` قرار گرفت — اولین کد واقعی داخل ساختار Module بخش ۰۱ CLAUDE.md. دو تصمیم مرزی آگاهانه:
- **`App\Models\User` جابه‌جا نشد.** طبق قانون مرز ماژول فقط `Customers\Models\Customer` استثنا دارد، ولی `User` اصلاً داخل هیچ Moduleای نیست — یک Entity سطح Framework است که هر جدول Core (role_user، permission_overrides.user_id، audit_logs.user_id، settings.updated_by) به آن ارجاع می‌دهد. جابه‌جایی آن به `app/Modules/Core/Models` یعنی بازنویسی Fortify Config، UserFactory، و ده‌ها فایل موجود و تست‌شده — تغییر معماری بزرگ و خارج از Scope «Permissions». `Role`/`Permission`/`PermissionOverride` به `App\Models\User` ارجاع می‌دهند؛ این مشابه استثنای `Customer` در نظر گرفته شد، نه نقض قانون مرز.
- **PermissionService به‌جای Policy واقعی:** لایه Model Policy در PRD ذکر شده، ولی چون هنوز هیچ Model محافظت‌شدنی (Customer/Order/...) وجود ندارد، Policy واقعی در این گام ساخته نشد. `PermissionService::allows()` طوری نوشته شده که از داخل هر Policy آینده (Sprint 1+) هم قابل فراخوانی است.

### منطق (TEST FIRST)

`PermissionService::allows(User, module, action)`:
1. اگر Permission در Catalog نباشد → `false` (بسته پیش‌فرض).
2. اگر `permission_overrides` رکورد دارد (تضمین‌شده حداکثر یکی به‌خاطر `UNIQUE(user_id,permission_id)`) → دقیقاً همان اثر (allow/deny) برمی‌گردد و **کار تمام می‌شود** — یعنی گام‌های ۱ و ۲ ترتیب PRD («deny > allow») به‌صورت طبیعی از همین قید یکتایی نتیجه می‌شوند: یک کاربر هرگز هم‌زمان Override allow و deny روی یک Permission ندارد.
3. وگرنه، اگر نقشی که Grant می‌دهد دارد → `true`.
4. وگرنه → `false`.

`tests/Feature/Modules/Core/PermissionServiceTest.php` (۱۳ تست، TEST FIRST — قبل از نوشتن Service اجرا و قرمز شدن‌شان تأیید شد) دقیقاً سناریوی بحرانی خود CLAUDE.md را پوشش می‌دهد: کاربری با نقش Manager که `segments.delete` می‌گیرد ولی Override او `deny` است → **DENY**. به‌علاوه: Override مستقل از نقش، Scope دقیق per-user/per-permission، و `allowedKeys()` (برای لایه UI).

### لایه‌های اعمال

- **Route Middleware:** `App\Http\Middleware\EnsurePermission` (alias نام `permission`، در `bootstrap/app.php`) — `abort(403)` واقعی. تست در `EnsurePermissionMiddlewareTest.php` روی یک Route موقتِ داخل تست (چون هنوز Route محافظت‌شدنی واقعی وجود ندارد).
- **Inertia Shared `auth.permissions`:** در `HandleInertiaRequests::share()`، آرایه‌ای از کلیدهای `"module.action"` که کاربر لاگین‌کرده مجاز است (از `PermissionService::allowedKeys()`) به هر صفحه فرستاده می‌شود.
- **React `can()`:** هوک `resources/js/hooks/use-can.ts`، فقط از همین Prop می‌خواند — طبق تصریح PRD فقط UX است، مرز امنیتی واقعی نیست.
- **ماسک موبایل:** مجوز `customers.view_full_phone` در Catalog Seed شد؛ مکانیزم واقعی Mask کردن به Sprint 3 (وقتی UI مشتری ساخته می‌شود) موکول است — چیزی برای Mask کردن هنوز وجود ندارد.

### یافته فنی حین نوشتن تست Middleware

ثبت پویای یک Route داخل تست با الگوی معمول `Route::get(...)->name(...)` باعث شد `route('...')` خطای «Route not defined» بدهد با اینکه `Route::getRoutes()->count()` مسیر را نشان می‌داد. علت: `RouteCollection::add()` نام مسیر را در لحظه‌ی افزودن ایندکس می‌کند، نه بعد از `->name()`؛ `refreshNameLookups()` فقط یک‌بار در بوت اپلیکیشن (بعد از بارگذاری `routes/web.php`) صدا زده می‌شود، نه بعد از ثبت پویا در میانه تست. راه‌حل: در `EnsurePermissionMiddlewareTest.php` از URL خام به‌جای `route()` استفاده شد — این یک محدودیت شناخته‌شده تست‌نویسی روی این نسخه Laravel است، نه باگ کد پروژه.

### Catalog و ماتریس نقش (Seeders)

چون بخش ۲۰ PRD ماتریس را به‌صورت کیفی («همه»، «view + …») توصیف کرده نه به‌صورت لیست دقیق `module.action`، یک Catalog صریح از روی متن PRD استخراج شد (`database/seeders/PermissionSeeder::catalog()`) — شامل هر مورد Named شده (`customers.view_full_phone`, `customers.export`, `customers.anonymize`, `identity.review`, `ai.request`, `segments.{view,create,edit,delete}`) به‌علاوه یک `view` برای هر Module ذکرشده در ماتریس (`audit`, `settings→manage`, `users→manage`, `customers`, `orders`, `metrics`, `dashboard`, `analytics`, `ai`) و `customers.note` برای Support. این یک **پیش‌فرض صریح مستندشده** است، نه استنباط بی‌رویه: هر Module واقعی (Customers در P1-01، Sync در P2، …) وقتی ساخته شود Permissionهای دقیق‌تر خودش را به همین Catalog اضافه می‌کند.

`RoleSeeder` این ماتریس را دقیقاً پیاده می‌کند (Owner=همه، Manager=همه به‌جز `audit`/`settings`/`users`، Analyst=هر `*.view` + سه مورد اضافه بدون هیچ `*.delete`، Support=سه Permission ثابت، Viewer=چهار Permission ثابت) و Idempotent است (`firstOrCreate` + `sync`). `tests/Feature/Modules/Core/RoleSeederTest.php` (۷ تست) هر ردیف ماتریس + Idempotency را تأیید می‌کند. `DatabaseSeeder` این دو Seeder را قبل از کاربر تستی صدا می‌زند.

### تأیید

`migrate:fresh --seed` سبز (۵ نقش، Catalog کامل). Full Suite: ۱۳۵ سبز (۳۶۰ Assertion). Pint/PHPStan/`tsc --noEmit`/`npm run build` سبز.

## P0-08 — Audit

- `App\Modules\Core\Services\AuditService::record()` تنها مسیر نوشتن در `audit_logs` است (Controllerها هرگز مستقیم نمی‌نویسند). `paginate()` برای صفحه Audit است. `App\Modules\Core\Models\AuditLog` + Enum `AuditActorType` (`user|system|ai`).
- **Redaction:** قبل از ذخیره، هر کلید `before`/`after` که شامل یکی از این زیررشته‌ها باشد (بدون حساسیت به حروف) با `[REDACTED]` جایگزین می‌شود: `password`، `remember_token`، `secret` (پوشش `two_factor_secret`، `consumer_secret`)، `recovery_code`، `consumer_key`، `api_key`، `token`، `config` (چون `integrations.config` اعتبارنامه Woo/AI را نگه می‌دارد). یک تست حین نوشتن نشان داد `two_factor_recovery_codes` با لیست اولیه پوشش داده نمی‌شد؛ `recovery_code` اضافه شد.
- **Trait `Auditable`** (`Modules/Core/Concerns`): روی مدل‌های حساس گذاشته می‌شود و `created/updated/deleted` را خودکار (با diff فقط فیلدهای تغییرکرده) لاگ می‌کند. عامل: کاربر لاگین‌کرده → `user`، وگرنه `system` (`ai` فقط با فراخوانی صریح ماژول Ai). فعلاً روی `PermissionOverride` (حساس‌ترین جدول Core فعلی) اعمال شد؛ Setting در P0-09.
- ⚠ **محدودیت Schema:** `audit_logs.auditable_id` طبق PRD `bigint` است ولی `settings.key` رشته‌ای است. تا P0-09 تصمیم جداگانه‌ای برای Audit Setting گرفته می‌شود (نه اینجا).
- **صفحه Audit:** `GET /audit` (`AuditLogController@index` → `AuditService::paginate()`؛ Controller منطق ندارد)، محافظت‌شده با `auth` + `permission:audit,view`. صفحه React `pages/audit/index.tsx` فارسی RTL با صفحه‌بندی دستی. **`dangerouslySetInnerHTML` عمداً استفاده نشد** (قانون CLAUDE.md §2) — نسخه اول برچسب HTML صفحه‌بندی Laravel را با آن رندر می‌کرد و قبل از Commit با «قبلی/بعدی» ساده جایگزین شد. کامپوننت `ui/table.tsx` دستی اضافه شد (CLI شادسی‌ان به‌خاطر نبودن `pnpm` شکست خورد) با کلاس‌های منطقی RTL. لینک سایدبار فقط با `useCan('audit','view')` نمایش داده می‌شود.
- ⚠ `php artisan wayfinder:generate` دستی، Helperهای `.form()` را حذف می‌کند؛ فقط `npm run build` (پلاگین Vite) صحیح تولید می‌کند.
- تست‌ها: `AuditServiceTest` (۷)، `AuditableTraitTest` (۳)، `AuditLogPageTest` (۳: مهمان/بدون مجوز/مجاز). Suite: ۱۴۸ سبز، Pint/PHPStan/tsc/build سبز.

## P0-09 — Settings + AlertService

- **`SettingService::get/set`** (`Modules/Core/Services`): کلیدها فقط از Enum `SettingKey` می‌آیند (نوع، پیش‌فرض، قوانین اعتبارسنجی)؛ `set()` با Validator رد می‌کند و چیزی ذخیره نمی‌شود. خواندن با `Cache::rememberForever` و ابطال کش در `set()`. تغییر واقعی → `updated_by`، رکورد Audit (`setting.updated` با before/after) و Event `SettingChanged`؛ مقدار یکسان → هیچ‌کدام. کلیدها: `alerts.enabled`، `alerts.dedupe_minutes`، `alerts.failed_jobs_threshold` (۲۰)، `alerts.consecutive_sync_failures` (۲)، `alerts.reconciliation_diff_percent` (۱٫۰) — همان آستانه‌های بخش ۲۲ PRD.
- **Secretها در Settings نیستند:** طبق CLAUDE.md §6 اعتبارنامه Woo/AI رمزنگاری‌شده در `integrations.config` (Sprint 2) می‌ماند؛ هیچ کلید Settings حساس تعریف نشد و Audit هم هر کلید حساس را Redact می‌کند. بودجه AI و حاشیه سود طبق PRD در `config/ai.php`/`config/metrics.php` می‌مانند، نه Settings (جلوگیری از دو منبع حقیقت).
- **تصمیم Audit برای کلید رشته‌ای:** `audit_logs.auditable_id` طبق PRD `bigint` است ولی `settings.key` رشته → `auditable_id = 0` و کلید داخل payload `before/after` ثبت می‌شود (Schema دست نخورد؛ اگر بعداً Audit روی Setting باید با `auditable_id` قابل جستجو باشد، Migration جدید لازم است).
- **`AlertService::critical(AlertKind, message, context)`**: شش نوع طبق PRD (`sync_failure`، `metric_run_failure`، `reconciliation_variance`، `failed_jobs_threshold`، `ai_budget_threshold`، `nightly_chain_timeout`). چون Phase 1 هیچ کانال اعلان (SMS/Telegram/Email) ندارد، هشدار = `Log::critical` + رکورد Audit با `alert.critical` (در صفحه Audit دیده می‌شود). Dedupe با `Cache::add` به‌ازای هر نوع در پنجره `alerts.dedupe_minutes`؛ با `alerts.enabled=false` خاموش است؛ خروجی `bool` (اعلام‌شد/نه). Schedulerهایی که این را صدا می‌زنند (Sprint 6) هنوز ساخته نشده‌اند.
- **اصلاح جانبی:** Redaction در `AuditService` بازگشتی شد (Context تودرتوی هشدار، مثل `consumer_secret` داخل `context`، قبلاً Redact نمی‌شد).
- تست: `SettingServiceTest` (۱۱)، `AlertServiceTest` (۶). Suite: ۱۶۹ سبز، Pint/PHPStan سبز.

## P0-10 — تست‌های معماری

`tests/Arch` (سوئیت `Arch` در `phpunit.xml`؛ بدون DB). به‌جای `arch()` خود Pest از یک اسکنر مبتنی بر Token (`tests/Arch/Scanner.php`) استفاده شد که کامنت/DocBlock را حذف می‌کند تا متن مستندات (مثل «ممنوع: DB::raw») هشدار الکی ندهد؛ `ScannerTest` خودِ اسکنر را اثبات می‌کند. قوانین (`ArchitectureTest.php`):

1. Controller بدون DB/Query (`DB::`، `::where/query/find/create…`، `->where/save/update/delete/select…`).
2. Controller هرگز Model ماژول را مستقیم `use` نمی‌کند (Controller→Service→Model).
3. Job بدون منطق کسب‌وکار (همان الگوها + Model ماژول).
4. ماژول‌ها Model یکدیگر را نمی‌بینند؛ تنها `Customers\Models\Customer` مجاز است.
5. ارجاع بین‌ماژولی فقط به `Services` یا `Events` (سخت‌گیرانه طبق CLAUDE.md؛ اگر Enum مشترک لازم شد باید صریحاً بحث و اضافه شود).
6. جدول وابستگی ماژول‌ها دقیقاً طبق بخش ۰۷ PRD.
7. `Segments`: هیچ `whereRaw/selectRaw/…/DB::raw/Expression`.
8. Raw SQL فقط در Migration و `Metrics`/`Analytics` (کل `app/`، `routes`, `config`).
9. بدون `dd/dump/ray/var_dump/print_r`، و در فرانت بدون `console.log/debugger/dangerouslySetInnerHTML`.
10. تست‌ها روی SQLite/`:memory:` نیستند (phpunit.xml، `.env.testing`، `.env.example`، فایل‌های تست).
11. `.env*` در Git نیست و هیچ کلید Woo (`ck_/cs_`)، کلید AI، یا Private Key در فایل‌های ردیابی‌شده نیست.
12. `declare(strict_types=1)` در `app/Modules` و `app/Support`؛ رشته وضعیت سفارش Hardcode نشده.

**Mutation check:** نقض عمدی (Job با DB، `whereRaw` در Segments، `use` مدل ماژول دیگر، `dd()`) موقتاً کاشته شد؛ ۷ تست شکست خوردند و پس از حذف همه سبز شدند.

### تخلف‌های واقعی که تست‌ها در کد استارتر پیدا کردند و رفع شدند
- `ProfileController` (`save`, `delete`) و `SecurityController` (`update` رمز، Query لیست Passkey) مستقیم در Controller به DB می‌نوشتند/می‌خواندند → به `Modules/Core/Services/UserAccountService` منتقل شد (رفتار و تست‌های موجود بدون تغییر).
- `two-factor-setup-modal.tsx` کد QR را با `dangerouslySetInnerHTML` رندر می‌کرد → با `<img src="data:image/svg+xml…">` جایگزین شد (SVG داخل `img` اسکریپت اجرا نمی‌کند).
- کامنت `phpunit.xml` که خودش کلمه SQLite داشت بازنویسی شد.

Suite: ۱۸۶ سبز؛ Pint/PHPStan/tsc/build سبز.

## GATE 0 — نتیجه

- Login/Logout/مسیر محافظت‌شده/۲FA (چالش، کد نامعتبر، کد بازیابی نامعتبر، Throttle): ۱۲ تست سبز + Smoke زنده روی سرور واقعی (`/login` ۲۰۰ با `lang="fa" dir="rtl"`، `/dashboard` و `/audit` برای مهمان ۳۰۲ به `/login`).
- **Deny-override:** کاربر با نقش Manager که `segments.delete` می‌گیرد ولی Override=`deny` دارد → DENY (تست `CRITICAL` + ۱۳ تست PermissionService + Middleware) سبز.
- **Arch:** ۱۷ تست سبز، با Mutation check اثبات‌شده.
- Full suite ۱۸۶ سبز؛ `pint --test` کل مخزن، PHPStan (Larastan)، `tsc --noEmit`، `npm run build` سبز (`verify-woo.php` فقط Format شد).
- باز/تصمیم‌های منتظر: (۱) ثبت‌نام عمومی هنوز باز است؛ (۲) ۲FA اجباری Owner/Manager = P8-01؛ (۳) `npm run check` روی جدول‌های Markdown خود `PRD.md` هشدار می‌دهد (فایل منبع حقیقت، دست‌نخورده)؛ (۴) بخش‌های Settings/Security UI هنوز انگلیسی است (Sprint 3).

## P1-01 — Customers + identities + conflicts + addresses + notes

Migrationهای ۰۱۲–۰۱۶ بخش ۰۹ PRD و ماژول `app/Modules/Customers` (`Models`, `Enums`) + `CustomerFactory`.

- **`customers`:** دقیقاً طبق Schema (`phone_normalized` یکتا، `status`/`lifecycle_stage` با CHECK، `metrics_dirty` پیش‌فرض true، Soft delete، همه Timestamp از نوع `timestamptz`). Indexهای PRD: جزئی `WHERE metrics_dirty = true` و GIN `gin_trgm_ops` روی `display_name`. **هیچ شمارنده تجمیعی روی جدول نیست** (C7) — تست Schema تضمین می‌کند.
- **`customer_identities`:** `UNIQUE(source, source_id)` (حتی بین دو مشتری)؛ `source` CHECK (`woo_user|woo_guest_order`).
- **`identity_conflicts`:** `woo_order_id` عمداً FK نیست (جدول `orders` در P1-03 می‌آید و تعارض باید مستقل از آن بماند)؛ `resolved_by` با حذف کاربر NULL می‌شود.
- **`customer_addresses`** (`type` CHECK) و **`customer_notes`** (`user_id` nullable + `SET NULL`: یادداشت‌ها از Woo قابل بازیابی نیستند، پس با حذف نویسنده باقی می‌مانند).
- FK فرزندها به `customers` با `CASCADE` است چون حذف واقعی مشتری در Phase 1 اتفاق نمی‌افتد (Soft delete/Anonymize)؛ سفارش‌ها در P1-03 `RESTRICT` خواهند بود.
- **پیش‌فرض‌های صریح (PRD ساکت بود):** (۱) `customer_identities.confidence` مقدارش را PRD نگفته؛ CHECK روی `high|medium|low` گذاشته شد (قاعده «هر ستون Enum یک CHECK») — در P2-04 در صورت نیاز تغییر می‌کند؛ (۲) Enumهای همین جدول‌ها (`CustomerStatus`, `LifecycleStage`, `IdentitySource`, `IdentityConfidence`, `IdentityConflictStatus`, `AddressType`) همین‌جا ساخته شدند چون Model بدون Enum «رشته لخت» می‌شد؛ P1-05 بقیه Enumها را اضافه می‌کند.
- **Mutator نرمال‌سازی:** `Customer::phone_normalized` هر مقدار نوشته‌شده را از `PhoneNormalizer` رد می‌کند؛ `+98 912…` و `0912…` هرگز دو مشتری نمی‌شوند و موبایل نامعتبر Exception می‌دهد (تست‌شده). منطق Resolve/Conflict (`CustomerIdentityService`) عمداً P2-04 است و اینجا ساخته نشد.
- **Auditable Trait روی این جدول‌ها اعمال نشد:** Trait در ماژول Core است و استفاده مستقیم از آن در Customers قانون مرز ماژول (فقط Service/Event) را نقض می‌کند؛ Audit تصمیم‌های بازبینی هویت در P2-04/Sprint 3 از طریق `AuditService` انجام می‌شود.
- تست: `tests/Integration/CustomersSchemaTest.php` (Constraintها، Indexها، بدون شمارنده، `timestamptz`) و `tests/Feature/Modules/Customers/CustomerModelTest.php` (Factory، نرمال‌سازی، رابطه‌ها، Soft delete). `migrate:rollback --step=5` و `migrate:fresh --seed` سبز. Suite: ۲۱۶ سبز، Pint/PHPStan/tsc/Arch سبز.

## P1-02 — Catalog

Migrationهای ۰۱۷–۰۲۱ بخش ۰۹ PRD (`product_categories`, `products`, `product_category_product`, `product_variations`, `product_costs`) و ماژول `app/Modules/Catalog` (`Models`, `Enums`) + Factoryها. فقط داده آینه‌ای Woo است؛ `CatalogService::upsertProduct/resolveVariationBySku` مربوط به Sync (P2-05/P2-06) است و ساخته نشد.

- Schema دقیقاً طبق PRD: `woo_*_id` یکتا، Index جزئی یکتای `sku WHERE sku IS NOT NULL`، `attributes` از نوع `jsonb` با پیش‌فرض `{}`، GIN `gin_trgm_ops` روی `products.name`، `price`/`unit_cost` از نوع `bigint` (تومان صحیح)، همه Timestamp از نوع `timestamptz`، `product_costs` خالی ساخته می‌شود (D11).
- **پیش‌فرض‌های صریح (PRD مقادیر Enum را نگفته):** `products.type` ∈ `simple|variable|grouped|external` و `products.status`/`product_variations.status` ∈ `publish|draft|pending|private` (مجموعه هسته Woo) با CHECK طبق قاعده «هر ستون Enum یک CHECK»؛ `product_costs.source` فقط `manual` (تنها مقدار PRD). ⚠ **الزام برای P2-05:** Mapper باید هر نوع/وضعیت خارج از این مجموعه (مثلاً نوع محصول افزونه‌ای) را نگاشت کند، نه اینکه ردیف رد شود؛ مقدار جدید = Migration جدید.
- **FKها:** واریانت‌ها با حذف محصول و پیوندهای دسته با حذف هر طرف Cascade می‌شوند؛ `parent_id` دسته با حذف والد NULL می‌شود؛ `product_costs.variation_id` عمداً `RESTRICT` است (هزینه دستی از Woo قابل بازیابی نیست، حذف بی‌صدا نباید ممکن باشد).
- **نام ستون `attributes`:** طبق PRD ثابت است ولی با ویژگی داخلی `Model::$attributes` Eloquent هم‌نام است. کار می‌کند (Cast `array`) و با تست خواندن/به‌روزرسانی/Refresh/`toArray`/`whereJsonContains` قفل شد؛ در کد آینده Sync از `->attributes` فقط از طریق Model استفاده شود، نه دستکاری `$model->attributes` خام.
- تست: `tests/Integration/CatalogSchemaTest.php` (۲۰: Constraintها، Indexها، NULLهای متعدد در SKU/Woo id، `bigint`، RESTRICT/CASCADE، `timestamptz`) و `tests/Feature/Modules/Catalog/CatalogModelTest.php` (۷). `migrate:rollback --step=5` / `migrate` / `migrate:fresh --seed` سبز. Suite: ۲۴۳ سبز، Pint/PHPStan/tsc/Arch سبز.

## P1-03 — Orders + items + status history + refunds

Migrationهای ۰۲۲–۰۲۵ بخش ۰۹ PRD و ماژول `app/Modules/Orders` (`Models`, `Enums`) + `OrderFactory`. منطق Sync (Upsert سفارش، بازمحاسبه `refunded_total`، تعیین `is_realized`) عمداً Sprint 2 است و ساخته نشد.

- Schema دقیقاً طبق PRD: `woo_order_id`/`woo_refund_id` یکتا (Idempotency)، `UNIQUE(order_id, woo_item_id)`، `customer_id` با `RESTRICT`، آیتم‌ها/تاریخچه/عودت با حذف سفارش `CASCADE`، `CHECK (total >= 0 AND refunded_total >= 0)`، Index نزولی `(customer_id, ordered_at DESC)` و Index جزئی `WHERE is_realized AND deleted_at IS NULL`، Soft delete (D6)، همه پول `bigint` و همه زمان `timestamptz`.
- **`net_revenue`** با `ALTER TABLE … GENERATED ALWAYS AS (total - refunded_total) STORED` و `DB::statement()` (قانون ۳ Migration) ساخته شد؛ در `$fillable` Model نیست و تست‌ها نوشتن مستقیم را رد و `attgenerated='s'` را تأیید می‌کنند.
- **پیش‌فرض‌های صریح (PRD ساکت بود):**
  1. `orders.status` و `order_status_history.from/to_status` رشته خام Woo هستند، **بدون CHECK و بدون Enum سمت اپ**: وضعیت‌ها را فروشگاه تعریف می‌کند (سفارش با وضعیت سفارشی نباید رد شود — CLAUDE.md §5) و CLAUDE.md §3 نوشتن رشته وضعیت در کد را ممنوع کرده؛ «تحقق‌یافته بودن» از `config('woo.realized_statuses')` می‌آید. `config/woo.php` هنوز وجود ندارد (P2-01) و همین‌جا ساخته نشد.
  2. `order_status_history.source` فقط `sync` (تنها مقدار PRD) با CHECK و Enum `OrderHistorySource`؛ مقدار جدید = Migration جدید.
  3. `order_items.product_id/variation_id` با حذف ردیف کاتالوگ `SET NULL` می‌شوند (آیتم و درآمد باید بمانند). Orders **رابطه Eloquent به Catalog ندارد** (`OrderItem::product()` عمداً نیست)؛ فقط شناسه، و Resolve از طریق سرویس Catalog در Sprint 2. Order فقط به `Customers\Models\Customer` (استثنای مجاز) ارجاع دارد و `Customer::orders()` عمداً اضافه نشد (Customers نباید Model Orders را ببیند).
  4. Indexهای FK اضافه‌ای که PRD نگفته: `order_items(product_id)`, `order_items(variation_id)`, `order_status_history(order_id, changed_at)`, `refunds(order_id)` — PostgreSQL برای FK خودکار Index نمی‌سازد و حذف/Join روی آن‌ها بدون Index Seq Scan می‌شد.
  5. `refunded_total ≤ total` و `SUM(refunds.amount) = refunded_total` در DB اعمال نمی‌شود (PRD فقط `>= 0` گفته)؛ تضمین آن با بازمحاسبه SQL در Sprint 2 است.
- **اصلاح در Arch Test (خطای خودم در P0-10):** قانون «فقط Service/Event بین ماژول‌ها» نام کلاس را Capture نمی‌کرد و استثنای `Customers\Models\Customer` هرگز نمی‌توانست Match شود؛ چون تا حالا هیچ ماژولی Customer را نمی‌خواند دیده نشده بود. اصلاح شد (فقط دقیقاً `Customer`، نه `CustomerAddress`) و با کاشت تخلف (`CustomerAddress`، Model Catalog) دوباره اثبات شد.
- تست: `tests/Integration/OrdersSchemaTest.php` (۳۰: Constraintها، ستون Generated، `bigint`، RESTRICT/CASCADE/SET NULL، Indexها، `timestamptz`، Idempotency) و `tests/Feature/Modules/Orders/OrderModelTest.php` (۱۰: عودت جزئی/کامل، `net_revenue`، وضعیت سفارشی، تاریخچه، Soft delete). `migrate:rollback --step=4` / `migrate` / `migrate:fresh --seed` سبز. Suite: ۲۸۳ سبز.

## P1-04 — Migrationهای Metrics / Segments / Analytics / Sync / AI

فقط Schema (۱۷ Migration، شماره‌های ۰۲۶–۰۴۲ بخش ۰۹ PRD): `metric_runs`, `customer_metrics`, `segments`, `segment_members`, `daily_metrics`, `cohort_snapshots`, `product_affinities`, `customer_category_purchases`, `customer_product_purchases`, `sync_cursors`, `sync_jobs`, `sync_logs`, `reconciliation_reports`, `integrations`, `ai_insights`, `ai_usage_daily`, `ai_tool_calls`. **هیچ Model/Enum/Service/Job ساخته نشد** — PRD هیچ‌کدام را به P1-04 نسبت نداده (Enumها = P1-05؛ منطق = Sprintهای ۲ تا ۷).

- Schema دقیقاً طبق PRD: نوع ستون‌ها (`bigint` تومان، `numeric(p,s)` برای امتیاز/نرخ/هزینه دلار، `jsonb`، `smallint`، همه `timestamptz`)، PK/UNIQUE، CHECKهای صریح (امتیاز ۱..۵، `mode`/`status`/`type`/`level`، `entity_a_id <> entity_b_id`، «سگمنت dynamic حتماً `rule` دارد»)، Indexهای PRD (`monetary DESC`، `(level, entity_a_id, lift DESC)`، یکتای جزئی `lower(name) WHERE deleted_at IS NULL` و `cache_key WHERE cache_key IS NOT NULL`).
- **پیش‌فرض‌های صریح (PRD ساکت بود):**
  1. `migration ۰۴۳` (نقش `hm_ai_readonly`) در P1-04 **ساخته نشد**: Backlog آن را به P7-01 (GATE 4، تست «INSERT باید شکست بخورد») سپرده، نیازمند `CREATEROLE` و رمز است. همین‌طور `ai_actions` طبق §19 عمداً وجود ندارد (تست دارد).
  2. FKها: جدول‌های مشتق (`customer_metrics`, `segment_members`, `customer_*_purchases`) با حذف والد `CASCADE` (قابل بازسازی‌اند)؛ `metric_run_id`, `segments.created_by`, `ai_insights.requested_by` → `SET NULL` (سگمنت/بینش با حذف کاربر یا Run نمی‌میرد)؛ `sync_logs`/`ai_tool_calls` با حذف والد `CASCADE`. `product_affinities.entity_*_id` چندشکلی (بسته به `level`) است → `bigint` ساده، نه FK.
  3. CHECK روی مقادیر Enum که PRD لیست نکرده: `customer_metrics.rfm_segment` = هشت سگمنت §۱۲؛ `sync_cursors.last_status` = مجموعه `sync_jobs.status`؛ `sync_logs.level` = هشت سطح استاندارد PSR-3 تا هر سطح `Log` لاراول بدون Migration جدید نگاشت شود. **بدون CHECK عمداً:** `sync_*.entity` (شناسه است نه وضعیت) و `integrations.last_health_status` (PRD مقداری نداده و مصرف‌کننده‌اش HealthCheckJob در Sprint 6 است).
  4. نوع/Default هایی که PRD نگفته: `ai_insights.period_start/end` = `date`؛ شمارنده‌های `sync_jobs` و توکن‌های `ai_insights` و ستون‌های `ai_usage_daily` `DEFAULT 0`؛ `ai_tool_calls.arguments` `DEFAULT '{}'`؛ `computed_at`/`started_at`/`added_at`/`created_at` `DEFAULT CURRENT_TIMESTAMP`؛ ستون‌های «بدون Default» در Rollupها (`daily_metrics` و…) `NOT NULL` بدون Default (سازنده Sprint 6 همیشه ردیف کامل می‌نویسد).
  5. **در DB اعمال نشد:** قواعد Metrics مثل «`churn_reason` وقتی سطح ریسک هست الزامی» و «`clv_estimated` وقتی `total_orders < 2` NULL» و «Affinity با `lift <= 1` ذخیره نشود» — در PRD جزو CHECKهای Schema نیستند و ترتیب چندمرحله‌ای بازمحاسبه (§۱۱) ممکن است حین اجرا موقتاً آن‌ها را نقض کند؛ تضمینشان با تست Calculatorها (Sprint 4/6) است.
- تست (PostgreSQL واقعی، `tests/Integration`): `SchemaProbe` کاتالوگ PostgreSQL را می‌خواند و برای هر جدول **همه ستون‌ها را با نوع/Nullable/Default مقایسه می‌کند و ستون اضافه را هم خطا می‌داند**؛ به‌علاوه `MetricsSchemaTest`, `SegmentsSchemaTest`, `AnalyticsSchemaTest`, `SyncSchemaTest`, `AiSchemaTest`, `Phase1TimestampSchemaTest` (CHECK/UNIQUE/FK با `ON DELETE` دقیق، Indexهای جزئی و نزولی، `timestamptz`، نبود `float`). با یک ستون نادرست عمدی، اثبات شد Probe خطا می‌دهد.
- `migrate:rollback --step=17` هر ۱۷ جدول را برمی‌دارد، `migrate` برمی‌گرداند، `migrate:fresh --seed` سبز؛ هیچ Migration قبلی ویرایش نشد. Suite: ۳۷۳ سبز.

## P1-05 — Enumها

۱۱ Enum برای ستون‌های Enum-مانند جدول‌های P1-04 که هنوز Enum نداشتند، هرکدام در ماژول مالک خودش (`Modules/*/Enums`، `string`-backed، `strict_types`): `Metrics` → `MetricRunMode`, `MetricRunStatus`, `RfmSegment` (به ترتیب CASE بخش ۱۲)، `ClvConfidence`, `ChurnRiskLevel`؛ `Segments` → `SegmentType`؛ `Analytics` → `AffinityLevel`؛ `Sync` → `SyncMode`, `SyncStatus` (برای `sync_jobs.status` و `sync_cursors.last_status`)، `SyncLogLevel`؛ `Ai` → `AiInsightType`. Enumهای P1-01..P1-03 و Core جابه‌جا/تکرار نشدند. فقط Case (بدون منطق/Service).

- **عمداً Enum نشد:** وضعیت سفارش (داده Woo، طبق CLAUDE.md §3/§5)، `sync_*.entity` (شناسه)، `integrations.last_health_status` (PRD مقداری نداده)، Operatorهای Segment (P5-01).
- **تست هم‌ترازی Enum↔DB:** `EnumSchemaAlignmentTest` برای هر ۲۵ ستون Enum-مانند (شامل Enumهای قبلی) مقادیر Enum را با مجموعه CHECK واقعی PostgreSQL مقایسه می‌کند — Enum و CHECK دیگر نمی‌توانند بی‌صدا از هم جدا شوند. (PostgreSQL یک `IN` تک‌مقداری را `= 'x'::text` چاپ می‌کند؛ Parser تست هر دو شکل را می‌خواند.)
- **Arch (`EnumConventionsTest`):** Enum فقط در `Modules/*/Enums`، `string`-backed، `strict_types`، نام یکتا در کل پروژه، بدون Enum وضعیت سفارش، و هیچ دو Enum مجموعه مقدار یکسان ندارند — با یک استثنای مستند: `ClvConfidence` (Metrics) و `IdentityConfidence` (Customers) مقدار یکسان `low/medium/high` دارند ولی معنا و ماژول متفاوت (Enum مشترک بین‌ماژولی ممنوع است).
- **اصلاح Arch Test قدیمی (نه سست‌کردن):** قانون «رشته وضعیت سفارش Hardcode نشود» روی `'completed'` در `MetricRunStatus`/`SyncStatus` (وضعیت Run/Job، نه سفارش Woo) خطا می‌داد؛ فقط همین دو فایل از اسکن مستثنا شدند و هر فایل دیگر (از جمله کل `Orders`) هنوز اسکن می‌شود. با کاشت `OrderStatus` Enum در Orders اثبات شد که همچنان گرفته می‌شود.
- Suite: ۴۴۸ سبز (Arch ۲۲).

## P1-06 — DemoDataSeeder + expected_metrics.json

`database/seeders/DemoDataSeeder.php` (بخش ۲۴ PRD: ۵۰ مشتری = ۲۰ تک‌خرید + ۱۵ تکرارخرید + ۸ وفادار + ۴ در معرض ریزش + ۳ دارای عودت) و `tests/fixtures/expected_metrics.json` (قرارداد GATE 2). فقط جدول‌های آینه‌ای (کاتالوگ، مشتری، سفارش، آیتم، تاریخچه، عودت) پر می‌شوند؛ جدول‌های مشتق/Sync/AI عمداً خالی می‌مانند (`metrics_dirty=true` برای هر ۵۰ مشتری).

- **قطعی بودن:** بدون ساعت، Random، Faker یا شبکه؛ همه‌چیز محاسبه‌ی حسابی روی اندیس مشتری (۱..۵۰) و ثابت `AS_OF = 2026-06-30 08:30 UTC`. تست مصرف `now()/rand()/fake()/Http::/WooClient` را در کد Seeder ممنوع می‌کند. دو بار `migrate:fresh --seed` هش داده (با `id`) یکسان می‌دهد؛ `db:seed` مجدد چیزی را تغییر نمی‌دهد (Seeder Idempotent است).
- **جدا از داده Production:** در `production` با Exception رد می‌شود و در `DatabaseSeeder` فقط `if (! app()->isProduction())` صدا زده می‌شود. شماره‌ها `989000000001..050` (ساختگی) و از `PhoneNormalizer` رد می‌شوند.
- **`config/woo.php` (فقط دو کلید):** `realized_statuses = ['processing','completed']` و `excluded_statuses` (بخش ۱۰) — چون CLAUDE.md §3 می‌گوید `is_realized` فقط از این Config می‌آید و Seeder نباید لیست Hardcode داشته باشد. تنظیمات اتصال/Retry/Webhook در P2-01 به همین فایل اضافه می‌شود.
- **پوشش شاخه‌ها:** سفارش لغوشده/pending/on-hold/failed (غیرتحقق‌یافته)، ۱ سفارش Soft-delete، ۱ سفارش عودت کامل با وضعیت `completed`، دو عودت بخشی (یکی مرجوعی قلم، یکی فقط مبلغ)، آیتم Unresolved با `product_id/variation_id = NULL` و `sku` + `name_snapshot`، دو `identity_conflicts` (pending و حل‌شده)، آدرس Billing/Shipping و یادداشت. قیمت آیتم‌ها از مجموع سفارش می‌آید (نه قیمت لیست کاتالوگ).
- **طراحی برای بی‌ابهامی عددی:** همه سفارش‌ها ساعت ۰۸:۳۰ UTC (فاصله‌ها روز صحیح)، همه مبالغ مضرب ۱۰٬۰۰۰ تومان، و درآمد شمارش‌شده هر مشتری مضرب دقیق تعداد سفارش‌هایش (AOV صحیح، بدون قاعده گرد‌کردن). Oracle به‌محض رسیدن به **تساوی دقیق ۰٫۵** (مثلاً امتیاز ریسک ۳۶٫۸۷۵) خطا می‌دهد؛ یک مورد (مشتری ۲۵) با یک‌روز جابه‌جایی فاصله‌ی گروه تکرارخرید رفع شد.
- **`expected_metrics.json`** (`as_of` تزریقی، هرگز `now()`؛ کلید = `phone_normalized`): Base aggregates، امتیازهای RFM با NTILE و اصلاح E3، `rfm_segment` (CASE بخش ۱۲)، `clv_historical/estimated/confidence` (بخش ۱۳)، `purchase_cycle_days`, `churn_risk_score/level`, `lifecycle_stage` (بخش‌های ۱۱/۱۴)، `cohort_month` و ماتریس Cohort (بخش ۱۵)، و مجموع‌های فروشگاه (نرخ خرید مجدد ۰٫۵۶۰۰). آستانه Churn: نمونه فاصله‌ها = ۶۰ (< ۲۰۰) پس **Fallback ۶۰/۱۲۰/۲۱۰** (بخش ۱۴)؛ مسیر Percentile واقعی با این داده پوشش داده نمی‌شود و باید در تست‌های Sprint 4 با داده‌ی ساختگی خودش بیاید.
- **دو پیاده‌سازی مستقل هم‌نتیجه‌اند:** (۱) Oracle آزمایشی PHP (`tests/Support/ReferenceMetrics.php`، محاسبه‌ی دقیق عدد صحیح/کسری) و (۲) همان فرمول‌های PRD به‌صورت SQL خالص PostgreSQL در `ExpectedMetricsFixtureTest` (شامل `ntile`, `percentile_cont`, `lag`, و توابع `to_jalali_month`/`jalali_month_diff` از P0-04). هر ۵۰ مشتری و هر ردیف Cohort در هر دو یکی است. با تغییر عمدی یک عدد Fixture و یک مبلغ Seeder، تست‌ها شکست خوردند.
- **Fixture قفل‌شده به داده:** `meta.dataset_fingerprint` (هش کلیدهای کسب‌وکاری، بدون `id`) با داده‌ی Seed فعلی مقایسه می‌شود؛ با تغییر Seeder تست می‌شکند تا Fixture **آگاهانه** بازبینی شود. بازتولید: Seed تازه + `ReferenceMetrics::compute(DemoDataSeeder::asOf())` + `datasetFingerprint()` و نوشتن JSON، سپس بازبینی Diff.
- **ابهام‌هایی که برای Sprint 4 باید صریح شوند (Fixture با حداقل فرض نوشته شد):** `monetary = SUM(net_revenue − shipping_total)` (D2)؛ `aov = total_revenue / total_orders`؛ `recency_days` نسبت به `as_of` تزریقی (Engine باید پارامتر `as_of` بپذیرد)؛ `distinct_categories` و `churn_reason` در Fixture نیستند (فرمت متن/تعریف در PRD نیامده). Segmentهای `cant_lose` و `lost` با گروه‌بندی الزامی PRD در داده دیده نمی‌شوند (نیازمند مشتری پرتکرار با قدیمی‌ترین Recency) و باید در تست واحد Sprint 4 با داده‌ی ساختگی بیایند.
- **اصلاح پیش‌موجود در `DatabaseSeeder`:** ساخت `Test User` با Factory در اجرای دوم `db:seed` با خطای یکتایی ایمیل می‌شکست؛ حالا فقط اگر وجود نداشته باشد ساخته می‌شود (تست رگرسیون دارد).
- تست: `DemoDataSeederTest` (۱۵) و `ExpectedMetricsFixtureTest` (۹). Suite: ۴۷۲ سبز.

## P2-01 — WooClient + HttpWooClient

فقط لایه انتقال (Transport) خواندنیِ Woo؛ هیچ منطق Sync، DTO، Mapper، Job یا Command ساخته نشد. کد در `app/Modules/Sync/{Services,Support,Exceptions}`.

- **مرز عمومی:** `Services\WooClient` (Interface) فقط دو متد دارد: `page(endpoint, page, ?SyncWindow, query)` و `pages(...)` (Generator). **هیچ متد نوشتنی وجود ندارد و نباید اضافه شود** (CLAUDE.md §0)؛ تست هم Interface و کلاس را با Reflection و هم کل ماژول Sync را برای فعل‌های نوشتنی HTTP اسکن می‌کند. پیاده‌سازی: `HttpWooClient` (Bind در `AppServiceProvider`، تنبل و نه Singleton: نبودن اعتبارنامه بوت اپ را نمی‌شکند و لحظه‌ی درخواست کلاینت `WooConfigurationException` می‌دهد). خروجی داده خام JSON است (`WooPage`)؛ نگاشت به DTO کار P2-03 است.
- **Endpoint فقط مسیر نسبی:** `^[A-Za-z0-9_-]+(/[A-Za-z0-9_-]+)*$`؛ URL مطلق، `//host`، `..` و `?` رد می‌شود تا Basic auth هرگز به میزبان دیگر نرود. Redirect دنبال نمی‌شود (`withoutRedirecting`) و `base_url` باید HTTPS باشد. کلید هرگز در Query String نیست.
- **Frozen cursor = `Support\SyncWindow`:** مقدار غیرقابل‌تغییر؛ `freeze(cursorValue, overlap)` در شروع Job ساخته می‌شود: `modified_before = now()` (به ثانیه کف‌شده تا مقدارِ ذخیره‌شده‌ی Cursor دقیقاً همان مرز پرس‌وجوشده باشد) و `modified_after = cursor − overlap_minutes` (Cursor اول = بدون کران پایین). همان مقدار روی **همه** صفحه‌ها می‌رود و با گذر زمان جابه‌جا نمی‌شود (تست با پیشروی ساعت بین صفحه‌ها). Query طبق PRD: `modified_after/modified_before/orderby=modified/order=asc`؛ فیلتر ارسالی Caller هرگز `page/per_page/پنجره` را Override نمی‌کند. **پیشروی Cursor فقط با `completed` و در SyncService (P2-08) است**، نه اینجا.
- **Retry:** قابل‌تکرار = 429/500/502/503/504 + قطع اتصال/Timeout؛ نردبان `config('woo.retry_backoff_seconds') = [2,8,30,120]`، حداکثر `max_retries = 4` (یعنی ۵ درخواست). در 429 مقدار `Retry-After` (ثانیه یا HTTP-date) بر نردبان مقدم است؛ غایب/نامعتبر/صفر/گذشته → نردبان PRD. خطای خاتمه‌ای (`WooRequestException`، بدون Retry): 400/401/403/404 **و هر وضعیت دیگری که PRD قابل‌تکرار ندانسته** (مثلاً 3xx/405/409/422/501) — پیش‌فرض صریح چون PRD فقط لیست تکرار و چهار وضعیت را نام برده. Log: هر Retry = `warning`، هر شکست نهایی = `error` با `endpoint`، `status`، نام کلیدهای Query (نه مقدارها؛ ممکن است شماره موبایل باشد) و `woo_code`. متن خطای cURL عمداً Log/Rethrow نمی‌شود چون کل URL (با Query) را دارد.
- **پاسخ ناقص (PRD ساکت بود، پیش‌فرض صریح):** 2xx غیر JSON، غیر لیست، آیتم غیرآبجکت، نبودن/نامعتبر بودن `X-WP-TotalPages`، یا آیتم در صفحه‌ای فراتر از `TotalPages` → `WooMalformedResponseException` بدون Retry. هدر نبود را حدس نمی‌زنیم چون حدس اشتباه یعنی Sync ناقصِ بی‌صدا.
- **Rate limit = `Services\RedisTokenBucket`:** Token bucket واقعی روی Redis، یک اسکریپت Lua اتمیک، ظرفیت = `rate_limit_per_minute` (۹۰) و شارژ پیوسته ۱٫۵ توکن/ثانیه. حالت خالی: توکن بعدی **رزرو** می‌شود و Caller همان‌قدر Sleep می‌کند (صف عادلانه؛ ۹۱ام ۶۶۷ms، ۹۲ام ۱۳۳۴ms). هر تلاش HTTP از جمله Retryها یک توکن می‌گیرد. اگر Redis در دسترس نباشد Exception می‌دهد و **هیچ درخواستی فرستاده نمی‌شود** (Fail-closed). ساعت = ساعت PHP (`now()`، برای تست قطعی)، نه `TIME` ردیس. ⚠ **معنای «۹۰ در دقیقه»:** ظرفیت Bucket برابر ۹۰ است، پس بعد از یک دقیقه بیکاری تا ۹۰ درخواست Burst مجاز است و در حالت پایدار ۹۰/دقیقه؛ در بدترین پنجره‌ی لغزان یک‌دقیقه‌ای تا ~۱۸۰. اگر Woo/هاست سخت‌گیرتر بود، ظرفیت را با یک کلید Config جدید کوچک کنید (PRD کلیدی برایش ندارد و اضافه نشد).
- **Config (`config/woo.php`):** `base_url/key/secret` از `.env` (`WOO_BASE_URL`، `WOO_CONSUMER_KEY`، `WOO_CONSUMER_SECRET` — همان نام‌های `verify-woo.php`؛ در `.env.example` خالی)، `version=wc/v3`، `timeout=30`، `per_page=50`، `max_retries=4`، `retry_backoff_seconds` (کلید جدید نسبت به لیست PRD تا اعداد جادویی در کد نباشد)، `rate_limit_per_minute=90`، `overlap_minutes=10`. `webhook_secret/webhook_allowed_ips` با P2-09 می‌آید.
- **زیرساخت تست:** `phpunit.xml` حالا `REDIS_DB=15` و `REDIS_PREFIX=heymode-testing-` می‌دهد تا تست‌های Limiter روی Redis واقعی هرگز داده‌ی توسعه را لمس نکنند. **Redis باید در حال اجرا باشد** (روی مک: `redis-server --daemonize yes`، بالای Setup ثبت‌شده در P0-02)؛ نبودنش تست‌ها را قرمز می‌کند نه Skip. `Http::preventStrayRequests()` تضمین می‌کند هیچ تستی به Woo واقعی نمی‌رسد. `FakeWooClient` و Fixtureها = P2-02.
- ⚠ **فرض‌های تأییدنشده روی Woo زنده (باید در P2-05/P2-06 و GATE 1 دیده شود):** (۱) `dates_are_gmt=true` همراه `modified_*` برای مقایسه با ستون GMT (طبق مستندات Woo؛ بدون آن مقدار با ساعت محلی فروشگاه مقایسه می‌شود). (۲) پشتیبانی `orderby=modified` و `modified_after/before` برای هر Endpoint (Categories احتمالاً اصلاً Window ندارد؛ در محصولات هم Enum مرتب‌سازی باید بررسی شود) — Window اختیاری است و Caller می‌تواند بدون آن صفحه‌بندی کند. (۳) `Retry-After` عمداً سقف ندارد (PRD: «honor»)؛ مقدار بسیار بزرگ Worker را همان‌قدر نگه می‌دارد.
- تست: `HttpWooClientTest` (۶۷)، `RedisTokenBucketTest` (۱۱)، `SyncWindowTest` (۸) = ۸۶ تست. با کاشت عمدی سه نقض (۴۰۴ قابل‌تکرار، نادیده‌گرفتن Retry-After، جابه‌جایی `modified_before`) تست‌ها شکستند و پس از بازگرداندن همه سبز شدند. Suite: ۵۵۸ سبز.

## P2-02 — FakeWooClient + Recorded Fixtures

فقط زیرساخت تست (CLAUDE.md §8: «Tests never hit the network. Use FakeWooClient»)؛ هیچ Migration، Model، DTO، Mapper، Job یا Service همگام‌سازی ساخته نشد و رفتار Production عوض نشد (Binding `WooClient → HttpWooClient` دست‌نخورده؛ تست تضمین می‌کند اپ هیچ‌جا `FakeWooClient` را Bind/ارجاع نمی‌دهد).

- **`Services\FakeWooClient`** همان قرارداد `WooClient` (`page`/`pages` با امضای یکسان، تست‌شده با Reflection) به‌علاوه `requests()` برای Assert روی آنچه کد تحت‌تست خواسته (شامل `SyncWindow`). فقط خواندنی است. **بازپخش (Replay) است، نه HttpWooClient دوم:** Retry، Retry-After، Rate limit و Redis در آن نیست؛ Fixture خودش ثبت می‌کند HttpWooClient چند تلاش کرده (`attempts`: ۱ برای خطای خاتمه‌ای، ۵ برای قابل‌تکرارِ تمام‌شده) و Fake همان نتیجه را به‌صورت همان Page/Exception برمی‌گرداند.
- **Fixture = یک مبادله‌ی ثبت‌شده:** `tests/fixtures/woo/**.json` (پوشه‌ها Endpoint را منعکس می‌کنند) با شکل `{request:{endpoint,page,query}, response:{status,headers,body,attempts?}}`؛ ۱۳ فایل: Orders (۲ صفحه + فیلتر `status=completed`)، Products (صفحه‌ی آخر **خالی** با Header صفحه‌ی اول = حذف رکورد بین دو صفحه)، Categories، مجموعه‌ی خالی (TotalPages 0)، و خطاها: ۴۰۱، ۴۰۴، ۵۰۳ و ۴۲۹ تمام‌شده، پاسخ مخدوش (HTML با ۲۰۰، لیست بدون `X-WP-TotalPages`). `WooFixture` (Support) تعریف را اعتبارسنجی می‌کند (Endpoint نسبی، صفحه ≥ ۱، وضعیت HTTP، Header/Query اسکالر) و `FakeWooClient` دو Fixture برای یک درخواست را رد می‌کند (`LogicException`) تا انتخاب بی‌صدا نشود. بارگذاری از دیسک فقط در `tests/Support/WooFixtures` است، نه در `app/`.
- **تطبیق درخواست:** Endpoint + صفحه + فیلترهای اضافی (مقادیر به رشته و بر اساس کلید مرتب). **Window در کلید نیست** (به ساعت وابسته است و Fixture ثابت هرگز دوبار Match نمی‌شد)؛ فقط ثبت می‌شود. درخواست بی‌Fixture → `WooFixtureNotFoundException` با پیام شامل Endpoint/صفحه و آنچه ثبت شده، **فقط نام کلید فیلترها نه مقدارشان**. این Exception عمداً `LogicException` است، نه `WooException`، تا کد Sync که `WooException` را برای Retry/Log می‌گیرد اشتباه تست را قورت ندهد.
- **جلوگیری از انحراف (Drift) بدون دست‌زدن به P2-01:** `FakeWooClientParityTest` هر ۱۳ مبادله را هم از `HttpWooClient` (روی مرز `Http::fake`، بدون شبکه) و هم از Fake رد می‌کند و نتیجه (Page یا Status/Attempts/Retryable/Code/کلاس Exception) باید یکسان باشد. منطق دسته‌بندی وضعیت در Fake عمداً کپی کوچکی از HttpWooClient است (لیست قابل‌تکرار + اعتبارسنجی بدنه)؛ این تست تنها نگهبان آن است — اگر قانون P2-01 عوض شد باید Fake/Fixture هم عوض شود. Mutation check: حذف ۵۰۳ از لیست قابل‌تکرار ← ۲ تست قرمز؛ برگرداندن صفحه‌ی خالی به‌جای خطا ← ۶؛ فراخوانی Http داخل Fake ← ۲۸؛ درز مقدار فیلتر در پیام ← ۱.
- **ایزوله‌سازی:** تست‌های Fake در `tests/Unit` هستند که Laravel را بوت نمی‌کنند (نه Container، نه DB، نه Redis، نه Config)؛ اسکن Token-محور هم تضمین می‌کند `FakeWooClient`/`WooFixture` به `Illuminate\`، Guzzle، `Http/Redis/DB/Cache::`، `config()/now()/app()`، Randomness و فعل‌های نوشتنی ارجاع ندارند. تست Feature هم ثابت می‌کند بعد از `instance(WooClient::class, …)` هیچ Query، کلید Redis یا درخواست HTTP رخ نمی‌دهد.
- ⚠ **Fixtureها «ثبت‌شده» از نظر شکل‌اند، نه ضبط‌شده از Woo زنده:** داده ساختگی و دست‌نویس است (موبایل‌های `0900…` که با `PhoneNormalizer` به `989000…` می‌روند، ایمیل خالی، بدون هیچ اعتبارنامه‌ای؛ تست Hygiene این‌ها را قفل می‌کند). شکل Order (مبالغ رشته‌ای، `currency=IRT`، `variation_id`/`product_id` صفر برای محصول حذف‌شده، موبایل با ارقام فارسی و علامت RTL) بر اساس یافته‌های P0-00 است و روی فروشگاه زنده Diff نشده. در P2-03/P2-05/P2-06 و GATE 1 باید با پاسخ واقعی (ناشناس‌شده) مقایسه شود.
- ⚠ **پوشش عمداً نساخته‌شده:** خطای اتصال/Timeout (`status = null`) Fixture ندارد — نیازی از Backlog نیامده؛ هر وقت یک تست Sync لازم داشت، اضافه‌ی کوچکی روی `WooFixture/replay` است. Endpoint `refunds`/`variations` موفق و `customers` موفق ثبت نشده (P2-05..P2-07 خودشان اضافه می‌کنند).
- **شاخه:** P2-01 پیش‌تر روی `main` (و Origin) بود و شاخه‌ی `sprint/2-sync` محلی وجود نداشت؛ طبق CLAUDE.md §9 (ادغام در main فقط پس از قبولی Sprint) شاخه‌ی `sprint/2-sync` دوباره از HEAD فعلی `main` (`e628b61`) ساخته شد.
- تست: `FakeWooClientTest` (۴۷، Unit) و `FakeWooClientParityTest` (۱۶، Feature) = ۶۳. Suite: ۶۲۱ سبز؛ Pint/PHPStan سبز.

## P2-03 — DTOs + Mappers + OrderStatusMapper (TEST FIRST)

فقط لایه‌ی تبدیل «پاسخ خام Woo → DTO»؛ هیچ DB/HTTP/Redis/Job/Model/سرویس دیگری در آن نیست و P2-04..P2-12 ساخته نشد. کد در `app/Modules/Sync/{DTOs,Mappers}` و `app/Modules/Orders/Services/OrderStatusMapper.php`؛ Migration، Route، Provider، `WooClient`/`HttpWooClient`/`FakeWooClient` دست‌نخورده‌اند.

- **DTOها (هفت‌تا، همه `final readonly`، فقط سازنده، بدون `mixed`/`float`):** `CategoryDto`، `ProductDto`، `VariationDto`، `OrderDto`، `OrderItemDto`، `RefundDto`، `RefundItemDto`. پول `int` تومان، زمان `CarbonImmutable` در UTC. `Customer` DTO ساخته نشد: P2-03 به آن نیاز ندارد، و اطلاعات هویتی سفارش (`wooCustomerId`، نام‌های Billing، موبایل) فیلد خود `OrderDto` است.
- **Mapperها (هرکدام دقیقاً یک متد عمومی `map()`):** `CategoryMapper`، `ProductMapper`، `VariationMapper(raw, wooProductId)`، `OrderMapper` (اقلام را داخل خودش می‌سازد)، `RefundMapper(raw, wooOrderId)` + `PayloadReader` (دسترسی تایپ‌شده و «بلند» به یک آبجکت JSON). Payload واریانت و عودت والد خود را نمی‌گوید، پس `wooProductId`/`wooOrderId` از Endpoint می‌آید نه از داده.
- **«فیلد ضروری گم‌شده ≠ مقدار جعلی»:** هر فیلد ضروری گم یا مخدوش → `WooMappingException` (زیرکلاس `WooException`) با `entity` و **مسیر فیلد** (`line_items.0.total`) و **نوع** مقدار، هرگز خود مقدار (ممکن است موبایل/نام باشد). `null` فقط جایی است که Woo واقعاً «نبود» می‌گوید: `''`/غایب برای `sku`/`slug`/`phone`/`reason`/`payment_method`، `0` برای `product_id`/`variation_id`/`parent`/`customer_id` (مهمان)، `null` برای `date_paid_gmt`/`date_completed_gmt`، `''` برای قیمت. **مقدار مخدوش در همین فیلدها هم خطاست، نه null** (تست‌شده: تاریخ پرداخت `garbage` خطا می‌دهد).
- **پول:** فقط اعداد صحیح کانونی: رشته `^(0|[1-9][0-9]*)$` (از `App\Support\Money::parseToman` رد می‌شود)، `int` نامنفی، یا `float` صحیحِ نامنفی؛ اعشار، منفی، جداکننده، فاصله، **صفر پیشرو** (`09121234567` موبایل است نه مبلغ — یک تست همین را قبل از سخت‌گیری گرفت) و سرریز رد می‌شود. مبلغ‌های منفی Woo در عودت (`total`، `quantity`) فقط در `RefundMapper` با `absoluteMoney/absoluteInt` به بزرگی مثبت تبدیل می‌شوند.
- **رشته‌های خام Woo، نه Enum:** `OrderDto::status`، `ProductDto::type/status`، `VariationDto::status` رشته‌ی خام Woo‌اند. دلیل: (۱) وضعیت سفارش طبق P1-03/P1-05 عمداً Enum ندارد، (۲) Sync مجاز به `use` کردن Enum ماژول Catalog نیست (Arch: بین‌ماژولی فقط Services/Events)، (۳) مقدار افزونه‌ای/سفارشی نباید ردیف را رد کند — این همان الزام ثبت‌شده در P1-02 برای «نگاشت، نه رد» است و نگاشت به Enumهای Catalog کار P2-05 می‌ماند.
- **`OrderStatusMapper::isRealized(string): bool`** (Orders/Services، طبق PRD §07): فقط `in_array` **دقیق** روی `config('woo.realized_statuses')` که در هر فراخوانی دوباره خوانده می‌شود؛ هیچ لیست/رشته‌ی وضعیتی در کد نیست (تست Scan + Arch قدیمی). ناشناخته/سفارشی/حروف‌متفاوت/`wc-` پیشوند‌دار → **نه realized** (پیش‌فرض ایمن «درآمد نیست»). Config خراب (غیرآرایه یا عضو خالی/غیررشته) → `LogicException`؛ لیست خالی مجاز است و یعنی هیچ چیز realized نیست. ⚠ **تفسیر «عودت جزئی/کامل» در جدول تست PRD §24:** ورودی Mapper فقط وضعیت است؛ عودت جزئی وضعیت Woo را `completed` نگه می‌دارد (realized)، عودت کامل از مسیر وضعیت Woo `refunded` می‌شود (با Config فعلی realized نیست). محاسبه‌ی `is_fully_refunded`/`refunded_total` و حالت «عودت کامل ولی وضعیت `completed`» (مثل مشتری Seeder در P1-06) مربوط به P2-07 است و اینجا نیامد.
- **آنچه Mapper عمداً انجام نمی‌دهد:** موبایل را نرمال نمی‌کند (`billingPhone` دقیقاً همان‌طور که Woo فرستاد، حتی با ارقام فارسی و RTL؛ `PhoneNormalizer` هنگام Resolve هویت در P2-04 اجرا می‌شود و سرنوشت موبایل نامعتبر تصمیم P2-04 است)، `is_realized` نمی‌سازد، `subtotal` سفارش را محاسبه نمی‌کند (Woo روی Order `subtotal` ندارد؛ P2-06 آن را از مجموع `lineSubtotal` اقلام می‌سازد)، ارز را چک نمی‌کند (`OrderDto::currency` می‌آید تا P2-06 با واحد فروشگاه مقایسه کند — نتیجه‌ی P0-00: `IRT`، بدون اعشار)، `is_full` عودت را تعیین نمی‌کند.
- **Arch (`tests/Arch/SyncMappingTest`، ۵ تست):** DTO/Mapper به `Illuminate\Database|Http|Redis|Queue|Bus|Log|Cache|Events|Facades`، Guzzle، `DB::/Http::/…`، `dispatch/app/config/now/env`، متدهای Eloquent، Model و هر ماژول دیگر ارجاع نمی‌دهند؛ Service/PhoneNormalizer/OrderStatusMapper/CustomerIdentity را صدا نمی‌زنند؛ هر Mapper فقط `map()` دارد.
- **Fixtureهای P2-02 (فقط افزوده، نه بازطراحی):** Woo فیلدهایی می‌فرستد که Fixtureهای حداقلیِ P2-02 نداشتند و Mapper الزامی می‌داند (`discount_total`، `total_tax`، `payment_method`، `date_paid_gmt/date_completed_gmt`، `coupon_lines`، `subtotal`/`price` اقلام؛ `slug`/`date_created_gmt`/`variations` محصول). این‌ها به Fixtureهای `orders/*` و `products/page-1` **اضافه** شد و دو Fixture موفق جدید (`products/103/variations`، `orders/5001/refunds`). با اسکریپت مقایسه با `HEAD` ثابت شد هیچ کلید/مقداری حذف یا عوض نشده؛ Parity P2-02 هر دو Fixture جدید را هم خودکار پوشش می‌دهد. تنها تغییر کد تست P2-02: `WooFixtures::paths()` حالا بازگشتی است (Glob قبلی فقط تا دو سطح پوشه می‌رفت) و تست Hygiene هم از آن استفاده می‌کند.
- ⚠ **فرض‌های تأییدنشده روی Woo زنده:** (۱) نام و شکل فیلدها (`total_tax`، `date_*_gmt`، `line_items[].price/subtotal`، `coupon_lines[].code`، Refund `amount`/`line_items[].total` منفی) از مستندات Woo v3 و یافته‌های P0-00 است، نه Diff با پاسخ واقعی؛ Fixtureها دست‌نویس‌اند. (۲) `date_*_gmt` فقط با قالب دقیق `Y-m-d\TH:i:s` پذیرفته می‌شود (بدون کسر ثانیه/Offset) — اگر فروشگاه واقعی چیز دیگری داد، خطای بلند می‌دهد و باید آگاهانه گسترش یابد. (۳) Refund line item فقط `id/product_id/variation_id/sku/quantity/total` دارد؛ اگر P2-07 برای تطبیق با قلم اصلی به `meta_data._refunded_item_id` نیاز داشت، باید به `RefundItemDto` اضافه شود (P2-07).
- تست (همه بدون DB؛ Unit/Arch): `OrderStatusMapperTest` (۳۲)، `PayloadReaderTest` (۸۷)، `OrderMapperTest` (۶۴)، `CatalogMappersTest` (۳۹)، `RefundMapperTest` (۲۰)، `DtoTest` (۳۰)، `SyncMappingTest` (۵) = ۲۷۷. **TEST FIRST:** همه قبل از پیاده‌سازی نوشته و قرمز (Class not found) دیده شدند. Mutation check (۸ نقض عمدی، همگی گرفته شدند و برگردانده شدند): مقدار پیش‌فرض ۰ برای `total` گم‌شده ← ۱ تست قرمز؛ `float` کسری به‌عنوان پول ← ۳؛ تاریخ مخدوش → `null` ← ۴؛ وضعیت‌های realized هاردکد ← ۹؛ وضعیت ناشناخته realized ← ۲۴؛ مقایسه‌ی غیرحساس به حروف ← ۱؛ نرمال‌سازی موبایل داخل Mapper ← ۴؛ DTO غیر‌readonly ← ۱. Suite: ۹۰۰ سبز.

## P2-04 — CustomerIdentityService + Identity Conflicts (TEST FIRST)

`Customers\Services\CustomerIdentityService::resolve(?string $phoneRaw, ?string $firstName, ?string $lastName, IdentitySource $source, string $sourceId, ?int $wooOrderId = null): Customer` طبق جریان PRD §08. **بدون Migration** (Schema P1-01 کافی بود)، بدون Route/Provider/Config، و بدون تغییر در P2-01..P2-03. ورودی‌ها primitive‌اند، نه `OrderDto`: جدول وابستگی ماژول‌ها (PRD §07) به Customers فقط Core را می‌دهد و Sync (P2-06) این سرویس را صدا می‌زند نه برعکس.

- **شماره‌ی نرمال‌شده تنها کلید هویت است:** هر موبایل اول از `PhoneNormalizer::normalize()` رد می‌شود (نه منطق موازی)؛ موبایل نامعتبر → همان `InvalidPhoneException` و **هیچ چیز نوشته نمی‌شود**. ایمیل، نام، Woo customer id و آدرس هرگز کلید Match نیستند و سرویس اصلاً پارامتر ایمیل ندارد (تست Reflection + Scan).
- **همزمانی:** `customers.phone_normalized` UNIQUE است (شامل ردیف‌های Soft-delete)، پس مشتری با `INSERT … ON CONFLICT DO NOTHING` (`insertOrIgnore`) ساخته می‌شود — باخت در مسابقه خطا نیست و تراکنش PostgreSQL را Abort نمی‌کند — و سپس با `SELECT … FOR UPDATE` خوانده می‌شود. قفل ردیف، Resolveهای هم‌زمان همان موبایل را سریال می‌کند و همین «چک-سپس-Insert» conflict را بدون Unique Index روی `identity_conflicts` ایمن می‌کند. مسابقه‌ی باخته با یک Insert تزریقی بین Lookup و Insert شبیه‌سازی و تست شد. مشتری Soft-delete‌شده‌ای که موبایل را دارد **پیدا می‌شود و بازیابی نمی‌شود** (Unique Index جلوی ردیف دوم را می‌گیرد و سفارش نباید گم شود).
- **تراکنش:** کل Resolve (مشتری + Identity + به‌روزرسانی نام/موبایل خام + Conflict) یک `DB::transaction` است؛ Eventها با `DB::afterCommit` می‌روند، پس با Rollback هیچ Eventی نمی‌رود (تست‌شده با شکست عمدی در ساخت Conflict: نه Identity نیمه‌کاره، نه نام/موبایل عوض‌شده). `customer_identities` با `ON CONFLICT DO NOTHING` (PRD §08 گام ۴) نوشته می‌شود.
- **معنای «نام سازگار»:** PRD می‌گوید «last_name differs and similarity low» ولی هیچ‌جا (PRD/ARCHITECTURE/CLAUDE) الگوریتم یا آستانه‌ی Similarity تعریف نشده — **فازی اختراع نشد.** فقط `last_name` مقایسه می‌شود و دو نام فقط وقتی سازگارند که **شکل کانونی** (`PersonNameNormalizer`) یکسان باشد: حذف تفاوت‌های تایپی (ي/ك/ى عربی ↔ ی/ک فارسی، نیم‌فاصله و هر نوع فاصله، علامت‌های جهت، کشیده، اعراب، حروف Latin). هر تفاوت دیگری Conflict است — جهت محافظه‌کارانه، چون Conflict هرگز ادغام یا مسدود نمی‌کند. ⚠ اگر ناظر محصول Similarity دقیق‌تری می‌خواهد (مثلاً `ریزی/رضایی`) باید تعریفش صریح شود.
- **نام‌ها:** نام خالی/غایب هرگز نام موجود را خالی نمی‌کند؛ نام موجود‌نداشته پر می‌شود؛ در حالت سازگار به «جدیدترین مقدار غیرخالی» به‌روز می‌شود (PRD گام ۵) و `display_name` دوباره ساخته می‌شود؛ در حالت **Conflict نام مشتری دست‌نخورده می‌ماند** (فقط `phone_raw_last` به‌روز می‌شود). نام/موبایل خام بلندتر از عرض ستون بریده می‌شود تا سفارش رد نشود.
- **Conflict:** یک ردیف `pending` با `existing_name`/`incoming_name` (نام کامل)، `woo_order_id`، `reason = last_name_mismatch`. **Idempotency:** یک ردیف به‌ازای (مشتری، نام ورودی، سفارش)؛ Resync همان سفارش یا Resolve دوباره **پس از بازبینی انسانی** (هر وضعیت) دوباره Conflict نمی‌سازد؛ بدون سفارش، کلید (مشتری، نام ورودی) است. سفارش با نام متفاوت همچنان به همان مشتری وصل می‌شود (Identity جدیدش هم ثبت می‌شود).
- **Eventهای عمومی Customers (PRD §07):** `CustomerCreated(customerId)` و `IdentityConflictDetected(customerId, conflictId, wooOrderId)` — فقط شناسه، بدون نام/موبایل — بعد از Commit و فقط وقتی واقعاً ساخته/ثبت شد (نه در مسابقه‌ی باخته یا Conflict تکراری). هنوز Listenerی ندارند.
- ⚠ **باگ Schema کشف‌شده (P1-01، خارج از Scope این تسک، رفع نشد):** `identity_conflicts.status` طبق PRD §09 `varchar(15)` است ولی مقدار مجاز `confirmed_different` **۱۹ کاراکتر** است؛ ذخیره‌ی آن با `value too long for type character varying(15)` رد می‌شود (تست P2-04 هنگام Update مستقیم ردیف Conflict به آن رسید). PRD خودش متناقض است و Migration منتشرشده دقیقاً از آن پیروی کرده. P2-04 فقط `pending` می‌نویسد و اثر نمی‌گیرد، ولی **هر جریان بازبینی (P2-12/بعدی) که Conflict را «متفاوت تأیید‌شده» کند می‌شکند**؛ رفع = یک Migration جدید که ستون را حداقل `varchar(20)` کند (Migration منتشرشده ویرایش نمی‌شود) و باید قبل از ساخت صفحه‌ی بازبینی، با تأیید، انجام شود. تست‌های Resolve-شده فعلاً فقط `confirmed_same` و `ignored` را پوشش می‌دهند (کامنت در تست).
- ⚠ **گام ۱ PRD (`InvalidPhoneException → store order with needs_review = true`) با Schema فعلی قابل اجرا نیست:** `orders.customer_id` NOT NULL/RESTRICT است، سفارش ستون بازبینی ندارد و مشتری بدون `phone_normalized` ساخته نمی‌شود. طبق دستور همین تسک، سرویس همان Exception را می‌اندازد؛ **تصمیم اینکه سفارش بدون موبایل قابل‌استفاده چه شود با P2-06 است** (پیشنهاد: تصمیم صریح قبل از آن). همچنین ⚠ پیام `InvalidPhoneException` (P0-03) خود شماره‌ی خام را دارد؛ Caller نباید آن پیام را Log کند (CLAUDE.md §6) — تغییر خود Exception خارج از Scope این تسک است.
- **تصمیم‌های صریح دیگر (PRD ساکت بود):** (۱) `first_seen_at` ست نمی‌شود — منبع درستش تاریخ اولین سفارش است که Resolve نمی‌داند (P2-06). (۲) `needs_review` مشتری روی Conflict ست نمی‌شود (PRD فقط ردیف `identity_conflicts` را می‌خواهد). (۳) اگر `(source, source_id)` قبلاً به مشتری دیگری وصل است، طبق `ON CONFLICT DO NOTHING` PRD همان‌جا می‌ماند و سفارش با موبایل Resolve می‌شود؛ Conflictی برایش ثبت نمی‌شود. (۴) `confidence` پیش‌فرض DB (`high`) است.
- تست (PostgreSQL واقعی): `PersonNameNormalizerTest` (۲۲، بدون Laravel) و `CustomerIdentityServiceTest` (۴۹) = ۷۱. **TEST FIRST:** قبل از پیاده‌سازی نوشته و قرمز (Class not found) دیده شدند؛ یک شکست در اجرای اول باگ داده‌ی خود تست بود (ارقام فارسی ۱۲رقمی) و دیگری همان باگ Schema بالا. Mutation check (۸ نقض عمدی، همگی گرفته و برگردانده شدند): دور زدن `PhoneNormalizer` ← ۲۵ تست قرمز؛ Insert ساده به‌جای `ON CONFLICT DO NOTHING` ← ۲؛ Conflict که مشتری دوم می‌سازد ← ۴؛ حذف ثبت Conflict ← ۹؛ بازنویسی نام مشتری در Conflict ← ۲؛ ایمیل به‌عنوان کلید ← ۱؛ حذف Dedupe ← ۵؛ حذف تراکنش ← ۱ (دقیقاً تست Rollback). ⚠ **محدودیت اثبات:** قفل `FOR UPDATE` با تست تک‌اتصالی اثبات نمی‌شود (تست چند-اتصالی هم‌زمان در این محیط ممکن نیست)؛ ایمنی اصلی یکتایی مشتری را خود Unique Index + `ON CONFLICT` تضمین می‌کند و تست‌شده است، قفل فقط Dedupe Conflictها را زیر هم‌زمانی سریال می‌کند. Suite: ۹۷۱ سبز.

## P2-05 — Category + Product + Variation Sync

Woo → Catalog به‌صورت **Upsert آینه‌ای**. **بدون Migration** (Schema P1-02 کافی بود)، بدون Route/Provider/Config، و بدون دست‌زدن به P2-01..P2-04 یا Fixtureهای P2-02. Pint طبق AGENTS.md: `vendor/bin/pint --dirty --format agent`.

- **معماری و مرز ماژول:** `Sync\Services\CatalogSyncService` (فقط از `WooClient` می‌خواند، با Mapperهای P2-03 به DTO تبدیل می‌کند) → `Catalog\Services\CatalogService` (API عمومی نوشتن، PRD §07) → مدل‌های Catalog. جدول وابستگی PRD §07 به Catalog فقط Core را می‌دهد، پس Catalog نمی‌تواند DTOهای Sync را بپذیرد؛ ورودی‌ها **Value Objectهای متعلق به Catalog** هستند (`CategoryInput`، `ProductInput`، `VariationInput`، زیر `Catalog\Services` چون Arch فقط `Services`/`Events` را برای ارجاع بین‌ماژولی مجاز می‌داند) و Sync فیلدها را کپی می‌کند. تکرارِ شکل DTO نتیجه‌ی مستقیم همین قاعده است. نوع/وضعیت Woo تا مرز Catalog رشته می‌ماند و نگاشت به Enumهای Catalog همان‌جاست (الزام ثبت‌شده در P1-02).
- **هویت = فقط `woo_*_id`** (هرکدام UNIQUE): یک Woo id ⇒ یک ردیف؛ تکرار همان ورودی هیچ چیز را عوض نمی‌کند (حتی `updated_at` دسته‌ها). **هیچ‌چیز هرگز حذف نمی‌شود:** دسته/محصول/واریانتی که Woo دیگر فهرست نمی‌کند می‌ماند (Order item و هزینه‌ی دستی `product_costs` ممکن است به آن اشاره کنند؛ حذف RESTRICT/SET NULL خطرناک است و PRD چیزی درباره‌اش نگفته).
- **دسته‌ها:** یک دسته‌بندی کامل (همه صفحه‌ها) اول Map می‌شود و بعد **یک تراکنش دو-مرحله‌ای** می‌نویسد: مرحله ۱ Upsert همه، مرحله ۲ اتصال `parent_id` — پس ترتیب والد/فرزند و صفحه‌بندی مهم نیست و یک Payload مخدوش هیچ ردیفی نمی‌نویسد. والد ناشناخته ⇒ `parent_id = NULL` + Warning (شناسه‌ها)، **هرگز دسته‌ی ساختگی**؛ Sync بعدی که والد را داشته باشد وصل می‌کند. اگر Woo والد را عوض کند/ریشه کند، همان اعمال می‌شود (والد قدیمیِ دیگر ادعا‌نشده نگه داشته نمی‌شود).
- **محصول:** Upsert با `woo_product_id`؛ `created_at_woo` مال Woo است (Payload بدون تاریخ آن را پاک نمی‌کند)، `synced_at` = زمان محلی Sync، `created_at` محلی دست‌نخورده. نوع/وضعیت خارج از مجموعه‌ی هسته **رد نمی‌شود:** نوع ناشناخته → `simple` (پیش‌فرض خود ستون در PRD)، وضعیت ناشناخته (`future`/`trash`/…) → `draft` (غیرفعال)، هر دو با Warning و مقدار خام Woo. ⚠ **این دو نگاشت تصمیم صریح من است، نه PRD** (Enum اجازه‌ی مقدار دیگر نمی‌دهد و Migration جدید لازم می‌شد)؛ اطلاعات نوع خام ذخیره نمی‌شود.
- **دسته‌های محصول = جایگزینی (Mirror):** مجموعه‌ی `product_category_product` دقیقاً برابر مجموعه‌ی فعلی Woo می‌شود (`sync()`؛ دسته‌ی حذف‌شده از محصول Unlink می‌شود ولی خودِ دسته حذف نمی‌شود). دلیل: CLAUDE.md §0 (Woo منبع حقیقت) و P1-02 («فقط داده‌ی آینه‌ای»)؛ Pivot داده‌ی مستقل ندارد و قابل بازسازی است، Merge ارتباط کهنه را برای همیشه نگه می‌داشت. دسته‌ای که هنوز Sync نشده **رد می‌شود و Warning می‌دهد** (Product همچنان Sync می‌شود؛ «درآمد مهم‌تر از نگاشت»).
- **واریانت‌ها:** با `woo_variation_id`؛ والد از **ساختار** می‌آید (واریانت همراه `ProductInput` می‌آید و هرگز جدا مشخص نمی‌شود) پس به محصول اشتباه وصل نمی‌شود. فقط برای محصولاتی که نوع Woo‌شان `variable` است، از `products/{id}/variations` **قبل از نوشتن محصول** خوانده می‌شوند (محصول متغیر هرگز بدون واریانت‌هایش ذخیره نمی‌شود). واریانت Woo که قبلاً زیر محصول دیگری است → `CatalogIntegrityException(variation_parent_mismatch)`، جابه‌جا نمی‌شود.
- **تعارض SKU (⚠ سیاست در PRD تعریف نشده — سیاستی اختراع نشد):** SKUی که واریانت دیگری دارد، یا تکرار SKU در همان Payload ⇒ `CatalogIntegrityException(sku_conflict)` و **هیچ چیزی نوشته نمی‌شود** (نه بازواگذاری، نه NULL کردن، نه دست‌کاری هیچ‌کدام). Index جزئی یکتای DB خط دفاع آخر است. مالکِ‌ همان SKU که دوباره Sync شود تعارض نیست؛ SKU آزادشده قابل استفاده‌ی مجدد است. **پیامد مهم برای P2-08:** این خطا قطعی است (تکرار Job همان را می‌دهد)، پس اگر Cursor فقط با موفقیت جلو می‌رود، یک تعارض واقعی Sync محصولات را متوقف می‌کند تا انسان رفعش کند؛ اینکه Job چنین خطایی را «هشدار و ادامه» یا «شکست» بداند تصمیم P2-08/محصول است.
- **محصول ساده و SKU/قیمتش:** `products` ستون `sku`/`price` ندارد و PRD نمی‌گوید محصول ساده «واریانت پیش‌فرض» (با `woo_variation_id NULL`) بگیرد؛ این تصمیم اختراع نشد. پس `ProductDto::sku/price` ذخیره نمی‌شود و SKU محصول ساده با Sync فعلی در Catalog **قابل Resolve نیست** (فقط واریانت‌ها SKU دارند). ⚠ **باید قبل از P2-06 تصمیم‌گیری شود** (گام ۲/۳ Resolve و هزینه‌ی سطح واریانت به آن وابسته‌اند).
- **تراکنش:** هر **محصول** (ردیف + Pivot + همه‌ی واریانت‌ها) یک `DB::transaction`؛ هر **دسته‌بندی** یک تراکنش. عمداً یک تراکنش غول‌آسا روی کل Sync نیست. شکست در واریانت دوم، محصول و واریانت اول و Pivot را Rollback می‌کند؛ محصولات قبلاً Commit‌شده می‌مانند و Run تکرارپذیر است. `ProductSynced(productId, wooProductId)` (فقط شناسه) بعد از Commit و فقط برای نوشتنِ موفق. `lockForUpdate` روی خواندن ردیف؛ رقابت هم‌زمان نادر (Job یکتاست) با Unique Index بلند شکست می‌خورد، نه خراب‌کاری.
- **آنچه ساخته نشد:** `resolveVariationBySku()` (PRD §07؛ الگوریتم Resolve مال P2-06 است)، SyncService/Job/Cursor/Window (Full List بدون Window، طبق PRD §10 Polling شبانه)، هزینه/قیمت‌گذاری/موجودی.
- **⚠ پیش‌فرض‌های تأییدنشده:** فقط نوع Woo `variable` واریانت دارد (افزونه‌های `variable-subscription` واریانت‌شان خوانده نمی‌شود و محصول `simple` ذخیره می‌شود — Warning می‌دهد)؛ `products/{id}/variations` روی فروشگاه زنده با Diff بررسی نشده؛ نام یا مقدار بیش از عرض ستون (۱۶۰/۲۵۰) خطای DB بلند می‌دهد (عمداً بریده نمی‌شود، برخلاف نام مشتری در P2-04).
- تست (PostgreSQL واقعی): `CatalogServiceTest` (۴۹)، `CatalogSyncServiceTest` (۱۳، با `FakeWooClient` و Fixtureهای ثبت‌شده، بدون HTTP)، `CatalogSyncBoundaryTest` (۳، Arch) = ۶۵. Mutation check (۱۲ نقض عمدی، همگی گرفته و برگردانده شدند): دور زدن یکتایی محصول ← ۱۵ تست قرمز؛ یکتایی واریانت ← ۱۰؛ نادیده‌گرفتن SKU ← ۵؛ Pivot با Merge ← ۱ و با Attach تکراری ← ۴؛ شناسایی محصول با نام ← ۵؛ شناسایی واریانت با SKU ← ۷؛ واریانت به محصول اشتباه ← ۳؛ از دست رفتن والد دسته ← ۵؛ حذف چک والد واریانت ← ۱؛ دور زدن Mapper ← ۲؛ حذف تراکنش محصول ← ۶ و دسته‌بندی ← ۱. Suite: ۱۰۳۶ سبز.

## P2-06 — Order + Items Sync + 4-Step Product Resolution

Woo → Orders: `Sync\Services\OrderSyncService::syncPage(int $page = 1, ?SyncWindow $window = null): OrderSyncResult` (فقط از `WooClient` می‌خواند، با `OrderMapper` P2-03 به DTO تبدیل می‌کند، ارز را چک می‌کند) → `Orders\Services\OrderService::upsert(OrderInput): int` (PRD §07) که `CustomerIdentityService` و `CatalogService` را از طریق Serviceشان صدا می‌زند. **بدون Migration** (Schema P1-03 کافی بود)؛ P2-01/02/03، Fixtureها، `OrderStatusMapper`، Provider و Route دست‌نخورده. مرز ماژول مثل P2-05: Orders فقط Core/Customers/Catalog را می‌بیند، پس ورودی‌ها Value Object متعلق به Orders هستند (`OrderInput`، `OrderItemInput`؛ **بدون فیلد ایمیل** و بدون فیلد عودت).

- **یک سفارش + مجموعه‌ی کامل اقلامش = یک تراکنش:** هویت `woo_order_id` (UNIQUE)؛ تکرار همان سفارش هیچ چیز را عوض نمی‌کند؛ ردیف Soft-delete‌شده در همان‌جا به‌روز می‌شود (نه تکرار، نه بازیابی). اقلام طبق PRD §10 **حذف-سپس-درج** می‌شوند؛ شکست در هر مرحله (حتی درج قلم) مشتری تازه‌ساخته، سفارش و اقلام را با هم Rollback می‌کند و حالت قبلی سالم می‌ماند (تست‌شده). هر سفارش تراکنش جدا دارد، نه یک تراکنش روی کل صفحه؛ شکست وسط صفحه سفارش‌های قبلی را Commit‌شده می‌گذارد و Sync دوباره ایمن است.
- **مشتری:** فقط `CustomerIdentityService` (شماره‌ی نرمال‌شده). Conflict نام سفارش را رد نمی‌کند (P2-04). برای اینکه Orders به Enumهای Customers وابسته نشود (Arch: فقط Services/Events)، یک متد نازک **افزوده** شد: `CustomerIdentityService::resolveForWooOrder(phone, first, last, ?wooCustomerId, wooOrderId)` — کاربر ثبت‌نام‌شده با شناسه‌ی مشتری Woo (`woo_user`)، مهمان با شناسه‌ی سفارش (`woo_guest_order`). سمانتیک P2-04 تغییر نکرد. ⚠ **سفارش بدون موبایل قابل‌استفاده:** گام ۱ PRD §08 («ذخیره با needs_review») با Schema قابل اجرا نیست (`orders.customer_id` NOT NULL، سفارش ستون بازبینی ندارد، مشتری بدون `phone_normalized` ساخته نمی‌شود). سرویس **Exception بلند** می‌دهد و چیزی نمی‌نویسد: `OrderCustomerUnresolvedException(wooOrderId)`؛ پیام فقط شناسه‌ی سفارش را دارد و Exception اصلی را زنجیر نمی‌کند (متنش شماره‌ی خام دارد). **پیامد:** چنین سفارشی هرگز در آینه نمی‌آید و GATE 1 (اختلاف تعداد سفارش = صفر) را برایش نقض می‌کند — تصمیم (مشتری جایگزین؟ `customer_id` nullable؟ ستون بازبینی روی سفارش؟) نیازمند Migration و تأیید است و **پیش از GATE 1** باید گرفته شود. داده‌ی P0-00 (V4: ۱۰۰٪ موبایل دارند) این را نادر می‌کند نه ناممکن.
- **`is_realized`:** فقط `OrderStatusMapper::isRealized()` (Config)؛ وضعیت خام Woo همان‌طور که هست ذخیره می‌شود؛ تست با تغییر Config نتیجه‌ی جدید می‌گیرد و Arch رشته‌ی وضعیت در سرویس را ردیابی می‌کند.
- **پول:** همه `int`؛ `subtotal` سفارش = مجموع `lineSubtotal` اقلام (Woo روی Order `subtotal` ندارد — تصمیم P2-03 که اینجا اجرا شد). **ارز:** `OrderDto::currency` با `config('woo.currency')` (تازه اضافه شد، `IRT` طبق P0-00) مقایسه می‌شود و ارز دیگر قبل از هر نوشتن `WooCurrencyMismatchException` می‌دهد — هرگز تبدیل نمی‌شود. کلیدهای `subtotal tax`/`total tax` اقلام در Schema و DTO نیستند و اختراع نشدند.
- **فیلدهای عودت مال P2-07 است و اینجا نوشته/صفر نمی‌شود:** `refunded_total` و `is_fully_refunded` هرگز در Upsert نیستند (Arch این را قفل می‌کند)، سفارش جدید مقدار پیش‌فرض DB (۰/false) می‌گیرد. چون حذف-سپس-درج `refunded_qty`/`refunded_amount` اقلام را بی‌صدا صفر می‌کرد (و با `refunded_total` نگه‌داشته‌شده ناسازگار می‌شد)، این دو مقدار به قلم جایگزین **با همان `woo_item_id` منتقل می‌شوند**؛ قلم جدید ۰ می‌گیرد. ⚠ این انحراف کوچک از «حذف-سپس-درج»ِ لفظی تصمیم من است؛ P2-07 همچنان باید آن‌ها را بازمحاسبه کند.
- **Resolve چهارمرحله‌ای (PRD §10)، دقیقاً به این ترتیب و بدون مرحله‌ی پنجم:** (۱) شناسه‌ی Variation Woo اگر وجود دارد (Woo صفر = «ندارد»؛ DTO آن را `null` می‌کند و هرگز پرس‌وجو نمی‌شود)؛ (۲) product id + SKU؛ (۳) SKU تنها؛ (۴) هیچ‌کدام ⇒ `product_id`/`variation_id` = NULL، SKU و `name_snapshot` حفظ، Warning (`woo_order_id`، `woo_item_id`، ids، sku — بدون PII)، سفارش موفق. **مرحله‌ی ۲ و ۳ همیشه یک نتیجه می‌دهند** چون Index جزئی SKU در DB فقط یک Variation برای هر SKU می‌گذارد؛ پس Tie-break اختراع نشد و ترتیب مراحل با **دنباله‌ی پرس‌وجوهای واقعی** تست شد (چه Lookupی، به چه ترتیب، کجا متوقف می‌شود). فقط Variation‌ها Resolve می‌شوند؛ Product از `variation.product_id` می‌آید. هرگز با نام یا فازی Resolve نمی‌شود و هرگز ردیف Catalog ساخته نمی‌شود. Lookupها **فقط‌خواندنی** و در `CatalogService` (`resolveVariationByWooId`، `resolveVariationByProductAndSku`، `resolveVariationBySku` — نام آخری در PRD §07 هست) و ترتیب در `OrderService`. هر Sync دوباره Resolve می‌کند، پس قلم Resolve‌نشده‌ی امروز بعد از رسیدن Catalog با Sync بعدی وصل می‌شود.
- ⚠ **محدودیت شناخته‌شده‌ی P2-05 که اینجا عملی شد:** فقط Variationها SKU دارند و محصول ساده Variation ندارد، پس **خط سفارشِ محصول ساده Resolve نمی‌شود** (NULL + SKU/نام) — مرحله‌ی ۲ «product id + SKU» فقط با Variation کار می‌کند. سفارش‌ها و درآمد گم نمی‌شوند، ولی پوشش Affinity/گزارش محصول (`oi.product_id IS NOT NULL`) برای محصولات ساده صفر است. رفع (Variation پیش‌فرض با `woo_variation_id NULL` برای محصول ساده در P2-05) تصمیم صریح لازم دارد؛ با آن، Sync بعدی اقلام را وصل می‌کند (تست ویژگی‌نگار: «leaves a simple product's line unresolved today»).
- **عمداً ساخته نشد:** `order_status_history` (منبع/`changed_at` تعریف‌نشده و بعداً قابل بازیابی نیست — ⚠ انتقال‌های وضعیت که الان دیده می‌شوند ثبت نمی‌شوند)، Event `OrderSynced`/`OrderStatusChanged`، علامت‌گذاری `customers.metrics_dirty` (نوشتن روی مدل Customers از Orders ممنوع است؛ تا P4 Run شبانه‌ی کامل این را می‌پوشاند)، Refund، SyncService/Job/Cursor/Webhook. رشته‌های `number`(۴۰)، `payment_method`(۶۰)، `sku`(۸۰)، `name_snapshot`(۲۵۰) به عرض ستون بریده می‌شوند تا سفارش رد نشود (Resolve با SKU کامل انجام می‌شود)؛ `status` بریده نمی‌شود چون هویتش را عوض می‌کند.
- تست (PostgreSQL واقعی، `FakeWooClient`، بدون HTTP): `OrderServiceTest` (۵۱)، `OrderSyncServiceTest` (۱۴)، `OrderSyncBoundaryTest` (۵، Arch)، `CustomerIdentityForWooOrderTest` (۴) = ۷۴. Mutation check (۱۴ نقض عمدی، همگی گرفته و برگردانده شدند): حذف مرحله‌ی ۱ ← ۷ تست قرمز؛ حذف مرحله‌ی ۲ ← ۴؛ جابه‌جایی ۲ و ۳ ← ۵؛ مرحله‌ی پنجم با نام ← ۱ (دقیقاً «هرگز با نام»)؛ وضعیت‌های Hardcode ← ۲؛ صفر کردن `refunded_total` ← ۲؛ گم شدن عودت اقلام ← ۱؛ الحاق به‌جای جایگزینی اقلام ← ۱۳؛ حذف تراکنش ← ۲ (دقیقاً دو تست Rollback)؛ شناسایی با `number` ← ۳؛ رد سفارش با قلم Resolve‌نشده ← ۴۶؛ Variation id صفر ← ۱؛ حذف چک ارز ← ۱؛ SKU نادیده در مرحله‌ی ۳ ← ۷. Suite: ۱۱۱۰ سبز.

## P2-07 — Refund Sync

Woo → Orders: `Sync\Services\RefundSyncService::syncOrder(int $wooOrderId): RefundSyncSummary` **همه‌ی صفحه‌های** `orders/{id}/refunds` را از `WooClient` می‌خواند، **همه** را با `RefundMapper` Map می‌کند و فقط بعد از پایان کامل خواندن و Map، مجموعه‌ی کامل را به `Orders\Services\RefundService::sync()` می‌دهد؛ پس خواندن ناقص/خراب هرگز چیزی را حذف یا عوض نمی‌کند (تست‌شده). **بدون Migration** (Schema P1-03 کافی بود)، بدون Route/Provider/Config، و P2-04/05/06 و Client دست‌نخورده. مرز ماژول مثل P2-05/06: ورودی‌های متعلق به Orders (`RefundInput`، `RefundItemInput`).

- **پیوند خط عودت به قلم سفارش (تصمیم A1، تأییدشده روی فروشگاه زنده):** Schema جدول اقلام عودت ندارد؛ وضعیت سطح قلم فقط `order_items.refunded_qty/refunded_amount` است (PRD خط ۸۰۹). `id` خط عودت شناسه‌ی خود همان خط است، نه قلم اصلی. `RefundItemDto` (P2-03) یک فیلد Nullable `originalWooItemId` گرفت که از **`meta_data[_refunded_item_id]`** همان خط می‌آید (Woo آن را رشته می‌فرستد). `verify-woo.php --refund-link=<id>` با **یک GET فقط‌خواندنی** روی سفارش #53960 نشان داد هر ۲ خط عودتش این کلید را با شناسه‌ی عددی مثبت دارند و **تنها کلید meta** همین است. ⚠ فقط عددی‌بودن بررسی شد، نه اینکه مقدار دقیقاً همان `id` یکی از اقلام سفارش است (برای آن GET دوم روی خود سفارش لازم است). Mapper: نبودن Meta → `null`؛ مقدار مخدوش (غیرعددی، صفر، آرایه، نبودن `value`)، کلید تکراری، Entry بدون `key` → `WooMappingException` بلند با مسیر دقیق. `PayloadReader::wooId()` جدید (عدد صحیح مثبت یا رشته‌ی رقمی کانونی).
- **قلم بدون پیوند هرگز حدس زده نمی‌شود** (طبق V6): با Product/Variation/SKU تطبیق داده نمی‌شود؛ قلم سفارش دست‌نخورده می‌ماند، Warning (`woo_order_id`، `woo_refund_id`، `woo_refund_item_id`، `original_woo_item_id`؛ بدون PII) و در `RefundSyncSummary::unattributedLines` شمرده می‌شود؛ **مبلغش همچنان در `refunded_total` سفارش می‌آید**. همین برای پیوندی که به قلمی اشاره می‌کند که سفارش ندارد. عودت فقط‌مبلغ (بدون خط) بی‌Warning و بدون اثر روی اقلام است.
- **فرمول‌ها (تصمیم B، دقیقاً طبق تأیید):** `refunded_total = SUM(refunds.amount)` — با `->sum('amount')` از DB **بازمحاسبه و SET** می‌شود، هرگز `+=` (Arch این را قفل می‌کند). `orders.is_fully_refunded = refunded_total > 0 AND refunded_total >= total`؛ `refunds.is_full = amount > 0 AND amount >= order.total`. هر دو در **هر Sync همان سفارش** برای همه‌ی عودت‌ها دوباره محاسبه می‌شوند (`total` می‌تواند عوض شود). مجموع چند عودت بخشی که به `total` برسد سفارش را کاملاً عودت‌شده می‌کند ولی هیچ عودتِ تک را `is_full` نمی‌کند. سفارش رایگان (`total = 0`) با عودت صفر «کاملاً عودت‌شده» نیست. این فرمول تعمیم قرارداد Seeder P1-06 (`===`) با `>=` و شرط `> 0` است.
- **قلم‌ها = SET از مجموعه‌ی کامل همان فراخوانی:** چون خط‌های عودت جایی ذخیره نمی‌شوند، `refunded_qty/amount` هر قلم از روی خط‌های **همین مجموعه‌ی کامل** جمع و SET می‌شود (قلم بدون عودت = ۰؛ مقدار کهنه‌ی قبلی بازنویسی می‌شود، نه اضافه). عودتِ حذف‌شده از Woo خطوطش را هم از جمع بیرون می‌برد. ⚠ **پیامد:** وضعیت سطح قلم فقط با Sync دوباره‌ی کل عودت‌های سفارش قابل بازسازی است، نه از DB تنها (بدون تغییر Schema). P2-06 هنگام جایگزینی اقلام این مقادیر را حفظ می‌کند و Sync بعدی عودت آن‌ها را تأیید می‌کند.
- **Mirror-delete با Audit (تصمیم C):** عودتی که در فهرست **کامل** Woo نیست حذف می‌شود، **بعد از** نوشتن `AuditLog` با `action = refund.deleted`، `auditable_type = Refund`، `auditable_id` = شناسه‌ی محلی، `before = {woo_refund_id, amount, order_id, woo_order_id}` (بدون `reason` چون متن آزاد مشتری است)، `actor = system`، `source = sync`. Audit و حذف یک تراکنش‌اند: اگر حذف/ادامه شکست بخورد Audit هم برمی‌گردد (تست‌شده). فهرست خالی همه‌ی عودت‌های محلی آن سفارش را (با Audit هرکدام) حذف می‌کند. برای اینکه ماژول Orders مجبور به `use` کردن Enum ماژول Core نشود (Arch: فقط Services/Events)، متد نازک **افزوده‌ی** `Core\Services\AuditService::recordSystem()` ساخته شد (`record()` دست‌نخورده). Event `RefundRecorded`/`RefundSynced` عمداً ساخته نشد (نه در Scope، نه مصرف‌کننده).
- **هویت و تراکنش:** هویت عودت `woo_refund_id` (UNIQUE)؛ تکرار همان مجموعه چیزی را عوض نمی‌کند و شناسه‌های محلی ثابت می‌مانند؛ مبلغ/دلیل/تاریخ عوض‌شده همان ردیف را به‌روز می‌کند. کل `sync()` یک تراکنش با قفل ردیف سفارش (شامل Soft-delete، بازیابی نمی‌شود): شکست در هر مرحله عودت‌ها، حذف، Audit، اقلام و مجموع سفارش را با هم برمی‌گرداند. سفارشِ Sync‌نشده → `RefundSyncException(order_not_found)`؛ عودتی که از قبل زیر **سفارش دیگری** است → `RefundSyncException(refund_under_another_order)` و هیچ چیز جابه‌جا/نوشته نمی‌شود.
- تست (PostgreSQL واقعی، `FakeWooClient`، بدون HTTP): ۸۶ تست جدید = `PayloadReaderTest` (+۱۷)، `RefundMapperTest` (+۱۴)، `AuditServiceRecordSystemTest` (۱)، `RefundServiceTest` (۳۷)، `RefundSyncServiceTest` (۱۲)، `RefundSyncBoundaryTest` (۵، Arch). Fixture: فقط `meta_data` افزوده به خط عودت ۷۰۰۱ (اسکریپت مقایسه با `HEAD` ثابت کرد فقط افزودنی است). Mutation check (۲۱ نقض عمدی، همگی گرفته و برگردانده شدند): هویت عودت اشتباه ← ۱۵ تست قرمز؛ `refunded_total` افزایشی (نسخه‌ی رفتاری) ← ۹؛ نادیده‌گرفتن تغییر مبلغ ← ۱؛ نادیده‌گرفتن تغییر خطوط ← ۱۰؛ `>` به‌جای `>=` ← ۵؛ حذف شرط `> 0` ← ۳؛ `is_full` با مبلغ اشتباه ← ۱؛ جمع نشدن خطوط ← ۱؛ افزودن به مقدار ذخیره‌شده‌ی قلم ← ۹؛ **حدس‌زدن قلم برای خط بی‌پیوند** ← ۳؛ سفارش اشتباه ← ۳؛ حذف Mirror-delete ← ۵؛ حذف Audit ← ۳؛ حذف تراکنش ← ۴ (Rollback‌ها)؛ **حذف روی خواندن ناقص** ← ۲؛ نادیده‌گرفتن پیوند Woo در Mapper ← ۱۳؛ پذیرفتن id صفر ← ۱. Suite: ۱۱۹۶ سبز.

## P2-08 — SyncService، Cursor و Jobها

`Sync\Services\SyncService` (نام PRD §07: `run()`) یک **Run** را مدیریت می‌کند: `run(SyncEntity, SyncMode = Incremental): ?SyncJob` ردیف `sync_jobs` را با **پنجره‌ی منجمد** می‌سازد (`cursor_to = now()` به ثانیه کف‌شده، `cursor_from = cursor_value − overlap` از `config('woo.overlap_minutes')`، Cursor اول = بدون کران پایین) و صفحه‌ی ۱ را صف می‌کند. هر `SyncPageJob` فقط `runPage(runId, page)` را صدا می‌زند: یک صفحه سفارش (`OrderSyncService`)، عودت سفارش‌های نیازمند (`RefundSyncService`)، یک خط Log، و سپس صفحه‌ی بعد یا تکمیل. **زنجیره‌ی ترتیبی** (نه Fan-out): تعداد صفحه‌ها تا پاسخ صفحه‌ی ۱ معلوم نیست و Fan-out جدول Batch لازم دارد که وجود ندارد. **بدون Migration** (Schema P1-04 کافی بود)، بدون Route/Provider/Config/Scheduler؛ P2-01…07 و Fixtureها دست‌نخورده، جز افزوده‌های کوچک زیر.

- **پنجره در `sync_jobs` منجمد می‌ماند:** هر صفحه `SyncWindow(cursor_from, cursor_to)` را از همان ردیف می‌سازد، نه از ساعت؛ تست با پیشروی ساعت بین صفحه‌ها. **Cursor فقط در یک نقطه پیش می‌رود:** `SyncService::complete()`، فقط برای Runی که هنوز `running` است، به مقدار منجمد `cursor_to` (نه `now()`). شکست/Abandon هرگز `cursor_value` را لمس نمی‌کند (Arch: دقیقاً یک انتساب `cursor_value` در کل `app/`). وضعیت‌ها فقط `running → completed | failed` (`partial` نوشته نمی‌شود؛ سرویس‌های P2-06/07 روی اولین خطا صفحه را متوقف می‌کنند).
- **کشف عودت (تصمیم R-b):** `OrderDto` یک فیلد Nullable `refundsCount` گرفت (طول آرایه‌ی `refunds` در Payload؛ **نبودن کلید = `null` = نامعلوم، نه ۰**)، با `PayloadReader::nullableObjectCount()` (P2-03 افزوده). `OrderSyncResult::$refundOrderIds` اجتماع (الف) سفارش‌های همین صفحه با `refundsCount > 0` و (ب) سفارش‌های همین صفحه که **از قبل ردیف عودت محلی دارند** است، به ترتیب صفحه و بی‌تکرار؛ (ب) از `Orders\Services\RefundService::wooOrderIdsWithRefunds()` (متد نازک **افزوده‌ی** فقط‌خواندنی، Soft-delete را هم می‌بیند) و بعد از نوشتن صفحه پرسیده می‌شود تا عودتِ حذف‌شده در Woo (Payload خالی) با Mirror-delete تصمیم C (Audit) دوباره خوانده شود. `OrderSyncService` فقط **گزارش** می‌دهد و هرگز عودت نمی‌خواند/نمی‌نویسد. عودت‌ها روی Cursor `orders` سوارند؛ Cursor جدا برای `refunds` تا `SyncRefundsJob` (بعداً) نیست. ⚠ **یافته‌ی Fixture:** هر سه سفارش ثبت‌شده کلید `refunds` را دارند ولی `[]`؛ سفارش ۵۰۰۱ حتی با اینکه Fixture عودتش دو عودت دارد `[]` است؛ Fixtureها دست نخوردند و تست‌ها Payload عودت‌دار را با `WooPayloads::set` می‌سازند. ⚠ شکل زنده‌ی `refunds` در Payload سفارش روی فروشگاه واقعی **راستی‌آزمایی نشده** (فقط Fixture‌ها؛ شکل استاندارد wc/v3 `[{id, reason, total}]` است و فقط طول آن خوانده می‌شود).
- **Run کهنه (تصمیم S-a):** شروع تا وقتی Run `running` برای همان Entity **کم‌سن‌تر از ۳۶۰۰ ثانیه** (از `started_at`) باشد رد می‌شود (`null`، بدون تغییر، بدون Job). Run `running` با سن **≥ ۳۶۰۰ ثانیه** (دقیقاً ۳۶۰۰ هم) `failed` با `error = 'abandoned'` می‌شود (+ Log هشدار روی همان Run) و بعد Run جدید شروع می‌شود؛ Cursor لمس نمی‌شود. Job یک Run Abandon‌شده اگر بیدار شود کاری نمی‌کند، و در پایان صفحه یا هنگام تکمیل **دوباره زیر قفل چک می‌شود** (نه ثبت صفحه، نه Job بعدی، نه پیشروی Cursor؛ هر دو پنجره‌ی رقابت تست جدا دارند). قفل‌ها: اول ردیف Cursor، بعد ردیف Run. ⚠ سن از `started_at` است (طبق تصمیم)، نه آخرین فعالیت: Runی که واقعاً بیش از یک ساعت طول بکشد توسط شروع بعدی Abandon می‌شود.
- **بدون افزایش:** `pages_processed`/`records_processed` (= سفارش + عودت) از **Logهای صفحه‌ی همان Run** بازمحاسبه و SET می‌شوند (هر صفحه یک‌بار، حتی اگر دوبار Sync شود)؛ `sync_cursors.consecutive_failures` از تاریخچه‌ی Runها بازمحاسبه می‌شود (Runهای `failed` همان Entity بعد از آخرین `completed`)؛ پس دوبار ثبت شکست (Catch سرویس + Hook `failed()`) عدد را دو برابر نمی‌کند. Arch: بدون `increment`/`+=`/`++`/SQL خام.
- **سیاست شکست:** هر خطا (Woo، Mapping، ارز، مشتری بی‌تلفن، تعارض SKU، خواندن عودت) Run را `failed` می‌کند، Cursor ثابت می‌ماند، `consecutive_failures` بازمحاسبه می‌شود و استثنا دوباره پرتاب می‌شود تا صف هم ثبت کند. ⚠ **پیامد شناخته‌شده:** رکوردی که قطعی خراب است (تکرار Job همان را می‌دهد) Cursor را روی همان نقطه نگه می‌دارد تا انسان رفعش کند — عمداً؛ قابل‌مشاهده‌شدن در P2-12. `SafeErrorText` تنها راه تبدیل استثنا به متن است: هر دنباله‌ی ≥ ۷ رقم (هر خط، با جداکننده‌ی تکی) `[number]` می‌شود (شماره‌تلفنِ داخل پیام هرگز به `sync_jobs.error`/`sync_logs` نمی‌رسد؛ تاریخ هم Redact می‌شود، عمداً)؛ پیام هر Log در `log()` به ۵۰۰ نویسه بریده می‌شود؛ Context فقط عدد و شناسه.
- **Jobها (بدون منطق):** `SyncEntityJob(entity, mode)` → `run()`؛ `SyncPageJob(runId, page)` → `runPage()`. هر دو `ShouldQueue + ShouldBeUnique`، صف `sync`، `tries = 1`، `uniqueFor = 600`؛ یکتایی `SyncEntityJob` برای هر Entity و `SyncPageJob` برای هر «Run + صفحه». `timeout` Entity ۶۰ و Page ۸۰ ثانیه: زیر `retry_after` صف Redis (۹۰) تا صفحه‌ی در حال اجرا دوباره Reserve نشود (تست‌شده) و زیر `uniqueFor`. `SyncPageJob::failed()` (کرش/Timeout) Runی را که هنوز `running` است `failed` می‌کند و Idempotent است. حالت `Full`/`Webhook` در `run()` `InvalidArgumentException` می‌دهد (P2-10/P2-09).
- **ماژول/مدل‌ها:** `SyncEntity` (Enum، فقط `orders`)، مدل‌های `SyncJob`/`SyncCursor`/`SyncLog` زیر `Sync\Models` (فقط `SyncService` می‌نویسد؛ Arch). `SyncService` هیچ مدل ماژول دیگر، اعتبارنامه‌ی Woo یا HTTP را نمی‌بیند.
- **عمداً ساخته نشد:** Event `OrderSynced`/`RefundSynced`/`SyncFailed`، Scheduler، `hm:sync`، Webhook، Reconciliation، UI، `records_failed` (همیشه ۰)، Sync دسته‌بندی/محصول/مشتری در این Run.
- تست (PostgreSQL واقعی، `FakeWooClient`، بدون HTTP): ۸۷ تست جدید = `PayloadReaderTest` (+۷)، `OrderMapperTest` (+۵)، `RefundServiceTest` (+۶)، `OrderSyncServiceTest` (+۹)، `SyncServiceTest` (۳۷)، `SyncJobsTest` (۱۵)، `SyncRunBoundaryTest` (۸، Arch). Mutation check (۳۹ نقض عمدی، همگی گرفته و برگردانده شدند): پیشروی Cursor روی شکست ← ۶ تست قرمز؛ روی `now()` ← ۱؛ در شروع Run ← ۱۴؛ تکمیل بعد از صفحه‌ی ۱ ← ۸؛ حذف Overlap ← ۵؛ انجماد دوباره‌ی پنجره در هر صفحه ← ۱؛ مرز ۳۶۰۰ ← ۱؛ حذف رد شروع هم‌زمان ← ۴؛ Abandon‌نکردن ← ۴؛ شمارنده‌ی افزایشی ← ۲؛ بازنشدن `consecutive_failures` ← ۱؛ **حذف چک وضعیت در `complete()` ← ۱ و در `recordPage()` ← ۱** (اول هر دو زنده ماندند چون هر Guard دیگری را می‌پوشاند؛ دو تست پنجره‌ی رقابت اضافه شد)؛ ندیدن عودت (الف/ب) ← ۶/۳؛ Redact نشدن تلفن ← ۱؛ برش پیام ۵۰۰ ← ۱ (اول زنده ماند چون تست `QueryException` را هم `RuntimeException` می‌پذیرفت؛ تست سخت‌تر شد و کف Cap در `log()` یکتا شد)؛ یکتایی Page فقط روی Run ← ۷؛ `timeout` بالای `retry_after` ← ۱؛ Hook `failed()` خالی ← ۱. Suite: ۱۲۸۳ سبز؛ Arch ۴۸؛ PHPStan ۰ خطا.

## P2-09 — Webhook، HMAC و Dedupe

`POST /webhooks/woo` (`routes/webhooks.php`، نام `webhooks.woo`) طبق PRD §10: **تأیید اصالت ← مسیریابی Topic ← ثبت شناسه‌ی تحویل و صف‌کردن ← فوراً ۲۰۰؛ هرگز Inline پردازش نمی‌شود.** مسیر با `then:` در `bootstrap/app.php` **بیرون از گروه `web`** ثبت می‌شود (بدون Session/Cookie/CSRF؛ سرور Woo هیچ‌کدام را نمی‌فرستد). زنجیره: `WooWebhookRequest` (FormRequest؛ `authorize()` ← `Sync\Services\WooWebhookVerifier`) ← `WooWebhookController` (فقط یک فراخوانی) ← `Sync\Services\WooWebhookService` ← `SyncEntityJob`. `routes/api.php` وجود نداشت؛ فایل مستقل ساخته شد.

- **اصالت (تصمیم ۱-ب):** سه بررسی به‌ترتیب: (۱) `woo.webhook_secret` تنظیم باشد — **خالی = رد همه** (Fail-closed؛ کلید HMAC خالی جعل‌پذیر است)؛ (۲) IP مبدأ در `woo.webhook_allowed_ips` (لیست جداشده با کاما یا آرایه؛ IP، CIDR و IPv6 با `Symfony IpUtils`) — **لیست خالی = بررسی IP خاموش (فقط IP؛ امضا همیشه اجباری)**، لیستِ غیرخالی که فقط مدخل نامعتبر دارد همه را رد می‌کند؛ (۳) `X-WC-Webhook-Signature` = **`base64(HMAC-SHA256(بدنه‌ی خام، secret))`** با `hash_equals`؛ روی بایت‌های خام (`getContent()`) محاسبه می‌شود، پس بدنه‌ی دوباره‌کدشده تأیید نمی‌شود. هر شکستی همان ۴۰۱ با بدنه‌ی یکسان `{"message":"Unauthorized"}` است؛ **دلیل فقط در Log** می‌آید (`reason` + `ip`؛ هرگز بدنه/امضا/secret). ⚠ **لیست IP فعلاً خالی است تا IPهای فروشگاه تأیید شود** (`WOO_WEBHOOK_ALLOWED_IPS` در `.env.example` خالی). پشت Reverse Proxy: `config/trustedproxy.php` ← `TRUSTED_PROXIES` (کاما‌جدا؛ خالی = هیچ‌کس) که `TrustProxies` خود Laravel در هر درخواست از `config('trustedproxy.proxies')` می‌خواند؛ `X-Forwarded-For` فقط از Proxy معتمد پذیرفته می‌شود (تست‌شده). `WOO_WEBHOOK_SECRET`، `WOO_WEBHOOK_ALLOWED_IPS`، `TRUSTED_PROXIES` فقط از Config خوانده می‌شوند (Arch: بدون `env()` در کد Webhook).
- **⚠ اصلاح سرآیندها نسبت به متن اولیه‌ی تسک (تصمیم ۲؛ از دانش هسته‌ی WooCommerce، راستی‌آزمایی زنده نشده):** Woo `X-WC-Webhook-Topic: order.created` می‌فرستد، نه `woocommerce.order.created` ← **هر دو املا** به یک case از `WooWebhookTopic` می‌رسند و فقط **یک** پیشوند فهمیده می‌شود (مطابقت دقیق: حروف بزرگ/کوچک، فاصله). `X-WC-Webhook-Delivery-ID` یک Hash ۳۲‌نویسه‌ای است، نه عدد ← ستون **`varchar(64) UNIQUE`** (عدد، Hex و UUID هر سه جا می‌شوند)؛ الگو `^[A-Za-z0-9._-]{1,64}$`. Woo برای **هر تلاش تحویل** شناسه‌ی جدید می‌سازد، پس Dedupe فقط **بازپخش دقیق** را می‌گیرد؛ هجوم چند Webhook را یکتایی `SyncEntityJob` (به‌ازای Entity) به یک Job جمع می‌کند (تست‌شده).
- **Topicها:** فقط `order.created|updated|deleted` (Enum `WooWebhookTopic`؛ بدون عودت). Topic ناشناخته، یا نبودن سرآیند (مثلاً Ping) ← ۲۰۰ `{"status":"ignored"}`، **چیزی ثبت یا صف نمی‌شود**. تحویل امضاشده‌ی بدون شناسه‌ی قابل‌استفاده ← ۲۰۰ `ignored` + Warning با فقط `topic` (بدون بدنه). پاسخ‌ها: `accepted | duplicate | ignored` (Enum `WebhookOutcome`).
- **جدول `woo_webhook_deliveries`** (Migration جدید؛ `id`، `topic varchar(30)` با CHECK روی سه Topic (مقدار **کانونی**، نه سرآیند خام)، `woo_delivery_id varchar(64) UNIQUE`، `received_at timestamptz`). یکتایی DB خودِ Dedupe است، پس با رقابت دو تحویل یکسان هم برقرار می‌ماند. حذف/Prune ندارد (`PruneLogsJob` بعداً).
- **اتمی با Dispatch:** `insertOrIgnore` روی UNIQUE و صف‌کردن **یک** `SyncEntityJob` (Orders، Incremental) روی صف **`critical`** (طبق PRD §10؛ صف پیش‌فرض Job `sync` است و فقط همین‌جا عوض می‌شود) در یک تراکنش. شناسه‌ی تکراری ← ۲۰۰ `duplicate` بدون Dispatch. شکست Dispatch ← Rollback ثبت و ۵۰۰ (Woo دوباره تلاش می‌کند و به‌عنوان تکراری بلعیده نمی‌شود). ⚠ `dispatch()` قفل یکتایی Job را پیش از Push می‌گیرد و با شکست Push آن قفل تا `uniqueFor` (۶۰۰ ثانیه) می‌ماند و تلاش مجدد Woo را بی‌صدا می‌بلعید؛ سرویس قفل را قبل از پرتاب دوباره **آزاد می‌کند** (`UniqueLock::release`؛ تست‌شده).
- **بدنه هرگز خوانده/Log نمی‌شود:** فقط سه سرآیند (`Signature/Topic/Delivery-ID`) و `getContent()` (فقط برای HMAC) استفاده می‌شود؛ `validationData()` عمداً `[]` است؛ Arch: بدون `json_decode`/`->input`/`->all`/Log از درخواست. یافته: Laravel خودش بدنه‌ی JSON را هنگام ساخت Request (`Request::createFromBase`، پیش از هر Middleware) در حافظه Decode می‌کند و این از کد برنامه خاموش‌شدنی نیست؛ آن Bag هیچ‌جا خوانده نمی‌شود (بدنه‌ی غیر JSON/خالی هم ۲۰۰ می‌گیرد). ⚠ مسیر طراحی: یک‌بار به Middleware رفتم به این گمان که FormRequest بدنه‌ی احراز‌نشده را Decode می‌کند؛ Trace نشان داد Decode پیش‌تر در `createFromBase` رخ داده و FormRequest فقط همان را دوباره‌استفاده می‌کند؛ به تصمیم شما (FormRequest) برگشتم.
- **پیامدهای پذیرفته‌شده:** تا P2-10 هیچ Polling/Scheduler نیست؛ Run تازه (< ۱ ساعت، قاعده‌ی S-a در P2-08) Run ناشی از Webhook را نمی‌پذیرد؛ `order.deleted` همان Run افزایشی را شروع می‌کند و **چیزی را Mirror-delete نمی‌کند** (Woo سفارش حذف‌شده را فهرست نمی‌کند). Ping اولیه‌ی Woo هنگام ساخت Webhook ممکن است امضا نداشته باشد و ۴۰۱ بگیرد (پذیرفته‌شده).
- **عمداً ساخته نشد:** Webhook عودت، ثبت خودکار Webhook در Woo، `hm:sync`، Reconciliation، UI، Event، Prune جدول. فایل‌های دست‌نخورده: P2-01…08، Fixtureها، `web.php`.
- تست (PostgreSQL واقعی، بدون HTTP بیرونی): ۱۳۸ تست جدید = `WooWebhookTopicTest` (۲۷)، `WebhookSchemaTest` (۱۲)، `WooWebhookVerifierTest` (۲۷)، `WooWebhookTest` (۵۸، HTTP کامل با سرآیندهای واقعی Woo)، `WebhookBoundaryTest` (۱۴، Arch). Mutation check (۲۸ نقض عمدی، همگی گرفته و برگردانده شدند): مقایسه با `===` ← ۱ تست (فقط Arch؛ رفتارش یکسان است)؛ Hex به‌جای Base64 ← ۵۷؛ رد نشدن Secret خالی ← ۵؛ خاموش‌کردن IP ← ۱۰؛ لیست خالی = رد همه ← ۵۴؛ IP ناشناخته ← ۱؛ امضا هرگز چک نشود ← ۱۸؛ جابه‌جایی کلید/پیام ← ۵۴؛ بدون Dedupe ← ۳؛ بدون تراکنش ← ۲۷؛ صف `sync` ← ۸؛ نماندن آزادسازی قفل ← ۱؛ Job در حالت Full ← ۷؛ Topic ناشناخته پذیرفته ← ۹؛ ۴۰۳ به‌جای ۴۰۱ ← ۱۲؛ نشت بدنه در Log ← ۳؛ HMAC روی بدنه‌ی دوباره‌کدشده ← ۱۱؛ مسیر داخل گروه `web` ← ۱؛ Migration بدون UNIQUE ← ۵ و بدون CHECK ← ۴ و با شناسه‌ی عددی ← ۳۲. Suite: ۱۴۲۱ سبز.

## P2-10 — `hm:sync` و Full Sync قطعه‌قطعه‌ی قابل‌ادامه

`php artisan hm:sync {--entity=orders} {--full}` (`app/Console/Commands/SyncCommand.php`) **یک** `SyncEntityJob` را روی صف `sync` (نه `critical`؛ آن فقط Webhook است) می‌فرستد و بی‌انتظار خارج می‌شود؛ خروجی فقط یک خط: `Dispatched sync for orders (incremental|full)` (بدون Cursor/پنجره/Payload/Secret). `--entity` با Enum `SyncEntity` اعتبارسنجی می‌شود؛ ناشناخته ← پیام ثابت `Unknown entity. Supported: orders.` (بدون تکرار ورودی)، کد خروج ۱ و هیچ Jobی نمی‌رود. فرمان هیچ منطق/جدول/Config ندارد (Arch). **Scheduler:** Laravel ۱۳ فایل `Kernel.php` ندارد؛ زمان‌بندی در `routes/console.php` است: `Schedule::command('hm:sync', ['--entity' => 'orders'])->everyFifteenMinutes()->timezone('Asia/Tehran')` — همان Poll افزایشی PRD §22؛ بدون `withoutOverlapping` (فرمان فوراً خارج می‌شود؛ قاعده‌ی S-a در P2-08 و یکتایی Job جلوی هم‌پوشانی را می‌گیرند) و تنها Task زمان‌بندی‌شده. Cron سرور: `* * * * * php artisan schedule:run`. **بدون Migration، بدون `config/hm.php`** (کلیدهای Sync همیشه در `config/woo.php` بوده‌اند).

- **Epoch (تصمیم ۱):** `woo.sync_epoch = JalaliDate::toGregorian(1403, 7, 1)` (UTC، رشته‌ی ISO؛ **تاریخی Hardcode نشده**) = ۱ مهر ۱۴۰۳ = `2024-09-22 00:00 Asia/Tehran` = `2024-09-21T20:30:00Z`. ⚠ متن اولیه‌ی تسک `2024-09-23` نوشته بود که طبق `JalaliDate` (و معادل PL/pgSQL آن) **۲ مهر** است و روز اول ماهی را که Gate 1 برایش تطبیق صفر‌اختلاف می‌خواهد جا می‌انداخت؛ با تأیید صریح، ۱ مهر مبنا شد. Epoch مثل یک Cursor حساب می‌شود: پنجره‌ی Full از `epoch − overlap` (۲۰:۲۰Z) شروع می‌شود تا مرز دقیق (Woo `modified_after` را ممکن است انحصاری بگیرد) گم نشود.
- **Full Run دامنه‌ی Run دارد (تصمیم ۳):** `SyncService::run(Orders, Full)` پنجره را از Epoch می‌سازد (نه از Cursor ذخیره‌شده) و **هنگام شروع `sync_cursors` را نمی‌نویسد**؛ Cursor فقط با `completed` جابه‌جا می‌شود (همان قاعده‌ی P2-08 / CLAUDE.md §5). Full ناموفق (حتی بعد از چند صفحه) Cursor را دست‌نخورده می‌گذارد. Full کامل‌شده Cursor را روی `cursor_to` می‌گذارد؛ **قطعه‌ی اول یک Full می‌تواند Cursor را به عقب ببرد** (به مرز آن قطعه) — مقصود همین بازخوانی است. Arch: هنوز **دقیقاً یک** انتساب `cursor_value` در کل `app/` (`SyncService::complete`). حالت `Webhook` همچنان رد می‌شود (Webhook Run افزایشی می‌سازد)؛ تست P2-08 که `Full` را رد می‌دانست عمداً به‌روز شد.
- **قطعه‌بندی صفحه‌ای (تصمیم ۲):** کلید یکتای `woo.sync_max_pages_per_run` (پیش‌فرض ۵۰۰؛ آستانه = اندازه‌ی قطعه). **هر** Run (Full یا افزایشی) بعد از این‌همه صفحه اگر Woo باز هم صفحه داشت (`hasMore`) **`completed` می‌شود** و Cursor را روی **زمان `modified` آخرین سفارش پردازش‌شده** می‌گذارد؛ Run بعدی (Poll زمان‌بندی‌شده) از `مرز − overlap` ادامه می‌دهد. نیازی به شمارش صفحه‌ها از قبل نیست («بیش از ۵۰۰» یعنی صفحه‌ی ۵۰۰ هنوز `hasMore` دارد)؛ Run دقیقاً با ۵۰۰ صفحه قطعه‌ای نیست. بدون شرط «Cursor روی Epoch» (که پس از قطعه‌ی اول باطل می‌شد و ادامه‌پذیری را می‌شکست). مقدار نامعتبر (۰، منفی، غیرعددی) ← ۵۰۰. مرز = بیشینه‌ی `modified` همان صفحه (`OrderSyncResult::$lastModifiedAt`، افزوده‌ی P2-10؛ `null` برای صفحه‌ی بی‌سفارش). مرز با هر مرحله جلو می‌رود چون فهرست به ترتیب `modified` صعودی است. ادامه‌ی Catch-up تا رسیدن به Run نهایی با همان Poll ۱۵‌دقیقه‌ای انجام می‌شود (مثلاً ۱۱ سفارش، ۲ صفحه‌ی ۲‌تایی در هر Run ← ۴ Run؛ Cursorها ۰۷:۰۰، ۰۸:۰۰، ۰۹:۰۰ و در پایان `cursor_to`).
- **Stall Guard (تصمیم ۴):** قطعه‌ای که پنجره‌ی بعدی‌اش (`مرز − overlap`) **اکیداً دیرتر** از شروع پنجره‌ی خودش نباشد (بیش از یک قطعه سفارش در یک بازه‌ی overlap تغییر کرده؛ Import/ویرایش گروهی) همان سفارش‌ها را تا ابد می‌خواند؛ پس Run با `SyncStalledException` (پیام ثابت، فقط نام دو تنظیم `woo.sync_max_pages_per_run` و `woo.overlap_minutes`، بدون زمان/سفارش) `failed` می‌شود، Cursor ثابت می‌ماند و `consecutive_failures` بازمحاسبه می‌شود. برابری = Stall؛ یک ثانیه جلوتر = پیشرفت. Sync اولِ بی‌کران‌پایین (بدون `cursor_from`) هرگز Stall نمی‌شود.
- **تست با شبیه‌ساز Woo:** Fixtureهای ثبت‌شده پنجره را نادیده می‌گیرند؛ `tests/Support/WooOrdersSimulator` (بدون HTTP) `modified_after/before` را **انحصاری** (سخت‌ترین برداشت؛ Overlap است که مرز را نجات می‌دهد) و صفحه‌بندی را اعمال می‌کند. تست‌های End-to-End `hm:sync` را پشت‌سرهم اجرا می‌کنند (صف `sync` = Inline): همه‌ی ۱۱ سفارش نشستند، چیزی گم نشد، Runهای بعدی Idempotent‌اند، Run آخر روی `cursor_to` بسته می‌شود.
- **پیامدهای پذیرفته‌شده و واقعیت‌ها:** (۱) `--full` وقتی Runی کم‌سن‌تر از ۱ ساعت هست توسط S-a بی‌صدا رد می‌شود؛ `SyncEntityJob` هم به‌ازای Entity یکتاست، پس Poll صف‌شده یک `--full` را می‌بلعد — فرمان Dispatch می‌کند و خارج می‌شود و نمی‌تواند بفهمد. (۲) در Catch-up، Webhook فقط Run از Cursor فعلی (عقب‌مانده) می‌سازد؛ سفارش‌های تازه تا رسیدن Catch-up منتظر می‌مانند. (۳) V8: کل تاریخچه ۲۴٬۴۲۱ سفارش = ۴۸۹ صفحه‌ی ۵۰‌تایی، **زیر آستانه‌ی ۵۰۰**؛ پس در اندازه‌ی فعلی این فروشگاه قطعه‌بندی حتی برای Full کامل فعال نمی‌شود (سپر ایمنی است؛ تست‌ها با آستانه‌ی ۲ کار می‌کنند). (۴) شکل زنده‌ی `hm:sync` روی Woo واقعی اجرا نشد (هیچ GET زنده‌ای انجام نشد؛ صف محلی Redis است).
- **تغییرات کوچک در کارهای قبلی:** `OrderSyncResult` آرگومان پنجم (`lastModifiedAt`، الزامی؛ فقط `OrderSyncService` می‌سازد)؛ Guard معماری P2-09 «هیچ Command نیست» به «هیچ Command مرتبط با Webhook نیست» تغییر کرد چون `hm:sync` قانونی است.
- **عمداً ساخته نشد:** Reconciliation، Health Check Job، `hm:sync all` و Entityهای کاتالوگ/مشتری، تغییر در گیرنده‌ی Webhook (P2-09)، UI، Gate 1.
- تست (PostgreSQL واقعی، بدون HTTP): ۵۱ تست خالص جدید = `SyncChunkingTest` (۲۴)، `SyncCommandTest` (۱۷، فرمان + Schedule)، `SyncCommandBoundaryTest` (۸، Arch)، `OrderSyncServiceTest` (+۳)، `SyncServiceTest` (−۱: دو حالت Dataset رد‌شدنِ Full/Webhook به یک تست Webhook تبدیل شد). Mutation check (۲۵ نقض عمدی، همگی گرفته و برگردانده شدند): نادیده‌گرفتن Epoch ← ۶ تست قرمز؛ نوشتن Cursor هنگام شروع Full ← ۵؛ Epoch بی‌Overlap ← ۳؛ تاریخ `2024-09-23` در کد ← ۴؛ نادیده‌گرفتن سقف صفحه ← ۱۰؛ یکی‌کم/زیاد در سقف ← ۱۰؛ Cursor روی `cursor_to` به‌جای مرز ← ۹؛ مرز = کمترین `modified` ← ۱۱؛ کلمپ سقف نامعتبر روی ۱ ← ۴؛ حذف Stall Guard ← ۲؛ برابری = پیشرفت ← ۱؛ Guard بی‌Overlap ← ۲؛ Stall بدون `failed` ← ۲؛ پذیرفتن حالت Webhook ← ۱؛ Full ذخیره‌شده به‌صورت افزایشی ← ۲؛ فرمان همیشه افزایشی ← ۳؛ خروج ۰ برای Entity ناشناخته ← ۸؛ Poll هر ۵ دقیقه ← ۲؛ بی‌Timezone ← ۲؛ `withoutOverlapping` ← ۲؛ پیش‌فرض ۵۰ ← ۲؛ Epoch = ۲ مهر ← ۴؛ گزارش‌ندادن مرز ← ۱۲. Suite: ۱۴۷۲ سبز؛ Arch ۷۰؛ PHPStan ۰ خطا.

## P2-11 — Reconciliation (ReconciliationService، Job، گزارش ماهانه)

`Sync\Services\ReconciliationService::compare(ماه‌جلالی)` (نام PRD §07) Woo را با جدول محلی `orders` مقایسه می‌کند؛ خالص است و چیزی نمی‌نویسد. `reconcile()` مقایسه + ذخیره است (یک ردیف برای هر ماه، با هر اجرا جایگزین می‌شود). `hm:reconcile {--month=} {--all}` و `ReconcileMonthJob` روی آن سوارند. **بدون تغییر در Webhook (P2-09) و `hm:sync` (P2-10)، بدون UI، بدون Metrics.**

- **پنجره‌ی ماه (نیمه‌باز):** `[روز اول ۰۰:۰۰ تهران، روز اول ماه بعد ۰۰:۰۰ تهران)` با `JalaliDate::toGregorian` (مثلاً `1403-07` = `[2024-09-21T20:30Z, 2024-10-21T20:30Z)`؛ ماه اسفند ۱۴۰۳ ۳۰ روز است و به نوروز ۱۴۰۴ می‌رسد؛ تست‌شده). Woo: `after = شروع − ۱ ثانیه`، `before = پایان`، `dates_are_gmt=true` (هر دو مرز Woo انحصاری‌اند، پس با `ordered_at >= شروع AND < پایان` محلی یکی می‌شوند)؛ فقط `orders` و فقط چهار فیلد `id,status,total,currency` (`_fields`؛ کم‌ترین داده‌ی مشتری) — **هرگز Endpoint گزارش‌ها** (نیاز به دسترسی بالاتر). محلی بر اساس `ordered_at` (تاریخ **ایجاد**). ⚠ رفتار `after/before` و `_fields` روی Woo واقعی این فروشگاه آزموده نشده (فقط شبیه‌ساز).
- **تصمیم ۲ — مبنای درآمد:** تعداد روی **همه‌ی وضعیت‌ها** در هر دو طرف (`X-WP-Total` صفحه‌ی اول ↔ `COUNT(*)` غیر Soft-delete)؛ درآمد روی **وضعیت‌های realized** در هر دو طرف: Woo = جمع `total` سفارش‌هایی که `OrderStatusMapper::isRealized` (یعنی `config('woo.realized_statuses')`) می‌داند، از همان صفحه‌ها (بدون درخواست اضافه)؛ محلی = `SUM(total) WHERE is_realized`. وضعیتی در کد نیست. ⚠ اگر `realized_statuses` بعد از Sync عوض شود، سمت Woo از Config جدید و سمت محلی از `is_realized` ذخیره‌شده می‌خواند ← قرمز (این عمداً نشانه است؛ تست‌شده). Woo باید `X-WP-Total` بدهد و با تعداد سفارش‌های واقعاً خوانده‌شده برابر باشد (وگرنه ماه `failed` است؛ حتی برای ماه خالی). ارز غیر از `woo.currency` رد می‌شود.
- **فرمول (تصمیم ۴):** `count_diff = woo − local`، `revenue_diff = woo − local` (علامت‌دار)، `diff_percent` = **درصد مطلق**، ۴ رقم اعشار (`0.05` = ۰٫۰۵٪؛ `numeric(7,4)`)، با تقسیم بلند **صحیح** (بدون Float؛ `RevenueVariance`)، گرد‌شده به نزدیک‌ترین، سقف `999.9999`. **سبز = `count_diff = 0` و `abs(diff) × 100 < woo_revenue`** (اکیداً زیر ۱٪؛ دقیقاً ۱٪ قرمز است؛ از درصدِ گردشده استخراج نمی‌شود). درآمد Woo صفر: هر دو صفر ← سبز؛ محلی > ۰ ← قرمز با سقف. آستانه ۱٪ (CLAUDE.md §11 بر `diff_percent < 0.01` در PRD مقدم است؛ PRD ابهام واحد دارد). مجموع‌های بزرگ‌تر از حدی که حساب صحیح تضمین می‌کند `OverflowException` می‌دهند، نه تقریب.
- **جدول (تصمیم ۱ — Migration افزایشی):** `reconciliation_reports` از P1-04 (PRD §09) دست نخورده و ستون‌های PRD همان نام‌ها را دارند؛ Migration جدید افزود: `jalali_month varchar(7) UNIQUE`، `status` (`green|red|failed`، CHECK)، `error_message`، `reconciled_at`، `updated_at`؛ و هفت ستون اندازه‌گیری **Nullable** شد تا ماه `failed` ذخیره شود. دو CHECK اضافه: ستون‌های اندازه‌گیری فقط در `failed` می‌توانند خالی باشند و `is_acceptable ≡ (status = green)` (شکاف هرگز شبیه نتیجه نمی‌شود). `down()` ردیف‌های `failed` را حذف می‌کند (با NOT NULL های PRD سازگار نیستند). `period_start/period_end` = اولین/آخرین روز ماه به تاریخ تهران؛ `details` = پنجره‌ی UTC و `realized_statuses` استفاده‌شده. ⚠ **تست‌های P1-04 ناچار عوض شدند:** `SyncSchemaTest`ِ «دقیقاً مطابق PRD» با هر Migration افزایشی می‌شکند (ستون اضافه + Nullable)؛ فقط هفت پرچم Nullable و ستون‌های افزوده در همان تست اصلاح شد و دو فیلد جدید به ردیف نمونه اضافه شد؛ ادعاهای دیگر دست‌نخورده‌اند. ۲۹ تست افزایشی به همان فایل اضافه شد.
- **شکست:** `reconcile()` هر خطا (Woo، ارز، مبلغ، `X-WP-Total`) را به‌صورت ردیف `failed` با متن **پاک‌سازی‌شده** (`SafeErrorText`؛ رقم‌های ≥۷ حذف می‌شوند) و اندازه‌گیری‌های خالی ثبت و دوباره پرتاب می‌کند؛ اجرای موفق بعدی همان ردیف را سبز/قرمز می‌کند و خطا را پاک می‌کند. ماهی که اصلاً قابل Reconcile نیست (قالب نادرست، قبل از مهر ۱۴۰۳، ناتمام) ردیفی نمی‌نویسد.
- **ماه‌ها:** «آخرین ماه کامل» = ماه قبل از ماه جلالی جاری (به وقت تهران)؛ اولین ماه = ماه `woo.sync_epoch` (بدون رشته‌ی ماه در کد؛ Arch). فقط ماه‌های کامل از ۱۴۰۳-۰۷ قابل Reconcile‌اند (ماه در جریان هنوز تغییر می‌کند و هرگز قضاوت نمی‌شود). `hm:reconcile --month` ماه نامعتبر/قبل‌ازمهر/ناتمام و هم‌زمانی `--month` و `--all` ← خروج ۱ با پیام ثابت (ورودی تکرار نمی‌شود) و هیچ کاری نمی‌کند؛ بدون Flag ← آخرین ماه کامل؛ `--all` ← یک `ReconcileMonthJob` برای هر ماه؛ خروجی فقط یک خط از شمار و درصد. ماه **قرمز نتیجه است، نه خطا** (خروج ۰)؛ شکست ← خروج ۱. `ReconcileMonthJob`: صف `sync`، `tries=1`، یکتا به‌ازای ماه (`uniqueFor 600`)، `timeout 80` < `retry_after` (۹۰)، `failed()` شبکه‌ی ایمنی.
- **⚠ انحراف لازم از تصمیم ۴ (Scheduler):** `Schedule::command('hm:reconcile', ['--all' => true])` توسط Laravel به `--all='1'` کامپایل می‌شود و چون `--all` Flag بدون مقدار است، Symfony آن را رد می‌کند (`The "--all" option does not accept a value`) ← **اجرای شبانه هر شب بی‌صدا شکست می‌خورد** (با اجرای `schedule:list` و Bind دستی به Definition فرمان تأیید شد؛ چیزی اجرا نشد). به‌جایش `Schedule::command('hm:reconcile --all')->dailyAt('02:00')->timezone('Asia/Tehran')` ثبت شد و تست جدید خط فرمانِ زمان‌بندی‌شده را روی Definition واقعی فرمان Bind می‌کند (برای `hm:sync` هم افزوده شد). قبلاً «شامل `--all`» بودن کافی می‌نمود؛ آن تست ضعیف بود.
- **محلی:** Sync مدل Orders را نمی‌بیند، پس متد افزوده‌ی فقط‌خواندنیِ `OrderService::totalsInWindow()` (VO `OrderWindowTotals`؛ دو Aggregate، بدون بارگذاری ردیف) ساخته شد.
- **GATE 1 (قابل بررسی، خودکار عبور نمی‌کند):** `ReconciliationService::gateOne()` فقط وقتی `passed` است که **هر** ماه از ۱۴۰۳-۰۷ تا آخرین ماه کامل ردیف `green` داشته باشد؛ ماه بدون ردیف، `red`، `failed` و نبودن ماهِ کامل هر کدام دروازه را باز نگه می‌دارد؛ ردیف‌های بیرون از بازه نادیده‌اند؛ خروجی هر ماه را با اختلاف تعداد و درصد نشان می‌دهد (`failingMonths()`، `missingMonths()`).
- **⚠ وضعیت GATE 1 — باز (OPEN)، ۲۰ شهریور ۱۴۰۵ / ۲۰۲۶-۰۹-۲۰:** اسکیل `gate-check` اجرا شد، **بدون درخواست زنده به Woo و بدون Reset یا Migrate روی DB توسعه** (طبق تصمیم ۳). شواهد: بازه‌ی دروازه `1403-07 .. 1405-05` = **۲۳ ماه**؛ DB توسعه (`heymode`): `reconciliation_reports` = ۰ ردیف، `sync_jobs` = ۰، `sync_cursors` = ۰، `orders` = ۱۱۷ (داده‌ی Demo، نه Sync از فروشگاه)؛ Migrationهای P2-09 و P2-11 روی آن هنوز Pending‌اند. **۰ از ۲۳ ماه سبز.** منطق دروازه با Woo جعلی تست شد (۵ تست) ولی این «شاهد» نیست. بستن دروازه (مجوز جداگانه لازم است): Migrate روی DB تمیز (Demo را آلوده‌ی سمت محلی می‌کند)، صف Worker (Redis)، `hm:sync --full` زنده (V8: ≈۴۸۹ صفحه، زیر آستانه‌ی ۵۰۰ ← احتمالاً یک Run)، سپس `hm:reconcile --all`، و خواندن `gateOne()`. **P2-12 و هر چیز بعد از Sprint 2 تا عبور این دروازه شروع نمی‌شود.**
- **مشاهده‌ی تلفن (خواسته‌ی تسک):** اثر «سفارش بدون تلفن قابل‌استفاده» بر Reconciliation را نمی‌توان بدون داده‌ی زنده دید و چیزی یافت نشد؛ اگر Run زنده ماهی را به‌خاطر اختلاف تعداد قرمز کند، اول همین را بررسی کنید (`OrderCustomerUnresolvedException` سفارش را ذخیره نمی‌کند ← `count_diff > 0`).
- **ریسک‌ها:** هر ماه یک Job با `timeout 80` است؛ ماهی با بیش از حدود ۶۰ صفحه‌ی ۵۰‌تایی (≈۳٬۰۰۰ سفارش) ممکن است زیر محدودیت ۹۰ درخواست/دقیقه از `timeout` بگذرد و `failed` شود (ایمن، ولی دروازه را نمی‌بندد؛ راه‌حل: افزایش `REDIS_QUEUE_RETRY_AFTER` و `timeout` یا تقسیم Job به صفحه). سفارشی که در Woo Trash شده ولی محلی مانده (Mirror-delete سفارش وجود ندارد) `count_diff` می‌دهد؛ این سیگنال واقعی داده است. `X-WP-Total` و `after/before` را فقط اجرای زنده تأیید می‌کند.
- تست (PostgreSQL واقعی، شبیه‌ساز Woo، بدون HTTP): ۱۶۰ تست خالص جدید = `RevenueVarianceTest` (۱۶)، `ReconciliationMonthsTest` (۳۲)، `ReconciliationServiceTest` (۴۶)، `ReconcileMonthJobTest` (۹)، `ReconcileCommandTest` (۱۷، فرمان + Schedule)، `ReconciliationBoundaryTest` (۱۱، Arch)، `SyncSchemaTest` (+۲۹). Guardهای دامنه‌ی قبلی که «Reconciliation/دو Job/یک Command/یک Schedule» را ممنوع یا شمارش می‌کردند عمداً به‌روز شدند (P2-08/09/10). Mutation check (۴۸ نقض عمدی، همگی گرفته و برگردانده شدند؛ دو مورد اول زنده ماندند: نبودن `X-WP-Total` در ماه خالی که تست تازه‌ای گرفت، و یک Pre-check زائد که حذف شد): پنجره‌ی Woo (`after`/`before`/GMT) ← ۱۵/۷/۳۷؛ درآمد همه‌ی وضعیت‌ها ← ۱۲؛ نادیده‌گرفتن تعداد ← ۳؛ نادیده‌گرفتن درآمد ← ۴؛ `<=` به‌جای `<` (۱٪ دقیق) ← ۳؛ گرد نکردن ← ۲؛ سال بعد از اسفند ← ۳؛ ماه جاری کامل حساب شود ← ۲۳؛ ماه ناتمام پذیرفته ← ۱۱؛ Softdelete محلی ← ۱؛ درآمد محلی غیر-realized ← ۱۰؛ Schedule ۰۳:۰۰ ← ۲؛ فرم آرایه‌ای Schedule ← ۲؛ Migration بدون UNIQUE/CHECK/Nullable ← ۱/۱۵/۱۱. Suite: ۱۶۳۲ سبز؛ Arch ۸۱؛ PHPStan ۰ خطا.

## P2-12 — صفحه‌های سیستم (Health، Sync Logs، Identity Conflicts)

- **P2-12: System pages (Health, Sync Logs, Identity Conflicts) — read-only Inertia UI; no new DB columns; Gate 1 still OPEN (no live sync run authorized yet).** سه صفحه‌ی فقط‌خواندنی: `GET /system/health`، `GET /system/sync-logs`، `GET /system/identity-conflicts` (`routes/system.php`، فقط `GET`). بدون Migration، بدون ستون تازه، بدون فرمان یا Job تازه، بدون تغییر رفتار Sync/Webhook/Reconciliation.
- **دسترسی (تصمیم کاربر، پیشنهاد اول):** Health و Sync Logs پشت `auth` + `permission:system,view`؛ Identity Conflicts پشت `auth` + `permission:identity,review` (مجوز موجود). `system.view` **مجوز تازه در کاتالوگ `PermissionSeeder`** است (داده، نه Schema)؛ `RoleSeeder` دست نخورد و طبق قاعده‌ی خودش آن را به Owner، Manager و Analyst می‌دهد (Support و Viewer نه؛ `identity.review` فقط Owner و Manager). دلیل: متن تسک «فقط `auth`» را می‌گفت ولی PRD §20 و CLAUDE.md §6 «بسته به‌صورت پیش‌فرض» و Viewer/Support را از این صفحه‌ها جدا می‌کنند. ⚠ **DBهای موجود (dev/prod) باید `PermissionSeeder` و `RoleSeeder` را دوباره اجرا کنند** تا `system.view` ساخته و داده شود؛ در این جلسه اجرا نشد. Deny صریح روی `system.view` تست شد.
- **⚠ انحراف‌های لازم از متن تسک (هرکدام دلیل دارد):** (۱) **خواندن DB در Service ماژول است، نه Controller**: CLAUDE.md §1 و تست Arch `app/Http/Controllers` هر `DB::`/Model را در Controller ممنوع می‌کنند و تست سست نشد؛ هر Controller یک Service صدا می‌زند و Inertia برمی‌گرداند (`Sync\SyncHealthService`، `Sync\SyncRunLogService`، `Customers\IdentityConflictService`؛ ردیف‌ها VOهای `SyncRunRow`/`IdentityConflictRow` با `COLUMNS` ثابت). (۲) **نام‌های واقعی:** جدول `sync_jobs` (نه `sync_runs`)؛ `pages_processed`، `records_processed`، `finished_at` به‌جای `pages_fetched`، `orders_synced`، `completed_at/failed_at`. `identity_conflicts` ستون `type`/`description` ندارد: «نوع» = `status` و «توضیح» = `reason` (کد پایدار مثل `last_name_mismatch`؛ UI برچسب فارسی می‌دهد و کد خام را هم نشان می‌دهد). (۳) **صف Redis است، نه جدول `jobs`** (`QUEUE_CONNECTION=redis`؛ جدول `jobs` همیشه خالی و «۰» گمراه‌کننده می‌بود): عمق صف `sync` با `Queue::size('sync')` و تعداد شکست با Failer (`CountableFailedJobProvider`، فقط عدد؛ Payload/Trace هرگز خوانده نمی‌شود). اگر Backend صف در دسترس نباشد `null` («نامشخص») می‌شود و فقط نام کلاس خطا لاگ می‌شود؛ صفحه نمی‌شکند. (۴) مسیر/پوشه بر اساس Convention مخزن: `routes/system.php` (مثل `routes/audit.php`)، `resources/js/pages/system/*.tsx` (حروف کوچک، kebab)، لینک‌های Sidebar با `can()`؛ Laravel 13 / Inertia 3 / React 19 (نه 11/2/18 متن تسک).
- **⚠ تغییر روی کد P2-11 (کوچک، الزامی):** `ReconciliationService` قبلاً `WooClient` را در Constructor می‌گرفت و Binding آن بدون اعتبارنامه‌ی Woo `WooConfigurationException` می‌دهد، پس ساختن سرویس (و در نتیجه `gateOne()`) در فروشگاهی که Woo تنظیم نشده می‌شکست، یعنی صفحه‌ی Health دقیقاً وقتی لازم است خراب می‌شد. اکنون Client هنگام **خواندن ماه** از Container ساخته می‌شود؛ رفتار `compare/reconcile/gateOne` تغییر نکرد (همه‌ی تست‌های P2-11 سبز). یک متد فقط‌خواندنی `latest(int)` (سه ماه آخر، بدون `details`) و VO `ReconciliationMonthSummary` افزوده شد.
- **Health:** آخرین Run موفق `orders` (`completed`، جدیدترین `started_at`)، عمق صف `sync`، شمار Failed Jobs، سه ماه آخر Reconciliation (ماه + وضعیت + اختلاف تعداد/درصد؛ متن خطا فقط برای `failed` و همان‌طور که ذخیره شده، چون هنگام نوشتن پاک‌سازی شده) و پنل GATE 1 از `gateOne()` (باز = کهربایی/عبور = سبز، لیست ماه‌های تطبیق‌نشده و قرمز/ناموفق). با DB خالی: **باز، ۰ از ۲۳ ماه** — درست و مورد انتظار.
- **Sync Logs:** صفحه‌بندی ۲۵تایی، جدیدترین اول، فیلتر `status` و `entity` (مقدار ناشناخته رد می‌شود نه نادیده؛ `?status=` خالی = همه)، فیلترها در لینک‌های صفحه‌بندی می‌مانند. ستون‌ها: شروع، نوع، وضعیت، صفحه‌ها، سفارش‌ها، پایان، مدت (ثانیه، محاسبه‌شده). خطا **فقط برای Run با وضعیت `failed`** و با Expand در ردیف. Entity/status خام خوانده می‌شود تا مقدار ناشناخته‌ی آینده صفحه را نشکند. هرگز فرستاده نمی‌شود: `cursor_from/to` (ستون‌ها اصلاً Select نمی‌شوند)، `id`، `mode`، `records_failed`.
- **Identity Conflicts:** ۲۵تایی، جدیدترین اول؛ فقط `created_at`، `status`، `woo_order_id`، `reason`. **نام‌ها (`existing_name`/`incoming_name`)، `customer_id` و هر Join با orders/customers ارسال یا خوانده نمی‌شود** (تست Query-listen: هیچ Query به `orders/customers/...` نمی‌رود). ⚠ برای Review واقعی احتمالاً نام‌ها لازم می‌شود؛ تصمیم و مجوز جداگانه.
- **تاریخ‌ها:** Helper تازه‌ی `App\Support\TehranDateTime` (`JalaliDate::format` + ساعت `Asia/Tehran`، مثلاً `1405/01/01 00:00:00`) سمت PHP؛ React فقط رشته‌ی آماده را نشان می‌دهد (`dir="ltr"` برای ارقام).
- **Front-end:** `status-badge.tsx` (نبود؛ ساخته شد) و `pagination.tsx` (نبود؛ صفحه‌ی Audit نسخه‌ی خودش را دارد و دست نخورد)، نگاشت وضعیت‌ها در `lib/system-status.ts`، Props تایپ‌شده در `types/system.ts` (بدون `any`/`dangerouslySetInnerHTML`؛ Arch قفل می‌کند).
- **⚠ دو Guard قدیمی عمداً و باریک به‌روز شد:** تست P2-08 «فقط SyncService جدول Run را می‌نویسد» حالا سه کلاس **فقط‌خواندنیِ** P2-12 را از ممنوعیت اشاره به `SyncJob` مستثنا می‌کند (نبود هیچ نوشتنی در آن‌ها را `SystemPagesBoundaryTest` قفل می‌کند)؛ و Docblock `@property Carbon $created_at` به مدل `IdentityConflict` افزوده شد (فقط Docblock، صفر تغییر رفتار؛ PHPStan ریشه‌ی خطا را همین می‌دانست) — تنها دست‌خوردن به یک مدل موجود.
- **⚠ باگ Schema (P1-01) هنوز باز است و به P2-12 مربوط نیست:** `identity_conflicts.status` `varchar(15)` است ولی `confirmed_different` ۱۹ کاراکتر است (خط ۴۵۹ همین فایل). P2-12 فقط فهرست می‌کند و نمی‌تواند چنین ردیفی بسازد؛ **قبل از هر جریان بازبینی/Resolve** باید Migration تازه ستون را به `varchar(20)` برساند (تست این صفحه عمداً `confirmed_different` نمی‌نویسد).
- **⚠ وابستگی محیطی (از قبل بود، اکنون صفحه‌های تازه را هم شامل می‌شود):** تست‌های Inertia به `public/build/manifest.json` (خروجی Build محلی و Gitignored) نیاز دارند؛ بدون `npm run build` صفحه‌ی تازه در Manifest نیست و تست‌ها ۵۰۰ می‌دهند (Audit و Dashboard هم همین‌طورند). ⚠ **DB توسعه** (`heymode`) هنوز Migrationهای P2-09 و P2-11 را Pending دارد، پس صفحه‌ی Health روی آن تا Migrate (مجوز جداگانه) خطا می‌دهد؛ تست‌ها روی `heymode_testing` هستند. ⚠ صفحه‌های React در مرورگر باز نشدند: `tsc`، Lint (`vp lint`)، Build و قرارداد Propها (تست Feature) سبزند ولی رندر بصری و RTL دیده نشده.
- **تست (PostgreSQL واقعی، بدون شبکه):** ۷۶ تست خالص جدید = `HealthControllerTest` (۱۹)، `SyncLogsControllerTest` (۲۷)، `IdentityConflictsControllerTest` (۱۱)، `TehranDateTimeTest` (۶)، `SystemPagesBoundaryTest` (۱۱، Arch)، `RoleSeederTest` (+۲). هر سه صفحه: Guest ← Redirect لاگین، بدون مجوز ← ۴۰۳ (Deny صریح هم)، مجاز ← ۲۰۰ با نام Component؛ کلیدهای Props دقیق و نبودن هرگونه کلید Cursor/Secret/ID/Payload؛ صفحه‌بندی صفحه‌ی ۲؛ فیلترها؛ نبودن نوشتن. Mutation check (۲۸ نقض عمدی؛ دو مورد اول «زنده» ماندند و هر دو ضعف خود Mutation بود، نه تست: یکی مقدار معکوس‌شده‌ای که تصادفاً همان ۴۲ می‌شد و دیگری Seeder که در مجموعه‌ی تست آن اجرا نشده بود؛ با Mutation درست و مجموعه‌ی درست گرفته شدند): مجوز حذف‌شده ← ۴؛ مجوز اشتباه هویت ← ۱۱؛ ترتیب ← ۴/۲؛ فیلتر status/entity نادیده ← ۵/۱؛ خطا برای همه‌ی وضعیت‌ها ← ۴؛ Cursor Select شود ← ۱ (Arch)؛ آخرین Sync شامل failed ← ۲؛ صف اشتباه ← ۱؛ Failed Jobs صفر ← ۲؛ خطای صف گرفته نشود ← ۱؛ ساعت UTC ← ۹؛ نام‌ها Select شود ← ۱ (Arch)؛ ماه سبز در failing ← ۲؛ Client Woo زودهنگام ← ۱۶؛ Route نوشتنی ← ۱؛ صف از جدول `jobs` ← ۳. Suite: ۱۷۰۸ سبز (۱۶۳۲ + ۷۶)؛ Arch ۹۴؛ PHPStan ۰ خطا؛ Pint سبز؛ `tsc` سبز.
- **GATE 1 — همچنان باز (OPEN).** هیچ Sync زنده، `hm:reconcile`، Migrate یا Reset انجام نشد؛ وضعیت ثبت‌شده در P2-11 تغییر نکرد و Health آن را به‌درستی «باز، ۰ از ۲۳» نشان می‌دهد. توضیح: جمله‌ی «P2-12 … تا عبور این دروازه شروع نمی‌شود» در ثبت GATE 1 بالا بیش‌ازحد سخت‌گیرانه بود؛ PRD §25 و CLAUDE.md §11 دروازه را **بعد از** P2-12 (پایان Sprint 2) می‌گذارند، پس P2-12 به دستور صریح کاربر با دروازه‌ی باز ساخته شد. دروازه همچنان **Sprint 3 و بعد از آن** را مسدود نگه می‌دارد.

## P2-13 — سفارش بدون تلفن قابل‌استفاده (رفع مانع GATE 1)

- **یافته‌ی زنده (Full Sync اول، ۲۰ شهریور ۱۴۰۵):** Run روی صفحه‌ی ۱۶ با `OrderCustomerUnresolvedException` شکست خورد: سفارش Woo `15091` (مهمان، `completed`، ایجاد ۱۴۰۳-۱۰، `billing.phone` **خالی** ولی `shipping.phone` معتبر، بدون Meta تلفن‌مانند) هیچ تلفن قابل‌استفاده‌ای نداشت. این همان ریسک ثبت‌شده در P2-06 بود (سفارش بدون تلفن هرگز ذخیره نمی‌شد ← هم Sync می‌ایستاد هم `count_diff > 0` می‌شد). بعداً همان صفحه با همان Window دوباره خوانده شد و دیگر چنین سفارشی نداشت (فروشگاه زنده است؛ `X-WP-Total` از ۲۴٬۴۲۱ در V8 به ۲۴٬۵۶۰ رسیده) ← تعداد کل سفارش‌های بی‌تلفن **هنوز نامعلوم** است و فقط Sync کامل نشان می‌دهد.
- **تصمیم (کاربر، PRD §08 گام ۱):** سفارش بدون تلفن **ذخیره می‌شود**: `orders.customer_id = NULL` و `orders.needs_phone_review = true`؛ نه مشتری Placeholder ساخته می‌شود و نه سفارش رد می‌شود. «بدون تلفن قابل‌استفاده» فقط با `PhoneNormalizer` تشخیص داده می‌شود (null، خالی، فقط فاصله، کوتاه، پیش‌شماره‌ی نامعتبر). چون سفارش در Woo هست، Reconciliation آن را می‌شمارد (تست).
- **Migration افزایشی** `2026_09_20_110000_make_customer_id_nullable_and_add_needs_phone_review`: (۱) `orders.customer_id` nullable (FK و `RESTRICT` سر جایش)؛ (۲) `orders.needs_phone_review boolean NOT NULL DEFAULT false`؛ (۳) **CHECK** `customer_id IS NOT NULL OR needs_phone_review` تا سفارشِ بی‌مشتری هرگز بیرون از صف بازبینی نماند؛ (۴) **⚠ هم‌بستگیِ لازم که در متن تسک نبود:** `identity_conflicts.customer_id` هم nullable شد، چون Conflict یک سفارش بی‌تلفن مشتری ندارد (بدون Placeholder راه دیگری نیست)؛ (۵) Index یکتای جزئی `identity_conflicts (woo_order_id) WHERE reason = 'no_phone'` تا Resync یا دو Worker هم‌زمان Conflict را دوبار نسازند. `down()` سفارش‌های بی‌مشتری (Items/Refunds/History با Cascade) و Conflictهای بی‌مشتری را حذف می‌کند چون Schema قدیم نگهشان نمی‌دارد؛ داده‌ی Mirror است و Woo هنوز دارد. تست‌شده.
- **رفتار:** `OrderService` دیگر Exception نمی‌دهد؛ اگر تلفن نامعتبر بود، `CustomerIdentityService::recordOrderWithoutPhone()` (Customers مالک `identity_conflicts` است؛ Orders فقط Service صدا می‌زند) **یک** ردیف `pending` با `reason = no_phone`، `woo_order_id`، `customer_id = NULL` و **بدون نام و بدون تلفن** می‌نویسد (`INSERT ... ON CONFLICT DO NOTHING`؛ Resync یا ردیفی که بازبین بسته دوباره ساخته نمی‌شود). تلفن و نام هرگز در Log، Exception یا Conflict نمی‌روند (تست). `OrderCustomerUnresolvedException` حذف شد (بی‌مصرف). رویداد `IdentityConflictDetected` برای `no_phone` Dispatch **نمی‌شود** (`customerId` غیرقابل‌null است و شنونده‌ای ندارد).
- **تصمیم کوچک من (گزارش‌شده):** اگر سفارشی که قبلاً مشتری داشت در Sync بعدی بدون تلفن بیاید، **مشتریِ قبلی حفظ می‌شود** (نه Detach و نه Conflict) تا تاریخچه‌ی مشتری نپرد. برعکس، سفارش بی‌تلفنی که بعداً تلفن معتبر بگیرد به مشتری وصل و Flag پاک می‌شود؛ ⚠ ردیف `no_phone`ِ آن `pending` می‌ماند (وضعیتی برای «خودکار بسته شد» در Enum نیست).
- **UI (P2-12):** صفحه‌ی تعارض‌های هویت برچسب فارسی `no_phone` را دارد؛ `needs_phone_review` هنوز صفحه‌ی بازبینی ندارد.
- **مشاهده (تصمیم نگرفته شد):** سفارش 15091 تلفن **معتبرِ حمل‌ونقل** دارد؛ افتادن به `shipping.phone` صف بازبینی را کوچک می‌کرد ولی معنای هویت را (PRD §08: تلفن صورت‌حساب) عوض می‌کند و تصمیم محصولی است، پس انجام نشد.
- **⚠ ریسک ثبت‌شده برای GATE 1:** صفحه‌بندی Offset روی `orderby=modified asc` در فروشگاه زنده: سفارشی که بعد از Freeze ویرایش شود از مجموعه بیرون می‌افتد و سفارش‌های بعدی یک جا بالا می‌آیند؛ اگر این وسط صفحه‌ی خوانده‌شده باشد، یک سفارش بی‌صدا جا می‌ماند (و Cursor بعدی آن را نمی‌گیرد). Reconciliation دقیقاً همین را به‌صورت `count_diff` نشان می‌دهد؛ در آن صورت Full Sync تکرار می‌شود (Upsert ایدمپوتنت است).
- **تست (PostgreSQL واقعی):** ۲۹ تست خالص جدید = `OrderSyncPhonelessTest` (۲۰)، `NoPhoneConflictTest` (۴)، `PhonelessOrderSchemaTest` (۴)، `IdentityConflictsControllerTest` (+۱). **دو تست قدیمی عمداً معکوس شد** (نه سست): `OrderServiceTest` و `OrderSyncServiceTest` قبلاً «رد/توقف» را قفل می‌کردند و اکنون «ذخیره با Flag» را. Mutation check (۱۴ نقض؛ ۱۳ گرفته شد؛ یکی «زنده» = حذف Cast بولی مدل که روی PostgreSQL معادل است، مثل `is_realized`): Flag ثبت نشود ← ۱۸؛ Conflict ثبت نشود ← ۸؛ مشتری قبلی جدا شود ← ۱؛ Exception دوباره پرتاب شود ← ۱۹؛ `insert` به‌جای `insertOrIgnore` ← ۳؛ نام ذخیره شود ← ۲؛ Migration بدون CHECK/Index/Nullable ← ۱/۴/۲۶؛ `down()` سفارش بی‌مشتری را نگه دارد ← ۱؛ پیش‌فرض Flag `true` ← ۱. Suite: ۱۷۳۷ سبز (۱۷۰۸ + ۲۹)؛ PHPStan ۰ خطا؛ Pint سبز.

## P2-14 — `unit_price` از جمع سطر و تعداد (رفع مانع سوم GATE 1)

- **یافته‌ی زنده (Full Sync دوم، ۲۰ شهریور ۱۴۰۵):** Run روی صفحه‌ی ۹۴ با `WooMappingException: ... 'line_items.0.price': expected a whole-Toman amount, got float` شکست خورد. `line_items[].price` در Woo **مقدار مشتق‌شده‌ی اعشاری** است (جمع سطرِ بعد از تخفیف ÷ تعداد)، در حالی که `subtotal` و `total` سطر رشته‌ی صحیح‌اند. نمونه‌ی زنده: سفارش `22539` (`completed`، IRT) شش سطر اعشاری دارد، مثلاً `63649.75` برای تعداد ۴ و `total = 254599`؛ سایر نمونه‌ها `90202.5`، `46065.666…`، `27597.5`. هر سطر تخفیف‌خورده‌ای که بخش‌پذیر نباشد همین را می‌زند، پس این باگ Mapper بود (P2-03)، نه داده‌ی بد.
- **تصمیم (کاربر، قطعی):** `unit_price = floor(line_total / qty)` = «قیمت واقعاً پرداخت‌شده»، با **حساب صحیح** (`intdiv` روی دو مقدار نامنفی، همان floor)، و `0` وقتی `qty = 0`. `price` ووکامرس دیگر **خوانده نمی‌شود** (نه لازم است، نه اعشارش خطاست). `subtotal` و `total` سطر دست نخوردند. دلیل انتخاب: برای سفارش‌هایی که قبلاً ذخیره شده‌اند (قیمت صحیح) مقدار **دقیقاً همان قبلی** است. `unit_price` در هیچ محاسبه‌ای (درآمد، Reconciliation) نیست؛ همه از `total` می‌آیند.
- **حساب Float ممنوع (Arch-تست‌شده):** Mapper هیچ `(float)`، `floatval`، `round/floor/ceil`، `number_format` و `bc*` ندارد؛ تست مقادیر بالاتر از دقت دقیق Float (`2^53 + 1`) را دقیق نگه می‌دارد. اعشار `price` بی‌ضرر و نادیده است، حتی اگر متن یا `null` باشد یا نباشد.
- **⚠ انحراف کوچک از متن تسک (گزارش‌شده):** «تعداد ناموجود ← `unit_price = 0`» را اجرا **نکردم**. `quantity` فیلد الزامی و اعتبارسنجی‌شده‌ی قدیمی است (`nonNegativeInt`) و ستون `qty` را هم می‌سازد؛ پذیرفتن بی‌صدای «نبود» یعنی ذخیره‌ی `qty = 0` برای Payload ناقص (قانون «هرگز null/0 بی‌صدا»ی Mapper). پس نبودن `quantity` همچنان `WooMappingException` با نام فیلد است (تست) و هرگز تقسیم بر صفر یا TypeError نیست؛ `quantity = 0` (موجود ولی صفر) طبق متن `unit_price = 0` می‌دهد. اگر می‌خواهید نبودن هم پذیرفته شود، تصمیم جداست.
- **دو تست قدیمی عمداً تغییر کرد:** `OrderMapperTest` «`line_items.0.price` الزامی است» و «`price` اعشاری رد می‌شود» را قفل می‌کرد؛ قرارداد عوض شد، پس آن دو ردیف حذف و جایشان آزمون‌های تازه (اعشاری، صحیح، رشته‌ی صحیح، صفر، ۲^۵۳، `price` نبود/`null`/متن) آمد. تست در `tests/Unit/Modules/Sync/OrderMapperTest.php` است (متن تسک `tests/Feature/...` می‌گفت؛ Mapper تست Unit است).
- **تست:** ۱۸ تست خالص جدید (۱۷۳۷ ← ۱۷۵۵ سبز)؛ Arch ۹۴؛ PHPStan ۰ خطا؛ Pint سبز. Mutation check (۸ نقض، همه گرفته شد و فایل‌ها برگشتند): گرد‌کردن به‌جای floor ← ۱۲؛ تقسیم Float ← ۲؛ حذف Guard صفر ← ۱؛ خواندن دوباره‌ی `price` ← ۱۶؛ استفاده از `subtotal` ← ۱۶؛ صفر ← جمع ← ۱؛ تغییر `total` ← ۳.
- **وضعیت GATE 1:** بدون تغییر — همچنان باز؛ Full Sync سوم هنوز اجرا نشده و Reconciliation اجرا نشده است.

## GATE 1 — عبور کرد (Sprint 2 بسته شد)

- **GATE 1: PASSED — ۲۹ شهریور ۱۴۰۵ (1405/06/29 = 2026-09-20)، ۲۳ ماه (1403-07 تا 1405-05) همه green؛ بزرگ‌ترین اختلاف: ۰٫۰۰۰۰٪ (اختلاف تعداد سفارش ۰ در هر ماه).** `ReconciliationService::gateOne()` روی DB واقعی `heymode` `passed = true` داد (۲۳/۲۳ سبز، ماه گم‌شده ۰، ماه قرمز/ناموفق ۰) و داده‌ی صفحه‌ی Health همین را نشان می‌دهد. ثبت‌های قبلی «GATE 1 باز» (۲۰ شهریور، P2-11 و P2-12) به‌عنوان تاریخچه دست نخوردند و این ثبت جدیدتر آن‌ها را منسوخ می‌کند.
- **شواهد (زنده، فقط‌خواندنی نسبت به Woo):** Full Sync سوم `completed` — ۴۹۲ صفحه، ۲۴٬۸۹۴ رکورد، ≈۵۵ دقیقه، Cursor روی `2026-09-20T06:36:45Z` پیش رفت. ۲۲٬۵۶۱ سفارش در ماه‌های دروازه: Woo = محلی، **دقیقاً**، و مجموع درآمد realized هر ماه بی‌اختلاف (Toman صحیح). DB محلی: ۲۴٬۵۷۰ سفارش، ۱۹٬۵۸۶ مشتری، ۲۲۲ سفارش `needs_phone_review` با ۲۲۲ Conflict `no_phone`.
- **مسیر رسیدن (سه Sync، دو مانع واقعی):** Run ۱ روی سفارش بی‌تلفن `15091` شکست خورد ← **P2-13** (`customer_id` nullable + `needs_phone_review`، Commit `3d6b2cc`)؛ Run ۲ روی `line_items.price` اعشاری سفارش `22539` شکست خورد ← **P2-14** (`unit_price = floor(total/qty)` صحیح، Commit `f141ea0`)؛ Run ۳ کامل شد. داده‌ی دموی محلی (۱۱۷ سفارش `woo_order_id` 60001..60117، ۵۰ مشتری، با Cascade وابسته‌ها) با مجوز صریح پیش از Run سوم حذف شد (بررسی: صفر مشتری مشترک با سفارش واقعی). ⚠ **باقی‌مانده‌ی Demo:** کاتالوگ Demo (۱۲ محصول، ۵ دسته، ۲۴ Variation) هنوز هست؛ سفارش واقعی به آن‌ها وصل نیست (۰ از ۶۰٬۱۷۰ قلم `product_id` یا `variation_id` دارد؛ Sync کاتالوگ زنده هنوز اجرا نشده).
- **⚠ یافته‌ی عملیاتی مهم — Job ماهانه‌ی Reconciliation برای ماه‌های بزرگ Timeout می‌خورد:** ۲۲ `ReconcileMonthJob` (Dispatch را من نکردم؛ به‌نظر `hm:reconcile --all` از Terminal دیگری بود) ۱۹ ماه اول را سبز کردند، ولی `1405-02` (۱٬۵۶۱ سفارش) با `TimeoutExceededException` (۸۰ ثانیه) `failed` شد و **خودِ Worker با کد ۱۳۷ کشته شد**؛ `1405-03..05` هرگز اجرا نشدند. این همان ریسک ثبت‌شده‌ی P2-11 است و اکنون قطعی شد. چهار ماه با `hm:reconcile --month=` (هم‌زمان، بدون Timeout صف) بسته شد (۱٬۵۶۱، ۲٬۳۷۵، ۳٬۱۶۹ و ۳٬۰۱۴ سفارش، هر چهار سبز). **پیامد: `Schedule::command('hm:reconcile --all')` ساعت ۰۲:۰۰ همین‌طور خواهد شکست و Worker را خواهد کشت** تا Timeout/`retry_after` بالا نرود یا Job به صفحه تقسیم نشود؛ **پیش از روشن‌کردن Scheduler در Production باید رفع شود** (کار جدا، انجام نشد). سه Job قدیمیِ صف‌مانده (`1405-03..05`) در Redis محلی بود و همراه توقف آن دور ریخته شد؛ همان‌ها روی هر Worker تازه دوباره Worker را می‌کشتند.
- **مشاهده‌ی کسب‌وکاری:** ماه `1405-01` در هر دو طرف **۰ سفارش** دارد (سبز، ولی «خالی-سبز»): آخرین سفارش `2026-03-11` (اسفند ۱۴۰۴، ۲۰ اسفند) و اولین بعدی `2026-04-23` (۳ اردیبهشت) است؛ ماه قبل ۵۳۶ و ماه بعد ۱٬۵۶۱ سفارش. Woo و محلی هم‌نظرند، پس خطای Sync نیست؛ علتش (توقف فروشگاه؟ مهاجرت؟) را باید کسب‌وکار تأیید کند، چون Metrics بعدی (Cohort/Churn/RFM) از آن عبور می‌کنند. همچنین ۱۲ سفارش محلی قبل از 1403-07 هست (ایجادشده قبل، ویرایش‌شده بعد از Epoch) که بیرون بازه‌ی دروازه‌اند.
- **⚠ محدودیت‌های شواهد:** دروازه فقط ماه‌های **کامل** را می‌سنجد؛ ماه جاری (`1405-06`) و سفارش‌های پس از Freeze (`06:36:45Z`) تا Poll افزایشی بعدی خارج از شواهدند (Scheduler در Dev روشن نیست، پس `hm:sync` هر ۱۵ دقیقه هنوز اجرا نمی‌شود). درستی هدرهای Webhook (P2-09) و رفتار `refunds` در Payload همچنان فقط با فروشگاه واقعی تأیید می‌شوند و در این اجرا آزموده نشدند. سه ردیف `failed_jobs` (دو Run ناموفق و Timeout) عمداً دست نخوردند.
- **محیط:** Redis اجرا نبود؛ یک نمونه‌ی محلی موقت (Loopback، بدون Persist) شروع و در پایان متوقف شد. کد ۱۷۵۵ تست سبز، PHPStan ۰ خطا.
- **وضعیت پس از عبور:** دروازه‌ی ۱ دیگر Sprint 3 را مسدود نمی‌کند (CLAUDE.md §11). ⚠ پیشنهاد: قبل از Scheduler/Production، Timeout Job ماهانه و صف بازبینی سفارش‌های بی‌تلفن (۲۲۲) تعیین تکلیف شود.

## P3-01 — Timeout Job ماهانه‌ی Reconciliation (۳۰۰ ثانیه)

- **مشکل (ثبت‌شده در GATE 1):** `ReconcileMonthJob` با `timeout = 80` برای ماه‌های بزرگ کافی نبود؛ `1405-02` (۱٬۵۶۱ سفارش) در ۸۲ ثانیه `TimeoutExceededException` خورد و **Worker با کد ۱۳۷ کشته شد** (Laravel روی Timeout هر Job کل Worker را می‌کشد)، پس `1405-03..05` هرگز اجرا نشدند.
- **اندازه‌گیری واقعی (Worker Log + اجرای هم‌زمان `--month=` بدون محدودیت صف):** Jobهای موفق ۱۱–۸۰ ثانیه (چهار Job نزدیک ۶۲–۸۰ ثانیه)، ولی `1405-02` = ۱۰۳ ثانیه (۱٬۵۶۱ سفارش)، `1405-03` = ۱۴۳ (۲٬۳۷۵)، `1405-04` = ۲۰۶ (۳٬۱۶۹)، `1405-05` = ۱۹۰ (۳٬۰۱۴) ← ≈ **۶۵ میلی‌ثانیه به‌ازای هر سفارش** (Rate Limit و ۵۰ سفارش در هر صفحه).
- **تصمیم (کاربر):** `ReconcileMonthJob::$timeout` از ۸۰ به **۳۰۰**؛ بدون بازطراحی و بدون Chunking. بزرگ‌ترین ماه زنده ۲۰۶ ثانیه (۶۹٪ سقف) است؛ **سقف عملی ≈ ۴٬۵۰۰ سفارش در ماه**، بیشتر از آن باید دوباره بالا برود یا Job صفحه‌ای شود (در Docblock Job ثبت است).
- **⚠ هم‌بستگی لازم (متن تسک «یک خط» می‌گفت):** `queue.connections.redis.retry_after` هم از ۹۰ به **۳۳۰** رفت (`env('REDIS_QUEUE_RETRY_AFTER', 330)`؛ در `.env` Override نشده). دلیل: تست‌های موجود (`ReconcileMonthJobTest`، `SyncJobsTest`) و منطق Laravel می‌گویند `timeout` باید کمتر از `retry_after` باشد، وگرنه Job در حال اجرا بعد از ۹۰ ثانیه دوباره Reserve و **دو بار اجرا** می‌شود (با `tries = 1` دومی `MaxAttemptsExceeded` می‌گیرد و ماه را `failed` ثبت می‌کند در حالی که اولی هنوز مشغول است). تست تازه حاشیه‌ی ≥ ۳۰ ثانیه را برای هر سه Job قفل می‌کند. `uniqueFor = 600` هنوز بالاتر از `timeout` است. Jobهای دیگر (`timeout` ۶۰–۸۰) از بالارفتن `retry_after` فقط دیرتر Re-reserve می‌شوند.
- **⚠ راستی‌آزمایی نشده:** `config/horizon.php` Supervisor با `timeout => 60` دارد؛ طبق منطق Laravel مقدار `$timeout` خودِ Job مقدم است، ولی Horizon در این جلسه اجرا نشد، پس روی Production باید یک بار با یک ماه بزرگ دیده شود. همچنین `Schedule::command('hm:reconcile --all')` ساعت ۰۲:۰۰ حالا نظری‌ست که ۲۳ Job را پشت‌سرهم (با یک Worker ≈ نیم ساعت) تمام کند؛ زنده آزمایش نشد.
- **یافته‌ی جانبی ۱ — ۲۲۲ سفارش بی‌تلفن (فقط اطلاعات؛ دست نخورد):** وضعیت‌ها `completed` ۱۴۵، `cancelled` ۶۸، `pending` ۵، `refunded` ۴؛ درآمد realized آن‌ها **۸۲٬۰۱۹٬۹۲۸ تومان = ۰٫۶۹٪** کل درآمد realized (۰٫۹٪ کل سفارش‌ها)؛ متمرکز در `1404-09` (۷۸)، `1404-02` (۴۵)، `1404-01` (۳۴). صف بازبینی/UI کار Sprint بعدی است.
- **یافته‌ی جانبی ۲ — ⚠ سه سفارش با تاریخ اشتباه در Woo، نامرئی برای GATE 1:** `19627` (`cancelled`)، `19640` و `19642` (`completed`، هر دو بی‌تلفن) در Woo با `date_created` برابر **`1404-02-02` میلادی** ثبت شده‌اند (تاریخ جلالی ۱۴۰۴/۰۲/۰۲ = 2025-04-22 که در فیلد میلادی تایپ شده؛ `date_modified` همان 2025-04-22 است). چون سال ۱۴۰۴ میلادی قبل از همه‌ی ماه‌های دروازه است، هر دو طرف Reconciliation آن‌ها را **بیرون** می‌گذارند؛ درآمد realized آن‌ها ۲۷۹٬۸۰۰ تومان است. باید در Woo اصلاح شود یا Mapper تاریخ را رد/علامت بزند (تصمیم جدا). کلاً ۱۲ سفارش محلی قبل از 1403-07 هست (۳ مورد بالا + ۹ سفارش واقعاً قدیمی‌تر که بعد از Epoch ویرایش شده‌اند).
- **محیط:** کل Suite به Redis واقعی روی `127.0.0.1:6379` نیاز دارد (تست‌های `HttpWooClient`/`RedisTokenBucket`)؛ وقتی Redis پایین است ۹۶ تست «Connection refused» می‌دهند و این Regression نیست.
- **تست:** ۲ تست خالص جدید (۱۷۵۵ ← ۱۷۵۷ سبز)؛ Arch ۹۴؛ PHPStan ۰ خطا؛ Pint سبز. دو تست جدید قبل از تغییر شکست خوردند (۸۰ ≠ ۳۰۰؛ `retry_after` ۹۰ < ۱۱۰).

## Open findings before Sprint 3

_Label note: the section headed "P3-01" above is commit **P2-15** (renamed, because PRD P3-01 is the customer list)._

1. **Horizon supervisor timeout.** Horizon has a 60s supervisor timeout. Laravel gives the job's `$timeout` precedence over it, but this was not verified on production. Before enabling the scheduler on production, confirm `supervisor.timeout` in `config/horizon.php` is ≥ 330 or that the reconcile queue uses its own supervisor block with a higher value. ⚠ Note added when logging: a supervisor `timeout` of exactly 330 would equal `retry_after` (330) and break the `timeout < retry_after` rule that Laravel and `ReconcileMonthJobTest`/`SyncJobsTest` enforce (a running job would be re-reserved and run twice). The safe range for a dedicated reconcile supervisor is 300 ≤ timeout < 330 (the job's own `$timeout` is 300), or keep 60 and rely on the job-level `$timeout`; raising `retry_after` further is the way to allow a higher value.
2. **Three Woo orders with a Jalali year typed into the Gregorian field.** Orders #19627, #19640 and #19642 were entered with dates like 1404/02/02 in a Gregorian field. They fall before every gate month and are invisible to reconciliation. Realized revenue affected: 279,800 Toman (orders #19640 and #19642, both completed). Decision needed: fix in Woo, or add a mapper flag for dates before 2020-01-01.
3. **Phone-less orders (222).** 0.9% of all orders (145 completed, 68 cancelled, 5 pending, 4 refunded), 82,019,928 Toman realized revenue (0.69% of total). Heaviest months: 1404-09 (78), 1404-02 (45), 1404-01 (34). No review UI yet. Note: order 15091 has a valid `shipping.phone` — using it would shrink the queue but changes the identity model (PRD §08 decision needed).

## P3-01 (PRD) — لیست مشتریان، جستجو و فیلتر

`GET /customers` (`routes/internal.php`، نام `customers.index`؛ فایل جدید طبق PRD D15 و از `web.php` لود می‌شود) پشت `auth` + `permission:customers,view` است و Controller فقط `CustomerListRequest` (اعتبارسنجی؛ مقدار نامفهوم **رد** می‌شود نه نادیده) را می‌گیرد، `CustomerListService` را صدا می‌زند و Inertia (`customers/index`) برمی‌گرداند؛ همه‌ی منطق (جستجو، فیلتر، صفحه‌بندی، ماسک) در سرویس Customers است و Arch (منطق‌نبودن Controller، نبود SQL خام، نبود هر اشاره به `customer_metrics`/RFM/churn/CLV، فقط‌ستون‌های لازم) قفلش می‌کند. **جستجو** یک جعبه است: متن با `ILIKE` روی `display_name` (`%` و `_` کاربر Escape می‌شود) و همان متن از `PhoneNormalizer` (هر قالب ایرانی، ارقام فارسی/عربی، علامت جهت) می‌گذرد؛ اگر شماره‌ی معتبر شد، مشتری با همان `phone_normalized` هم منطبق می‌شود، و متن نامعتبر فقط جستجوی نام است (خطا نمی‌دهد). **فیلترها** (AND، فقط ستون‌های `customers`): `status`، `lifecycle_stage`، `province`، `city`، `needs_review` و بازه‌ی `first_seen_at` با **روز جلالی** (`YYYY/MM/DD` یا `-`، ارقام فارسی/ASCII؛ روز واقعی و سال ۱۳۰۰–۱۴۹۹ الزامی است تا تاریخ میلادیِ اشتباه رد شود؛ ابتدا و انتهای بازه شاملِ کل روز تهران، انتها به‌صورت `<` روز بعد). **⚠ فیلتر `needs_phone_review` ساخته نشد:** آن پرچم روی `orders` است و سفارش بی‌تلفن طبق P2-13 `customer_id = NULL` دارد، پس هیچ مشتریای آن را ندارد و فیلتر همیشه خالی می‌بود؛ جایش در لیست سفارش‌ها (P3-06) است. **ماسک:** `CustomerListRow` تنها جایی است که موبایل تصمیم گرفته می‌شود؛ بدون `customers.view_full_phone` (که `PermissionService` با ترتیب deny > allow > role > بسته تعیین می‌کند) فقط چهار رقم آخر می‌ماند و بقیه `*` است (`********4567`)، **در خودِ Response**، نه فقط در UI؛ با مجوز، شماره‌ی نرمال‌شده‌ی کامل می‌آید. ⚠ این نمایش کامل در لیست **Audit نمی‌نویسد** (Reveal تعاملی و ممیزی‌اش کار P3-02 است) و کاربر بدون مجوز با تایپ یک شماره‌ی کامل می‌تواند وجود آن مشتری و نامش را بفهمد (طبق خواسته‌ی تسک، جستجوی موبایل برای همه‌ی دارندگان `customers.view` مجاز است). **صفحه‌بندی:** Offset استاندارد Laravel، ۲۵ ردیف، ترتیب ثابت `created_at DESC, id DESC` (بدون تکرار/جاافتادگی)، فیلترها در لینک‌ها می‌مانند. **⚠ واقعیت داده و کارایی (اندازه‌گیری روی DB توسعه، ۱۹٬۵۸۶ مشتری، فقط‌خواندنی):** `first_seen_at`، `province`، `city` برای همه‌ی مشتریان Sync‌شده `NULL` است و `lifecycle_stage` همه `prospect`، پس روی داده‌ی واقعی فعلاً فقط جستجو و وضعیت نتیجه می‌دهند و بقیه‌ی فیلترها تا پرشدن ستون‌ها (Sprint 4 / Sync آدرس) خالی‌اند؛ **⚠ ایندکس GIN trigram روی `display_name` برای فارسی بی‌اثر است:** کلاستر PostgreSQL با `lc_ctype = C` ساخته شده و `pg_trgm` در این حالت از متن فارسی Trigram نمی‌سازد (`show_trgm('محمد') = {}`)، پس جستجو Seq Scan است؛ جستجو ≈ ۴۳۰–۴۶۵ms (شمارش ≈ ۲۵۰ + صفحه ≈ ۱۲۰) روی ۱۹٬۵۰۰ رکورد یعنی مرز هدف PRD §23 (<۵۰۰ms روی ۱۵٬۰۰۰)، و برای ۱۰۰٬۰۰۰ مشتری قطعاً نمی‌رسد؛ همچنین صفحه‌ی بدون فیلتر (Sort روی `created_at, id` بدون ایندکس) ≈ ۱۲۵ms است. **تصمیم‌های زیرساختی لازم (انجام نشد، خارج از Scope):** ساخت DB با Locale یونیکد (`fa_IR.UTF-8`/ICU) یا روش دیگر جستجوی فارسی، و ایندکس `customers (created_at DESC, id DESC)`. **تست:** ۱۳۰ تست خالص جدید (Unit `PhoneMask`/`Digits`/`JalaliDay`، Feature سرویس روی `DemoDataSeeder`، Feature HTTP، Arch)، هر سه سطح مجوز (مهمان ← لاگین، بدون مجوز ← ۴۰۳، Deny صریح)، ماسک با/بدون مجوز و نبودن شماره‌ی کامل در JSON، قالب‌های مختلف شماره، نام فارسی، فیلترها، مرز روز تهران، صفحه‌بندی؛ Mutation check ۲۲ نقض عمدی همگی گرفته و برگردانده شد. Suite ۱۸۸۷ سبز (۱۷۵۷ + ۱۳۰)، Arch ۱۲۴، PHPStan ۰ خطا.

## P3-02 — نمایش تعاملی شماره‌ی کامل با ثبت Audit (Phone Reveal)

`POST /customers/{customer}/reveal-phone` (`routes/internal.php`، نام `customers.reveal-phone`، `whereNumber`) پشت `auth` + `permission:customers,view_full_phone` + `throttle:10,1,phone-reveal` است؛ `PhoneRevealController` فقط `PhoneRevealRequest` را می‌گیرد (که مشتری را با `findOrFail` می‌یابد ← حذف‌شده/ناموجود = ۴۰۴، و **بعد از** Middlewareها اجرا می‌شود تا کاربر بی‌مجوز هرگز ۴۰۴ (افشای وجود شناسه) نبیند و Controller مدل Import نکند) و `PhoneRevealService::reveal(Customer, Request)` را صدا می‌زند و دقیقاً `{"phone": "<شماره‌ی نرمال‌شده‌ی کامل>"}` با `Cache-Control: no-store` برمی‌گرداند (نه `customer_id`، نه نام، نه ایمیل). سرویس **در یک Transaction** یک ردیف `phone_reveal_logs` (`customer_id`، `revealed_by` از `$request->user()` نه Facade `Auth`، `revealed_at` = `now()` دیتابیس، `ip`، `user_agent` با `mb_substr` تا ۵۰۰ **نویسه**) می‌نویسد و شماره را فقط بعد از Commit برمی‌گرداند، پس نه شماره بدون Audit آزاد می‌شود و نه Audit یک Reveal شکست‌خورده می‌ماند؛ مشتری soft-delete‌شده یا بی‌شماره (ستون `NOT NULL` است، پس «بی‌شماره» یعنی رشته‌ی خالی/فاصله) پیش از هر نوشتنی ۴۰۴ می‌دهد (در سرویس هم چک می‌شود، نه فقط در لایه‌ی HTTP)؛ هیچ Log و هیچ پیام خطایی شماره یا نام ندارد (Arch + تست `MessageLogged`)، و فقط `PhoneRevealService` روی `phone_reveal_logs` می‌نویسد (Arch). **Migration** `2026_09_20_120000_create_phone_reveal_logs_table`: ایندکس‌های `(customer_id, revealed_at DESC)` و `(revealed_by, revealed_at DESC)`؛ `customer_id` **CASCADE** (طبق تسک؛ حذف واقعی مشتری ردیف‌هایش را می‌برد، soft-delete نه)، `revealed_by` **RESTRICT** (تصمیم من: مسیر Audit نباید با حذف کاربر ناپدید شود)، `ip` قابل‌null (درخواست ممکن است IP نداشته باشد)، `user_agent` `NOT NULL DEFAULT ''`؛ **جدول اختصاصی به‌جای `audit_logs`** طبق خواسته‌ی تسک. **⚠ تناقض متن تسک و تصمیم کاربر:** مسیر `view_full_phone` می‌خواست، ولی دارندگان همان مجوز در P3-01 شماره‌ی کامل را از قبل در لیست می‌دیدند و بی‌مجوزها ۴۰۳ می‌گرفتند، یعنی دکمه برای کسانی که می‌دیدند همیشه ۴۰۳ می‌داد و برای دارندگان لازم نبود و Audit هرگز نوشته نمی‌شد؛ با پرسش از کاربر **لیست از P3-01 معکوس شد: برای همه (حتی دارنده‌ی `view_full_phone`) همیشه فقط چهار رقم آخر** (`********4567`)، دارنده‌ی مجوز روی شماره کلیک می‌کند و از مسیر Audit‌شده می‌گیرد، و بدون مجوز فقط متن ماسک با قفل است. در نتیجه ⚠ «نمایش کامل بدون Audit» ثبت‌شده در P3-01 **رفع شد** (`CustomerListService` دیگر `PermissionService` و پارامتر `viewer` ندارد؛ تست‌ها و Arch P3-01 عمداً معکوس شدند، نه سست). **تصمیم قالب:** شماره‌ی نرمال‌شده (`989121234567`)، نه ملی (`09121234567` در مثال تسک، فقط توضیحی)، هماهنگ با `phone_normalized`. **دکمه‌ی React** (`components/customers/PhoneRevealButton.tsx`): حالت‌های idle ← loading ← revealed/error، `fetch` با `Accept: application/json` و توکن CSRF از کوکی `XSRF-TOKEN` (Inertia برای پاسخ JSON مناسب نیست)، پیام‌ها «تعداد درخواست‌ها بیش از حد مجاز است» (۴۲۹)، «دسترسی ندارید» (۴۰۳)، «خطا در نمایش شماره»؛ شماره‌ی کامل فقط در State خودِ کامپوننت می‌ماند (Arch: نه `console`، نه `window`، نه Storage، نه Clipboard)؛ به‌جای `span`، `button` است (Accessibility) و پرچم `hasPermission` معنای «بیننده می‌تواند Reveal کند» دارد (از `useCan`؛ فقط UX، اجرای واقعی با Endpoint). **یافته‌ی Throttle:** Laravel صفحه‌ی Throttle را **پیش از** Middleware سفارشی `permission` اجرا می‌کند، پس بیننده‌ی بی‌مجوز هم بعد از ۱۰ تلاش ۴۲۹ می‌گیرد (برای جلوگیری از کاوش خوب است و تست/مستند شد، «۴۰۳ بدون شمارش» ادعا نشد)؛ و Throttle درون‌خطی بدون پارامتر سوم با هر Route درون‌خطی دیگر **یک سطل مشترک** دارد، پس `phone-reveal` سطل جدا دارد. **انحراف‌های کوچک:** تست سرویس در `tests/Feature/Modules/Customers/` است نه `tests/Unit/` (به DB نیاز دارد و فقط Feature/Integration `RefreshDatabase` دارند)؛ `revealed_at` ساعت دیتابیس است و از `travelTo` پیروی نمی‌کند (تست با ساعت واقعی)؛ Trailer کامیت طبق متن تسک «Claude Sonnet 4.6» است. **⚠ هنوز باز:** جستجوی موبایل برای دارندگان `customers.view` (P3-01) هنوز وجود مشتری و نامش را با تایپ شماره‌ی کامل فاش می‌کند، **بدون Audit** (طبق تسک P3-01)؛ Migration روی DB توسعه **اجرا نشده** (فقط `heymode_testing`). **تست:** ۵۶ تست خالص جدید (۱۸۸۷ ← ۱۹۴۳ سبز؛ شامل بازنویسی تست‌های ماسک P3-01)، Arch ۱۳۶، PHPStan ۰ خطا؛ Mutation check ۱۸ نقض عمدی (R01–R05 تسک: حذف Audit ← ۱۵، حذف Transaction ← ۳، بی‌چک حذف‌شده ← ۲، بی‌چک بی‌شماره ← ۴، UA بدون برش ← ۳؛ به‌علاوه Log شماره، کاربر اشتباه، IP، حذف مجوز/Throttle، کلید اضافه در پاسخ، Cache، برگشت لیست به شماره‌ی کامل، Cascade/Restrict/ایندکس، پیام ۴۲۹ و مجوز صفحه) همگی گرفته و فایل‌ها برگردانده شد.

## P3-03 — Customer 360 (`GET /customers/{customer}`)

صفحه‌ی پروفایل یک مشتری، فقط‌خواندنی: Header (نام، شماره‌ی **ماسک‌شده** با `PhoneRevealButton` همان P3-02، وضعیت، مرحله، استان/شهر، اولین مشاهده)، چهار کارت معیار، RFM و ریسک، ۵ سفارش و ۵ محصول آخر. **Query Budget = ۵ (نه ۶):** جدول `customer_events` **وجود ندارد** (تایم‌لاین P3-04)، پس Q6 حذف شد، `timeline` در DTO همیشه `[]` است و صفحه هیچ بخش تایم‌لاینی نشان نمی‌دهد (Arch پویا: `CustomerShowService` فقط اگر Migration این جدول را بسازد حق دارد به آن اشاره کند). پنج Query: مشتری (`findOrFail`، حذف‌شده ← ۴۰۴)، ردیف `customer_metrics`، ۵ سفارش آخر، خطوط آیتم سفارش‌های Realized برای ۵ محصول آخر، و شمار کل سفارش‌ها؛ تست سرویس دقیقاً `=== 5` را Assert می‌کند و رشد تاریخچه (۴۰ سفارش) هم چیزی اضافه نمی‌کند. **اندازه‌گیری روی DB توسعه** (`heymode`: ۱۹٬۵۸۶ مشتری، ۲۴٬۵۷۰ سفارش، ۶۰٬۱۷۰ آیتم؛ پرسفارش‌ترین مشتری ۴۵ سفارش، فقط SELECT): سرویس p50 ≈ ۴ms و p95 ≈ ۱۲ms؛ کل درخواست از Kernel (Auth + Permission + Shared props + رندر) p50 ≈ ۳۰–۳۸ms، p95 ≈ ۵۰–۱۲۰ms، بدترین ≈ ۱۵۰ms (Gate: < ۸۰۰ms ✓). ⚠ `customer_metrics` در DB توسعه **خالی** است (Sprint 4)، پس Q2 با داده‌ی واقعی سنجیده نشده. **⚠ یافته‌ی Query کل درخواست:** کل درخواست ۱۲ Query است (۵ تا پروفایل + ۷ تا پلامبینگ مجوز: `permission` Middleware و `allowedKeys` در Shared props هر دو Roles/Permissions/Overrides را جدا می‌خوانند؛ با Session دیتابیسی ۱ Query کاربر هم اضافه می‌شود). بودجه‌ی ۶ تا برای **داده‌ی صفحه** رعایت شده و تست Arch/Feature تضمین می‌کند هر Query دیگر فقط مربوط به مجوز است؛ اگر Gate «کل درخواست ≤ ۶» خوانده شود، رفعش Memoize کردن `PermissionService::allowedKeys` در طول درخواست است (Core، خارج از این تسک). **تصمیم‌ها و انحراف‌ها از متن تسک:** (۱) `churn_risk_score` در PRD مقیاس ۰–۱۰۰ دارد (`numeric(5,2)`) نه ۰–۱، پس آستانه‌های ۰٫۷/۰٫۴ تسک نادرست بود؛ Badge از `churn_risk_level` (`low/medium/high/lost`) ساخته می‌شود که موتور Metrics ذخیره می‌کند (CLAUDE.md §4: آستانه از توزیع واقعی فروشگاه)، امتیاز فقط به‌صورت رشته‌ی دقیق کنار آن نشان داده می‌شود و Arch جلوی هر مقایسه‌ی عددی در مرورگر را می‌گیرد. (۲) پول **تومان** است نه ریال (CLAUDE.md §2)؛ Arch واژه‌ی «ریال» را در صفحه ممنوع می‌کند. (۳) نام ستون‌ها از Schema واقعی: `aov`، `r_score/f_score/m_score`، `orders.total`؛ `clv_confidence` و `churn_reason` هم اضافه شد چون CLAUDE.md نمایش CLV بدون اطمینان و ریسک بدون دلیل را ممنوع می‌کند. (۴) Controller جدا (`CustomerShowController`، Invokable) است چون `CustomerController` وجود ندارد و لیست P3-01 Invokable است؛ به‌جای Route Model Binding، سرویس خودش `findOrFail` می‌کند (مثل P3-02) چون Controller نباید Model ماژول را Type-hint کند و ۴۰۴ ← ۴۰۳ ترتیب حفظ شود (بی‌مجوز هرگز وجود یک id را نمی‌فهمد). (۵) «محصول خریداری‌شده» یعنی سفارش با `is_realized=true`؛ نام/SKU از `name_snapshot` آخرین خرید است (نه JOIN با `products`) تا آیتم بدون محصول حل‌شده (`product_id` NULL) هم بماند. (۶) **Rule 7** (SQL خام فقط Metrics/Analytics) با `array_agg` نقض می‌شد و Allowlist شل نشد: Query محصولات یک SELECT محدود روی خطوط همان یک مشتری است (ده‌ها ردیف) که در PHP گروه می‌شود. **⚠ استثنای مرزی ثبت‌شده:** جدول وابستگی PRD §07 برای Customers فقط `Core` را مجاز می‌داند، اما PRD خودش `CustomerProfileService::buildThreeSixty()` را در Customers گذاشته؛ سرویس با Query Builder جدول‌های `orders`، `order_items` و `customer_metrics` را **می‌خواند** بدون `use` کردن هیچ کلاسی از Orders/Metrics (Arch: فقط همین سه جدول، فقط‌خواندنی). اگر Orders↔Customers بخواهند این را Interface کنند، جهت وابستگی چرخه می‌سازد؛ **تصمیم مالک: اصلاح قاعده‌ی §07** (بخش «اصلاح قاعده‌ی وابستگی §07» در پایین‌ترین بخش این فایل). **تست:** ۵۴ تست خالص جدید (۱۹۴۳ ← ۱۹۹۷ سبز؛ ۲۶ سرویس، ۱۵ کنترلر، ۱۳ Arch، به‌علاوه به‌روزرسانی عمدی Arch P3-01 از «یک GET» به «دو GET»)، `tests/Arch` ۱۲۷/۱۲۷، PHPStan ۰ خطا؛ Mutation check ۲۰ نقض عمدی (S01–S05 تسک: Query اضافه ← ۴، ماسک حذف ← ۵، فیلتر حذف‌شده ← ۲، Null-handling متریک ← ۳، ایمیل در DTO ← ۴؛ به‌علاوه N+1، Realized، ترتیب/سقف، مجوز Route، Cut-off مرورگر، «ریال»، گروه‌بندی محصول و شمار) همگی گرفته شد؛ S12 (حذف Guard بخش معیارها) اول **زنده ماند** چون Arch فقط وجود `metrics !== null &&` را می‌دید، Assertion سفت‌تر شد و گرفته شد.

## اصلاح قاعده‌ی وابستگی §07 — Data dependency در برابر Module dependency

**تصمیم (مالک، گزینه‌ی ۱):** ماژول `Customers` می‌تواند جدول‌های `orders`، `order_items` و `customer_metrics` را **فقط‌خواندنی و با Query Builder** بخواند. این یک **وابستگی داده** (Data dependency) است، نه **وابستگی ماژول** (Module dependency): جدول وابستگی PRD §07 که برای `Customers` فقط `Core` را می‌دهد **تغییر نمی‌کند** و همچنان درباره‌ی **کد** است. **چرا:** PRD خودش `CustomerProfileService::buildThreeSixty()` را در `Customers` گذاشته و Customer 360 (PRD §18) به سفارش‌ها و متریک‌های یک مشتری نیاز دارد؛ Interface کردن آن از Orders/Metrics یک چرخه می‌سازد چون `Orders → Customers` و `Metrics → Customers` از قبل وجود دارند (§07)، و انتقال صفحه به Metrics مالکیت پروفایل را از ماژول مشتری جدا می‌کرد.

**مرز دقیق این استثنا:**
- **مجاز:** `DB::table('orders' | 'order_items' | 'customer_metrics')` با Query Builder و Binding، فقط `SELECT` (بدون هیچ نوشتن، Dispatch یا Log)، برای داده‌ی یک مشتری.
- **ممنوع (همان قواعد قبلی، بدون تغییر):** `use` هر کلاسی از `Orders`، `Metrics` یا هر ماژول دیگر (Model، Service، Enum، Event) جز `Customers\Models\Customer`؛ SQL خام (Rule 7: فقط Metrics/Analytics)؛ خواندن هر جدول دیگر ماژول‌های دیگر (مثلاً `products`، `audit_logs`، `sync_jobs`)؛ محاسبه‌ی معیار در لحظه (معیارها فقط از `customer_metrics` خوانده می‌شوند).
- **اعمال:** `tests/Arch/CustomerShowBoundaryTest.php` همین‌ها را قفل می‌کند: فقط این سه جدول (به‌علاوه `customers`)، بدون `use` کلاس ماژول‌های دیگر، بدون نوشتن، بدون SQL خام. تست‌های سراسری `ArchitectureTest` (جدول وابستگی کلاسی، Model بین‌ماژولی، SQL خام) **دست‌نخورده و سبزند** — چون این استثنا به‌خاطر ماهیت «داده، نه کلاس» هرگز رفتار آن‌ها را تغییر نمی‌دهد.
- **دامنه:** فعلاً فقط `CustomerShowService` (P3-03) از آن استفاده می‌کند. افزودن مصرف‌کننده‌ی دوم یا جدول چهارم یعنی تصمیم تازه و ثبت تازه در همین بخش.

`PRD.md` §07 دست‌نخورده ماند (سند مرجع محصول)؛ این بخش تفسیر پیاده‌سازی آن است و در تناقض با آن، PRD برنده می‌ماند مگر مالک خلافش را بگوید.

## P3-04 — Timeline مشتری با Cursor Pagination (`GET /customers/{customer}/timeline`)

**جدول `customer_events` وجود نداشت و ساخته شد** (`2026_09_20_130000_create_customer_events_table`): `id`، `customer_id` (FK، CASCADE؛ حذف نرم مشتری رویدادها را نگه می‌دارد)، `event_type varchar(50)`، `payload jsonb` (Nullable)، `happened_at` و `created_at` (`timestamptz`)، Index `(customer_id, happened_at DESC, id DESC)`. **تصمیم:** `event_type` مجموعه‌ی بسته است، نه متن آزاد: Enum `CustomerEventType` (`order_placed`، `order_refunded`، `note_added`، `status_changed`) به‌علاوه‌ی CHECK (CLAUDE.md §2/§3: هر ستون Enum-گونه CHECK دارد؛ «مثال» تسک همان چهار مقدار گرفته شد) و نوع تازه یعنی Migration تازه. ⚠ **هیچ چیزی در این Build در `customer_events` نمی‌نویسد** (نه Sync سفارش، نه یادداشت، نه تغییر وضعیت): Timeline روی داده‌ی واقعی تا وجود «نویسنده» خالی است و صفحه در این حالت بخش را **نشان نمی‌دهد** (مثل P3-03). تصمیم بعدی برای مالک: Backfill از `orders` و/یا نوشتن رویداد از `OrderService`/یادداشت‌ها (تسک جدا).

**سرویس و Cursor** (`CustomerTimelineService`، فقط‌خواندنی): ترتیب `happened_at DESC, id DESC`؛ Cursor = Base64 **URL-safe بدون Padding** از `{"happened_at":"<UTC ISO>","id":<int>}` (نیاز به Percent-encoding ندارد)، فقط و فقط در همین سرویس Encode/Decode می‌شود (Arch: نه Controller، نه Request، نه DTO، نه React)، Decode سخت‌گیرانه است (Base64 Strict، دقیقاً همین دو کلید، `id` عدد صحیح ≥ ۱، تاریخ واقعی که بعد از Parse عیناً برگردد، طول ≤ ۲۰۰) و فقط به‌شکل **مقدار Bind‌شده** وارد Query می‌شود؛ Cursor نامعتبر ← ۴۲۲ روی `cursor` پیش از هر Query، و Cursor همیشه داخل `customer_id` همان مشتری می‌ماند (Cursor مشتری دیگر چیزی فاش نمی‌کند، تست‌شده). یک ردیف اضافه خوانده می‌شود تا `has_more` **دقیق** باشد (صفحه‌ی کاملاً پر و بدون بعدی ← `false`). `per_page` پیش‌فرض ۲۰، حداکثر ۵۰ (بیشتر ← ۴۲۲ نه Clamp)؛ `pageFor()` برای Endpoint (مشتری حذف‌شده/ناموجود ← ۴۰۴، ۲ Query) و `page(Customer)` برای فراخوان‌ی که مشتری را قبلاً بارگذاری کرده (۱ Query).

**⚠ یافته‌ی اندازه‌گیری — و اصلاحش:** نسخه‌ی اول شرط Cursor را به‌شکل `OR` بازشده (`happened_at < ? OR (happened_at = ? AND id < ?)`) می‌نوشت؛ EXPLAIN روی داده‌ی مصنوعی (DB تست `heymode_testing`، ۴۳۰٬۰۰۰ رویداد، یک مشتری با ۳۰٬۰۰۰، همه در یک تراکنش Rollback‌شده) نشان داد شرط به‌صورت **Filter** اعمال می‌شود (۱۰٬۰۰۰ ردیف خوانده و دور ریخته، ۶۱۰ Buffer، ۲٫۵ms در عمق ۱۰٬۰۰۰) یعنی هزینه با عمق رشد می‌کرد — همان مشکل Offset. با `whereRowValues(['happened_at','id'], '<', [...])` (همان مقایسه‌ی سطری `(happened_at, id) < (?, ?)` تسک؛ API خود Query Builder است نه SQL خام، پس Rule 7 سالم می‌ماند) شرط **Index Cond** شد: ۵ Buffer، ۰٫۰۵۶ms، و صفحه‌ی ۲۰۰م هم‌هزینه‌ی صفحه‌ی اول. اندازه‌گیری نهایی (سرویس + PHP، p50/p95): صفحه‌ی اول ۳٫۳/۳٫۷ms، `per_page=50` ۵٫۷/۷٫۱ms، ۲۰۰ صفحه عمق ۵٫۷/۶٫۶ms. Arch شکل `whereRowValues` را قفل می‌کند. ⚠ این عددها روی DB **تست** با داده‌ی مصنوعی‌اند؛ روی DB توسعه اندازه‌گیری نشد چون Migration هنوز آن‌جا اجرا نشده.

**Payload و تاریخ:** فقط پنج کلید (`order_id`، `note`، `old_status`، `new_status`، `woo_order_id`) و فقط مقدار Scalar ساده (آرایه‌ی تودرتو نمی‌تواند کلید دیگری را بیرون ببرد)؛ کلیدها به ترتیب Allowlist خارج می‌شوند نه ترتیب ذخیره (JSONB کلیدها را جابه‌جا می‌کند)؛ بدون کلید مجاز ← `payload: null` (نه `{}`). ⚠ `note` متن آزاد است و به هر دارنده‌ی `customers.view` نشان داده می‌شود. هر رویداد `happened_at_jalali` (تاریخ جلالی **به‌همراه ساعت تهران**، ارقام ASCII مثل بقیه‌ی برنامه؛ مثال تسک با ارقام فارسی و بدون ساعت فقط توضیحی بود) و `happened_at_iso` (UTC، `…Z`) دارد و مقدار خام هرگز نمی‌رود (Arch: کلید `happened_at` فقط یک بار، داخل Cursor). **Frontend:** `CustomerTimeline.tsx` (خط عمودی در لبه‌ی Inline-start = راست RTL، آیکون بر اساس نوع با آیکون پیش‌فرض، Load-more با حالت‌های idle/loading/error، Cursor بدون دست‌کاری برگردانده می‌شود، آدرس از Wayfinder، پاسخ با Type-guard بررسی می‌شود)؛ `show.tsx` آن را از Props صفحه می‌گیرد و فقط اگر رویدادی هست نشان می‌دهد (بررسی `Schema::hasTable` در زمان اجرا پیاده نشد: Migration همین تسک جدول را تضمین می‌کند و بررسی، Query اضافه و مسیر مرده می‌بود).

**اثر بر P3-03:** صفحه‌ی ۳۶۰ صفحه‌ی اول Timeline را در Props می‌آورد، پس بودجه‌ی داده‌ی صفحه از ۵ به **۶ (دقیقاً حد مجاز)** رسید (Q6 همان «ده رویداد آخر» P3-03 بود)؛ کل درخواست ۱۳ Query است (۶ + ۷ پلامبینگ مجوز، یافته‌ی P3-03 در مورد `allowedKeys` سر جایش). تست‌ها/Arch P3-03 و P3-01 (شمار GET Routeها ۲ ← ۳) **عمداً** به‌روز شدند، نه سست: شمارش Query ۵ ← ۶، شکل `timeline`، و Arch پویای «customer_events» که حالا می‌خواهد سرویس ۳۶۰ از راه `CustomerTimelineService` بخواند نه مستقیم از جدول. **انحراف‌ها:** تست Unit فقط بخش خالص (Cursor و Payload) است چون Unit بدون Laravel/DB اجرا می‌شود؛ ترتیب و صفحه‌بندی روی ردیف واقعی در `tests/Feature/Modules/Customers/CustomerTimelineServiceDbTest.php`. Trailer کامیت طبق متن تسک «Claude Sonnet 4.6».

**تست:** ۱۲۴ تست خالص جدید (۱۹۹۷ ← ۲۱۲۱ سبز؛ Unit ۳۶، سرویس با DB ۲۷، کنترلر ۳۷، Schema ۶، Arch ۱۵، به‌علاوه ۳ تست تازه در تست‌های P3-03 برای Timeline داخل پروفایل)، `tests/Arch` ۱۴۲/۱۴۲، PHPStan ۰ خطا. **Mutation check ۲۰ نقض عمدی همگی گرفته و فایل‌ها برگردانده شد** (T01: Allowlist حذف ← ۱۲؛ T03: DESC←ASC ← ۱۰؛ T04: چک حذف‌شده ← ۲؛ به‌علاوه Tie-break، `<=`، `has_more`، تاریخ خام، سقف `per_page`، Scope مشتری، مجوز Route، Scalar، Jalali، Cursor مرورگر، کارت همیشه‌نمایان، Query هفتم، کلید اضافه، Cursor از ردیف اشتباه). ⚠ **T02 (Decode اشتباه) در دو شکل اول زنده ماند و هر دو «Mutant معادل» بودند، نه حفره‌ی تست:** (الف) جابه‌جایی `-_`↔`+/`: JSON این Cursor فقط بایت‌های `[0-9A-Za-z":,{}-]` دارد و هیچ‌کدام Base64 `+` یا `/` تولید نمی‌کند؛ (ب) حذف Padding: `base64_decode(..., true)` PHP بدون `=` هم می‌پذیرد، پس آن خط **کد مرده** بود و **حذف شد**. T02 با Decodeهای قابل‌مشاهده (id یک واحد بیشتر، ثانیه‌ی دیرتر) اجرا و گرفته شد.

**⚠ وضعیت DB توسعه:** Migration `customer_events` روی `heymode` **اجرا نشده** (فقط `heymode_testing`)؛ تا `php artisan migrate` صفحه‌ی `/customers/{id}` روی DB توسعه خطا می‌دهد چون Q6 جدول را می‌خواند.

## P3-05 — تب‌های سفارش‌ها/محصولات و یادداشت‌ها در Customer 360

**پنج Route** (همه `auth` + `customers.view`، `whereNumber`): `GET customers/{customer}/orders` (`customers.orders`)، `GET .../products` (`customers.products`)، `GET .../notes` (`customers.notes.index`؛ لازم بود چون بودجه‌ی صفحه‌ی ۳۶۰ با Timeline دقیقاً ۶ است و ۷م نمی‌شد)، `POST .../notes` (`customers.notes.store`) و `DELETE .../notes/{note}` (`customers.notes.destroy`). سرویس‌های `CustomerOrdersService`، `CustomerProductsService`، `CustomerNotesService` (ماژول Customers) و Controllerهای بدون منطق (یک سرویس، JSON/۲۰۱/۲۰۴). هر سه فهرست **Cursor** دارند (`per_page` پیش‌فرض ۲۵، حداکثر ۵۰، بیشتر ← ۴۲۲ نه Clamp؛ Cursor بد ← ۴۲۲؛ مشتری حذف‌نرم/ناموجود ← ۴۰۴؛ بی‌مجوز همیشه ۴۰۳ پیش از ۴۰۴)؛ `total_count` فقط در سفارش‌ها و با `COUNT(*)` جدا. اندازه‌گیری روی DB توسعه (`heymode`، پرسفارش‌ترین مشتری: ۴۵ سفارش، ۵۵ خط آیتم، فقط SELECT): سفارش‌ها ۳ Query، p50 ≈ ۳ms؛ محصولات ۲ Query، p50 ≈ ۲ms؛ صفحه‌ی دوم هم‌هزینه‌ی اول.

**Cursor واحد (`PageCursor`، ماژول Customers/Support):** Base64 URL-safe بدون Padding از JSON با **دقیقاً** کلیدهای شکل مورد انتظار (`instant` = UTC ISO واقعی، `id` ≥ ۱، `text` ≤ ۲۵۵ نویسه)، Decode سخت‌گیرانه و همیشه فقط **مقدار Bind‌شده**. قاعده‌ی P3-04 («Cursor فقط در `CustomerTimelineService`») با نیاز سه فهرست تازه تعارض داشت؛ به‌جای سه Codec، **یک Codec** ساخته شد و Timeline به آن Delegate می‌کند (متدهای عمومی و تست‌های Unit قبلی دست‌نخورده؛ Arch P3-04 عمداً به «Base64 فقط در `PageCursor`» تغییر کرد). سفارش‌ها و یادداشت‌ها با `whereRowValues` (Index Cond، درس P3-04) صفحه‌بندی می‌شوند. **Mutant معادل P3-04 حالا کشته شد:** جابه‌جایی `-_`↔`+/` روی Cursor فقط‌عددی قابل مشاهده نبود، ولی نام‌های فارسی محصول بایت‌هایی می‌سازند که `+`/`/` می‌دهند، پس تست Unit با ۳۰۰ نام آن را می‌گیرد (C01/C02).

**تصمیم‌ها و انحراف‌ها از متن تسک:** (۱) **مبلغ تقسیم بر ۱۰ نشد:** `orders.total` از قبل **تومان** است (P0-00: «هیچ تبدیلی لازم نیست»؛ مبلغ نمونه ۴۰۳٬۸۸۰) و P3-03 هم همان را بی‌تقسیم نشان می‌دهد؛ ÷۱۰ مبلغ‌ها را ۱۰ برابر کوچک و ارقام آخر را حذف می‌کرد (تست: `403885` عیناً برمی‌گردد؛ Arch: نه `intdiv`، نه `/ 10`). (۲) **ستون‌ها از Schema واقعی:** `order_items.sku`/`qty` (نه `sku_snapshot`/`quantity`)؛ Response شناسه‌ی داخلی سفارش را نمی‌فرستد (کلید `woo_order_id` یکتاست). (۳) **محصولات: GROUP BY/SUM در PHP، نه SQL:** Rule 7 (SQL خام فقط Metrics/Analytics، `ArchitectureTest`) `selectRaw`/`groupByRaw` را در Customers می‌بندد و Data dependency مصوب P3-03 فقط دسترسی به **جدول** را مجاز کرد نه SQL خام؛ Allowlist شل نشد. یک Query محدود خطوط Realized همان یک مشتری را می‌خواند و همان‌جا Group/Sort/Cursor می‌شود (ده‌ها تا صدها ردیف؛ پرسفارش‌ترین مشتری واقعی ۵۵ خط). گروه‌بندی طبق تسک با `name_snapshot` است (دو نام برای یک محصول = دو ردیف)، ترتیب `last_ordered_at DESC, name ASC` با **`strcmp`** نه `<=>` (PHP نام‌های عددی‌نما «10»/«9» را با `<=>` عددی مقایسه می‌کند و با فیلتر Cursor ناسازگار می‌شد؛ تست و Arch). اگر Rule 7 برای این دو فایل باز شود، جایگزینی با SQL Aggregate تغییر یک‌فایلی است. (۴) **`customer_notes` از قبل وجود داشت (P1-01)** و خالی بود، پس Migration «create» ممکن نبود: `2026_09_20_140000_harden_customer_notes_table` آن را **Alter** می‌کند: `user_id` NOT NULL + `RESTRICT` (قبلاً Nullable + SET NULL)، `created_at` NOT NULL با پیش‌فرض `now()`، CHECK طول متن ۱..۲۰۰۰ نویسه بعد از حذف فاصله‌های ASCII (نخستین نسخه‌ام فقط space را می‌بُرید و متن فقط‌Tab/Enter رد می‌شد؛ تست گرفت)، و Index `(customer_id, created_at DESC, id DESC)` به‌جای Index تک‌ستونی (یک `id DESC` بیش از متن تسک، برای Tie-break Cursor). **نام ستون همان `user_id` ماند** (PRD §09)؛ API آن را `author_id` می‌نامد. ⚠ این **تصمیم P1 را برمی‌گرداند** («یادداشت پس از حذف نویسنده می‌ماند: `user_id` NULL»): یادداشت‌ها همچنان می‌مانند، ولی کاربری که یادداشت نوشته را نمی‌شود حذف کرد. (۵) **مجوزها:** `customers.manage_notes` **وجود نداشت** و به `PermissionSeeder` افزوده شد (Owner و Manager خودکار می‌گیرند؛ Analyst/Support/Viewer نه). نوشتن یادداشت طبق ماتریس نقش‌ها (PRD §20) به `customers.note` نیاز دارد که Analyst (همه‌ی `view`ها) ندارد، پس `POST` علاوه بر `customers.view` (طبق تسک) **`customers.note` را هم می‌خواهد** (یک خط در Route). حذف: نویسنده **تا وقتی `customers.note` دارد** یا دارنده‌ی `manage_notes`؛ بقیه ۴۰۳؛ Deny همیشه برنده (`PermissionService`)؛ یادداشت **زیر همان مشتری** جست‌وجو می‌شود (شناسه‌ی یادداشت زیر URL مشتری دیگر ← ۴۰۴). (۶) **رویداد `note_deleted`** به مجموعه‌ی بسته‌ی `event_type` افزوده شد (Migration جدا `2026_09_20_140100`، چون Migration P3-04 منتشرشده است؛ `down()` ردیف‌های نوع تازه را پیش از بازگردانی CHECK پاک می‌کند) + Enum `CustomerEventType::NoteDeleted` + آیکون/متن در `CustomerTimeline`. (۷) **بدنه‌ی یادداشت هیچ‌وقت وارد `customer_events` نمی‌شود:** Payload فقط `{"note_id": n}` است (`record()` فقط شناسه می‌گیرد؛ Arch: نه بدنه، نه `$note`)، و Allowlist Timeline حتی `note_id` را هم می‌ریزد (Timeline «یادداشت افزوده شد» را بی‌متن نشان می‌دهد). **`store`/`destroy` هر کدام یک `DB::transaction`** با یادداشت + رویداد؛ تست دو جهت شکست را می‌سازد (Listener که Throw می‌کند) و مستقیماً Assert می‌کند رویداد در Transaction (Savepoint) نوشته می‌شود. (۸) **DemoDataSeeder:** سه یادداشت نمونه‌اش `user_id = NULL` می‌نوشت و با NOT NULL ۲۴ تست Seeder/Fixture را می‌شکست؛ حالا یک کاربر نمونه‌ی غیرقابل‌ورود (`password = '!'`، Idempotent) نویسنده‌ی آن‌هاست. Fingerprint فقط مشتری/سفارش/آیتم/عودت را Hash می‌کند، پس `expected_metrics.json` و GATE 2 **دست‌نخورده‌اند** (`git diff tests/fixtures` خالی).

**Frontend:** `CustomerTabs.tsx` (تب‌های «سفارش‌ها»/«محصولات»، Fetch ساده نه Inertia)؛ **هیچ Fetchی تا کلیک تب نمی‌شود**: صفحه هیچ تبی انتخاب‌شده/باز‌شده ندارد و Panel هر تب با **اولین انتخاب** Mount می‌شود و همان‌جا صفحه‌ی اول را می‌گیرد (تب بازشده Mount و پنهان می‌ماند)؛ لینک «مشاهده همه سفارش‌ها»ی P3-03 که به «همین صفحه» اشاره می‌کرد حالا تب سفارش‌ها را باز می‌کند. `CustomerNotes.tsx` همیشه دیده می‌شود و هنگام Mount از Endpoint خودش می‌خواند؛ فرم فقط برای دارنده‌ی `customers.note` (تعیین‌کننده Endpoint است)، دکمه‌ی حذف برای دارنده‌ی `manage_notes` یا نویسنده‌ای که هنوز `customers.note` دارد (کمی دقیق‌تر از «`auth.id === author_id`»ی تسک تا با ۴۰۳ سرور بخواند)، حذف دومرحله‌ای، متن یادداشت با `{note.body}` (React Escape می‌کند؛ Arch: نه `dangerouslySetInnerHTML`، نه `innerHTML`). ابزار مشترک: `hooks/use-cursor-list.ts` (هیچ Fetchی تا `loadFirst()`؛ Cursor عیناً برگردانده می‌شود) و `lib/http.ts`. `PhoneRevealButton` دست‌نخورده ماند (کپی `xsrfToken` آن‌جا هنوز هست).

**تست:** ۲۰۸ تست خالص جدید (۲۱۲۱ ← ۲۳۲۹ سبز؛ `PageCursor` Unit ۴۲، سفارش‌ها ۳۸، محصولات ۳۱، یادداشت‌ها HTTP ۴۰، یادداشت‌ها سرویس ۲۱، Schema یادداشت‌ها ۱۱، Arch ۲۵)، `tests/Arch` ۱۶۷/۱۶۷، PHPStan ۰ خطا. تست‌های قبلی که **عمداً** عوض شدند: Arch P3-01 و P3-02 (شمار Routeها: GET ۶، POST ۲، DELETE ۱)، Arch P3-04 (Cursor در `PageCursor`)، و تست Schema P1 «یادداشت با حذف نویسنده می‌ماند» که حالا RESTRICT را ثابت می‌کند. **Mutation check ۲۸ نقض عمدی همگی گرفته و فایل‌ها برگردانده شد** (N01: بدنه در Payload ← ۶، N02/N02b/N02c: حذف بدون نویسنده/بدون چک/نویسنده‌ی بی‌مجوز ← ۳/۹/۳، N03a/b: Transaction حذف‌شده ← ۳/۴، O01a/b: شرط Cursor ← ۵/۵؛ به‌علاوه مجوز Route، Scope مشتری و یادداشت، سقف متن، رویداد نوشته‌نشده/نوع اشتباه، `total_count`، ÷۱۰، تاریخ خام، ترتیب، گروه‌بندی، `strcmp`، Realized، `has_more`، Cursor URL-safe، دکمه‌ی حذف، Fetch زودهنگام، HTML). **⚠ حافظه‌ی Suite:** کل Suite در یک Process اجرا می‌شود و از حد پیش‌فرض CLI (۱۲۸MB) گذشت (`Allowed memory size exhausted` در اجرای کامل؛ با حد بالاتر همه‌ی ۲۳۲۹ تست سبز، Peak ≈ ۱۶۶MB)؛ `phpunit.xml` حالا `memory_limit=512M` دارد (تغییر زیرساخت تست، خارج از فایل‌های Feature).

**⚠ وضعیت DB توسعه:** Migrationهای `2026_09_20_130000` (P3-04)، `140000` و `140100` روی `heymode` **اجرا نشده‌اند** و مجوز `customers.manage_notes` هم Seed نشده (`php artisan migrate` سپس `db:seed --class=PermissionSeeder` و `RoleSeeder`)؛ تا آن موقع صفحه‌ی `/customers/{id}` روی DB توسعه خطا می‌دهد (Q6 جدول `customer_events` را می‌خواند). ⚠ **ریسک:** `ProfileController` حذف حساب را با `$user->delete()` انجام می‌دهد؛ کاربری که یادداشت (یا Reveal Log) دارد با RESTRICT خطای FK می‌گیرد (۵۰۰)، نه پیام مناسب. رفع (مثلاً غیرفعال‌سازی به‌جای حذف) در Core است و خارج از این تسک. ⚠ صفحه در مرورگر باز نشد: `tsc`، Lint، Build و Arch سبزند ولی رفتار تب‌ها/فرم دیده نشد.

## P3-06 — لیست سفارش‌ها و صفحه‌ی جزئیات

**دو Route** (هر دو `auth` + `orders.view`، بدون Soft-delete روی `orders` پس شناسه‌ی ناموجود همیشه ۴۰۴ ساده است): `GET /orders` (`orders.index`، لیست با فیلتر و Offset Pagination) و `GET /orders/{order}` (`orders.show`، جزئیات یک سفارش). سرویس‌ها در ماژول Orders (`OrderListService`، `OrderShowService`) هستند و جدول `customers` را **فقط با Query Builder** می‌خوانند، بدون `use` هیچ کلاسی از Customers — حتی همان یک Model (`Customer`) که CLAUDE.md §1 در همه‌جا مجاز می‌داند؛ این‌جا حتی آن استثنا هم عمداً استفاده نشد تا مرز با تست Arch سخت‌گیرانه‌تر باشد. این همان «Data dependency در برابر Module dependency»ی است که در P3-03 تصویب شد، این‌بار در جهت عکس (Orders جدول Customers را می‌خواند، نه برعکس).

**لیست:** `orders` LEFT JOIN `customers` (فقط برای `display_name`؛ سفارش بدون مشتری با `needs_phone_review=true` هرگز با Join حذف نمی‌شود). فیلترها با AND: `woo_order_id` دقیق، `status` (اعتبارسنجی در برابر مقادیر **واقعاً ذخیره‌شده** در `orders.status` با `Rule::in()`، نه Enum — CLAUDE.md هاردکد وضعیت سفارش را ممنوع می‌کند و PRD صریحاً می‌گوید Enum وضعیت سفارش نسازید)، `is_realized`، **`needs_phone_review`** (فیلتر مخصوص همین لیست؛ فیلتر `needs_review` در P3-01 چیز دیگری است — پرچم `customers.needs_review` روی جدول `customers`، نه `orders.needs_phone_review`) و بازه‌ی روز جلالی روی `ordered_at` با همان `JalaliDay` و قرارداد `[from, to]` P3-01 (شروع Inclusive، پایان Exclusive روی نیمه‌شب روز بعد). ترتیب `ordered_at DESC, id DESC`؛ Offset Pagination ۲۵تایی — برخلاف Cursor صفحه‌های Customer 360 (P3-04/۰۵)، این لیست برای مرور صفحه‌به‌صفحه توسط انسان است نه پیمایش کامل توسط اسکریپت، پس شمارش Offset این‌جا مناسب است. مبلغ **بدون تبدیل** (تومان، دقیقاً همان `total`).

**جزئیات، حداکثر ۳ Query:** Q1 ردیف سفارش، Q2 آیتم‌های همان سفارش، Q3 مشتری **فقط اگر `customer_id` دارد** (رد نمی‌شود، Skip می‌شود). تلفن همیشه Mask است — **برای هر بیننده، حتی دارنده‌ی `customers.view_full_phone`** — و راه کامل‌دیدن فقط `PhoneRevealButton` سمت `orders/show.tsx` است که به Endpoint حسابرسی‌شده‌ی P3-02 می‌زند؛ `needs_phone_review` یک Flag صریح در Response است. `profile_url` با `route('customers.show', $id)` ساخته می‌شود.

**⚠ انحراف آگاهانه از متن تسک:** متن Backend گفته بود «phone_normalized → mask مگر customers.view_full_phone» یعنی نمایش شماره‌ی کامل برای دارنده‌ی این مجوز — این مستقیماً تصمیم بسته‌شده‌ی P3-02 را نقض می‌کرد («لیست همیشه Mask؛ نمایش کامل فقط از راه Endpoint حسابرسی‌شده، یک‌به‌یک»)، درحالی‌که همان تسک در بخش Frontend صریحاً `PhoneRevealButton` را برای همین صفحه خواسته بود — یعنی خود تسک با خودش ناسازگار بود. تصمیم گرفتم با بخش Frontend و با تصمیم بسته‌شده‌ی P3-02 هماهنگ بمانم: DTO همیشه Mask می‌کند، `hasPermission` روی دکمه فقط UX است. این‌جا صراحتاً ثبت می‌شود تا اگر مالک قصد دیگری داشته، بازبینی شود.

**اندازه‌گیری روی DB توسعه** (`heymode`؛ ۲۴٬۵۷۰ سفارش، فقط SELECT): جزئیات سریع است (سفارش با مشتری: ۳ Query، p50 ≈ ۱٫۵ms؛ بدون مشتری: ۲ Query، p50 ≈ ۱ms). **⚠ یافته‌ی Index:** صفحه‌ی اول لیست **بدون فیلتر** کند‌تر است (p50 ≈ ۳۰–۵۵ms، تا ۲۲۴ms در بدترین حالت مشاهده‌شده): `EXPLAIN` نشان می‌دهد `Seq Scan` روی کل جدول (۷۰۶ Buffer) برای مرتب‌سازی `ordered_at DESC, id DESC` — ایندکس‌های موجود (`orders_customer_ordered_at_index`، `orders_realized_ordered_at_index`) هیچ‌کدام با `ordered_at` **به‌تنهایی** (بدون `customer_id`/`is_realized` در جلو) کار نمی‌کنند، و `COUNT(*)` صفحه‌بندی هم روی همین جدول Seq Scan می‌زند. این لیست بدون فیلتر دقیقاً حالت «باز کردن صفحه‌ی سفارش‌ها بدون هیچ فیلتری» است که به‌احتمال زیاد رایج‌ترین بار اول است. **تصمیم من:** بدون اجازه Migration/Index تازه نساختم (خارج از فایل‌های اعلام‌شده‌ی این تسک)؛ ایندکس پیشنهادی `(ordered_at DESC, id DESC)` روی `orders` برای تصمیم مالک ثبت می‌شود.

**تست:** ۵۱ تست خالص جدید (۲۳۲۹ ← ۲۳۸۰ سبز؛ لیست ۱۸، جزئیات ۱۹، Arch ۱۴)، `tests/Arch` ۱۸۱/۱۸۱ (شامل به‌روزرسانی عمدی شمار GET در `CustomerListBoundaryTest`: ۶ ← ۸)، PHPStan ۰ خطا. **Mutation check ۱۰ نقض عمدی همگی گرفته و فایل‌ها برگردانده شد** (OR01: فیلتر `needs_phone_review` حذف ← ۱؛ OR02: Mask حذف از DTO ← ۳؛ OR03: Query اضافه بدون تغییر رفتار ← ۲؛ OR04: سفارش بدون مشتری Throw به‌جای `customer: null` ← ۴؛ به‌علاوه فیلتر `is_realized`/`status`/بازه‌ی تاریخ، مجوز Route، تقسیم مبلغ بر ۱۰، و Scope آیتم‌ها). Migration تازه‌ای لازم نبود.

## P3-07 — لیست محصولات با آمار فروش (پایان Sprint 3)

**یک Route**، `GET /products` (`products.index`)، پشت `auth` + **`catalog,view`** — این Permission وجود نداشت و به `PermissionSeeder` اضافه شد (Owner/Manager/Analyst به‌صورت خودکار می‌گیرند، چون قاعده‌ی موجودشان است؛ Support/Viewer مثل `orders.view` بدون تغییر عمدی نقشه‌ی نقش‌ها نگه داشته شدند). سرویس (`ProductListService`) جدول‌های `order_items`/`orders` را **فقط با Query Builder** می‌خواند و هیچ کلاسی از ماژول Orders را `use` نمی‌کند — حتی استثنای `Customer` که CLAUDE.md همه‌جا مجاز می‌داند این‌جا هم عمداً استفاده نشد؛ همان «Data dependency در برابر Module dependency»ی P3-03/۰۶، این‌بار سمت Catalog.

**⚠ استثنای Rule 7 برای Aggregate فروش Catalog (تصمیم آگاهانه، نه سهوی):** متن تسک ادعا کرده بود «Rule 7 فقط Metrics/Analytics را محدود می‌کند»، اما تست واقعی (`ArchitectureTest::it confines raw SQL...`) فقط همان دو ماژول را Allow‌list می‌کرد — Catalog از قبل مجاز نبود. با این حال SUM/COUNT/MAX گروه‌بندی‌شده در یک ردیف در Laravel Query Builder بدون `selectRaw`/`orderByRaw` امکان‌پذیر نیست، و CLAUDE.md §3 صریح می‌گوید «تحلیل سنگین یک عبارت SQL است، هرگز حلقه‌ی PHP روی ردیف‌ها» — محاسبه‌ی این آمار (روی همه‌ی سفارش‌های Realized کل فروشگاه، نه یک مشتری) با حلقه‌ی PHP دقیقاً همان قاعده را نقض می‌کرد. **تصمیم:** Rule 7 فقط برای یک فایل، `app/Modules/Catalog/Services/ProductListService.php`، Amend شد (`ArchitectureTest`، Allowlist جدا از Prefix ماژول‌ها)؛ بقیه‌ی Catalog همچنان ممنوع است (تست تازه `CatalogBoundaryTest` این را قفل می‌کند) و هر Fragment خام همان فایل باید رشته‌ی ثابت باشد — هیچ `$` یا Concatenation ورودی کاربر در آن راه ندارد (تست می‌سنجد). این دقیقاً هم‌خانواده‌ی انحراف P3-05 برای گروه‌بندی محصولات مشتری است، با این تفاوت که آن‌جا مقیاس کوچک (سفارش‌های یک مشتری) اجازه داد PHP جایگزین SQL شود؛ این‌جا مقیاس کل فروشگاه است و SQL درست‌تر بود.

**Aggregate:** `order_items JOIN orders WHERE orders.is_realized = true GROUP BY product_id` → `total_qty_sold`، `total_revenue`، `order_count` (`COUNT(DISTINCT order_id)`، تا دو خط یک سفارش دوبار شمرده نشود)، `last_sold_at`؛ با `leftJoinSub` روی `products`. محصول بدون فروش (بدون تطبیق LEFT JOIN) در DTO صفر می‌شود (نه Null — صفر واقعیتی است، نه مقدار گم‌شده)، `last_sold_at` همچنان Null (صفحه: «هنوز فروش نداشته»). **SKU:** جدول `products` اصلاً ستون `sku` ندارد (بر خلاف فرض متن تسک) — SKU روی `product_variations` است و هر محصول (Simple یا Variable) دست‌کم یک Variation دارد. SKU نمایشی = پایین‌ترین `id` Variation دارای SKU (برای محصول Simple دقیقاً همان SKU؛ برای Variable فقط یک نماینده است، نه یکتا) با `leftJoinSub`؛ فیلتر SKU دقیق روی **هر** Variation محصول با `whereExists` می‌گردد، نه فقط SKU نماینده. `status` Enum خود Catalog (`ProductStatus`) است، نه رشته‌ی خام Woo مثل وضعیت سفارش — پس اعتبارسنجی با `Rule::enum()` این‌جا مجاز است. ترتیب پیش‌فرض `COALESCE(sales.total_revenue, 0) DESC, products.id DESC` (Postgres پیش‌فرض NULL را در DESC اول می‌گذارد؛ بدون Coalesce محصولات بی‌فروش بالای لیست می‌افتادند).

**Woo Admin Link:** `admin_url` سمت Backend از `config('woo.base_url')` ساخته می‌شود (هرگز در Frontend)، فقط وقتی مقدار دارد، وگرنه Null و صفحه فقط نام را متن ساده نشان می‌دهد.

**تست:** ۳۸ تست خالص جدید (۲۳۸۰ ← ۲۴۱۸ سبز؛ کنترلر ۲۲، `CatalogBoundaryTest` ۱۶) + `ArchitectureTest` Amend‌شده، Arch کل ۱۹۷/۱۹۷ (شمار GET در `CustomerListBoundaryTest`: ۸ ← ۹)، PHPStan ۰ خطا. **Mutation check ۳ نقض عمدی طبق تسک همگی گرفته و فایل‌ها برگردانده شد** (C01: حذف فیلتر `is_realized` از Aggregate؛ C02: ترتیب ASC؛ C03: محصول بدون فروش Null به‌جای صفر).

### تأیید Gate پایان Sprint 3 — `CustomerShowServiceTest` (P3-03)

روی DB توسعه (`heymode`؛ پرسفارش‌ترین مشتری واقعی: ۴۵ سفارش)، فقط SELECT:

| سطح | Query | p50 | p95 | بیشینه |
|---|---|---|---|---|
| سرویس (`CustomerShowService::show`) | **۶** (دقیقاً بودجه) | ۴٫۰۲ms | ۷٫۱۳ms | ۷٫۴۰ms |
| کل درخواست HTTP (Auth+Permission+Shared props+Render) | ۱۳ (۶ داده + ۷ پلامبینگ مجوز، یافته‌ی مستندشده در P3-03) | ۲۲٫۷۳ms | ۲۸٫۶۱ms | ۱۹۵٫۳۳ms |

هر دو رقم به‌مراتب زیر ۸۰۰ms هستند. تست خودکار (`php artisan test --filter=CustomerShowServiceTest`) نیز کاملاً سبز است: ۲۸/۲۸. **نتیجه: Gate Sprint 3 (Customer 360 ≤ ۶ Query و < ۸۰۰ms) عبور کرد ✓** — هم روی داده‌ی تست و هم روی داده‌ی واقعی توسعه.

**Sprint 3 کامل شد:** P3-01 تا P3-07 هرکدام Commit جدا دارند؛ صفحات مشتریان (لیست/۳۶۰/Timeline/تب‌ها/یادداشت)، سفارش‌ها (لیست/جزئیات) و محصولات همگی پشت مجوز مخصوص خودشان، همه فقط‌خواندنی به‌جز یادداشت‌ها (P3-05) که هرکدام یک `customer_events` هم می‌نویسند.
